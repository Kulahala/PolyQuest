# PolyQuest Documentation Archive

> 本文件是文档维护门（DOC-MAINT-01）的历史归档，不是当前行为或路线图的权威来源。当前运行时事实以源码、Config 和实际资产为准；当前架构契约见 ARCHITECTURE.md，当前路线见 ROADMAP.md。
>
> 初始归档建立于 2026-08-29。前置内容是维护开始时的工作区快照，包含此前已经存在的未提交路线调整，不等同于 HEAD `f474515` 的干净快照；后续追加条目以各自标题标注其 HEAD/working-tree 状态。它保留文档维护前的路线图、Architecture 顶部历史状态段和 TODO-02B3 计划/收口记录，便于追溯；不要从历史记录推断尚未实现的未来项。

## Pre-Maintenance ROADMAP.md Snapshot

# PolyQuest Roadmap

## Purpose And Scope

PolyQuest is a UE 5.8 C++ GAS-first, single-player stylized action RPG. It rebuilds the proven player-facing contracts from the UE 5.7 `Test` project, rather than performing an in-place copy of its FSM implementation, assets, or save data.

The previous `Test` project remains the FSM behavior reference and validation baseline. PolyQuest reuses proven gameplay contracts after deliberate GAS redesign, but does not retain its C++ state machine, save schema, authored assets, or Marketplace content wholesale.

## Current State

- UE 5.8 Third Person C++ template created.
- Official Unreal MCP and VibeUE-enhanced editor access verified read-only.
- `GameplayAbilities` is available in the editor; `TODO-00B` added the game-module GAS dependencies, established the player ASC/AttributeSet contract, and registered the initial project Gameplay Tags.
- `TODO-01A` has completed its native C++ lifecycle and a local PIE fixture. Its selected player mesh/Skeleton/material closure and animation sequences are stable source assets; its GameplayAbility, GameplayEffects, Montage, AnimBP, `BP_Player`, and input assets remain deliberately uncommitted authoring WIP.
- `TODO-00C` replaced the active template route with `/Game/Maps/Scene01`, a desktop-only product Controller route, and a closed ThirdPerson/Variant retirement. The user recompiled `PolyQuestEditor` and replayed the existing PIE validation path after cleanup.
- `TODO-01C` has completed the first local GAS Dodge, Stamina exhaustion/recovery, attack cancellation, and NotifyState-timed invulnerability loop. Its authored GA/GE, Montage, AnimBP, Blueprint, input, retargeting, map, and test-fixture assets remain deliberately local WIP.
- `TODO-01E` has completed the native Primary short/hold arbitration and Charged Attack lifecycle. Its authored GA/GE, Montage, retargeted Sequence, Blueprint, Loadout, and input assets remain deliberately local WIP.
- `TODO-01F` has completed the native Sprint, Jump, Sprint Jump air-speed, and optional Sprint Attack lifecycle. Its authored GA/GE, Montage, retargeted Sequence, Blueprint, AnimBP, Loadout, input, and map assets remain deliberately local WIP; the dedicated Sprint locomotion loop is deferred to `TODO-07B`.
- `TODO-01G` has completed a health review of the player-combat foundation. It repaired synchronous Montage-end reentry in Light and Charged startup and aligned Dodge with active-Montage identity filtering and unified teardown.
- `TODO-01H` has completed the native shared melee-delivery foundation for Light, Charged, and Sprint Attack. `TODO-03A` has since moved the player path to an equipped weapon definition and runtime markers; the fixed `WeaponMesh` / `BladeTraceBase` / `BladeTraceTip` lookup remains only as the valid authored enemy fixture fallback. Mutable authored combat assets remain local WIP and are not a clean-checkout fixture.
- `TODO-02A` has completed the first enemy native AI and melee-intent foundation. The configured local Scene01 fixture has user-confirmed PIE and visual evidence; its mutable Blueprint, StateTree, Gameplay Ability/Effect, Montage, AnimBP, marker, collision, map, and imported-asset setup remains local WIP.
- `TODO-02B` has completed the fixed-world oblique Perspective camera and planar action-facing foundation. The user confirmed the focused Scene01 PIE and visual route, including the accepted position-lag response to empty-space Root Motion camera motion. Authored input, Blueprint, and Scene01 presentation assets remain local WIP.
- `TODO-02C1` has completed the first enemy's single-attack Profile, exact 2D actor-center reach, and post-attack cooldown contract. The user confirmed compile and focused Scene01 PIE; the authored DataAsset, StateTree, Blueprint, Gameplay Ability/Effect, Montage, AnimBP, and map fixture remain local WIP.
- `TODO-02C2` through `TODO-02C3C` completed the enemy terminal, death-presentation, ragdoll, first charged-damage hard-interrupt, and first Poise/Stance Break slices. C3B and C3C accepted in-place reaction/stance playback; C3F subsequently delivered the grounded Root Motion Big reaction while Stance Break remains in place. Related authored Content remains local WIP.
- `TODO-02D1` has completed the first player directional Guard and Guard Break slice. The user confirmed `PolyQuestEditor` compilation and Scene01 PIE; Guard/Guard Break authored fixtures remain local `Content/**` WIP rather than a clean-checkout asset closure.
- `TODO-02D2` has completed the first Q-triggered timed Parry and enemy Poise counter slice. The user confirmed the post-repair `PolyQuestEditor` compile and Scene01 PIE route; authored Parry input, Gameplay Ability/Effects, Montage, Blueprint, and map assets remain local `Content/**` WIP rather than a clean-checkout asset closure.
- `TODO-02E` has audited the completed bidirectional melee loop with no blocker found, removed the retired legacy Dodge/Sprint input properties and the unreferenced `Input.Dodge` tag, renamed the project identity in `DefaultGame.ini`, and synchronized the lagging README/ARCHITECTURE documentation. The user confirmed the post-lean `PolyQuestEditor` compile, `BP_Player` re-save, and Scene01 PIE regression smoke.
- `TODO-03A` has completed the first player equipment and Combat Loadout foundation. An authored melee-weapon definition and player-only runtime component now own the equipped display, trace markers, weapon-granted specs, and optional Loadout selection through a fail-closed swap transaction; the user confirmed the post-repair `PolyQuestEditor` compile and Scene01 PIE route. DataAssets, Blueprints, temporary swap-debug authoring, and all other `Content/**` work remain local WIP.
- `TODO-03A1` completed the initial equipment-domain contract: the `UWeaponDefinition` base with its compatible melee subtype, the two-tag Defense Profile, both hand slots with TwoHanded/OffHand composition rejection, the keep-if-compatible prepared `1-4` layout with exact-handle activation, and the component as the single input resolver. `TODO-03A2` has since retired the reference-list combat-action layer in favor of direct GameplayAbility classes and completed the first Unarmed path; drop-swap, Shield, enemy presets, and Bow remain later work.
- `TODO-03A2` completed the player MainHand/Unarmed combat contract: class-based base/candidate/prepared action arrays replace `GrantedWeaponAbilities` and `UCombatActionDefinition`; `DA_Weapon_Unarmed` uses the owner SkeletalMesh socket for its ordinary marker pair without a display mesh; and the first exact-handle skill route uses `UPlayerMeleeSkillAbility` plus `GA_Skill_Whirlwind`. The user confirmed the final local Editor readback and Scene01 PIE route, including normal Whirlwind playback from `1`; authored GA/GE/Montage/DataAsset assets remain local WIP.
- `TODO-03A3` completed the world equipment pickup and atomic drop-swap contract: `AWorldWeaponPickup` provides map interaction and collision management, while `UWeaponEquipmentComponent` executes atomic world pickup transactions (`TryEquipWorldPickup`) with bidirectional normalization (TwoHanded clears OffHand; Shield while holding TwoHanded clears main to `UnarmedFallbackDefinition`), Apply failure and Drop failure rollbacks, provisional drop collision staging, and `0.5s` `FormerOwner` anti-repickup protection. The user confirmed `PolyQuest.Equipment.TransactionMatrix` automation test Success across all 11 test cases including full ASC AbilitySpec rollback verification, then confirmed the Scene01 manual pickup route with displaced weapons visible on the ground near the previous player position.
- `TODO-03A3B` completed the melee sweep trace socket authoring and calibration contract: `UMeleeWeaponDefinition` supports optional Static Mesh sockets `BladeBaseSocketName` / `BladeTipSocketName` with fail-closed all-or-nothing pair validation against `WeaponMesh->FindSocket`, including non-coincident local socket locations. `UWeaponEquipmentComponent::ApplyComposition` attaches transient Blade Base/Tip `USceneComponent` markers to the displayed Static Mesh via `SnapToTargetNotIncludingScale`, while legacy displayed definitions retain relative-vector mode and Unarmed retains owner-SkeletalMesh attachment. The user completed Visual Studio compilation, configured `Trace_Base` and `Trace_Tip` on `SM_Wep_Ornate_Sword_02` and `DA_Weapon_Sword`, and confirmed `PolyQuest.Equipment.TransactionMatrix` automation test Success (covering real socket resolution, marker attachment, incomplete-pair rejection, missing-socket rejection, and coincident-location rejection).
- `TODO-03A4` completed the first OffHand Shield composite-defense contract: tag-only `UDefenseProfileDefinition` resolves OffHand override -> MainHand fallback -> generic default, while an explicit profile must resolve exactly one provider-owned Guard class with an exact profile tag plus a hierarchical match in the generic Guard category, and one distinct provider-owned Parry class with the equivalent Parry tags, before the equipment transaction mutates state. This accepts Editor-authored child-only GameplayTag containers without accepting unrelated sibling tags. Shield grants only those component-owned defense specs; it does not take over MainHand attacks, trace/damage delivery, or `1-4` selection. The user confirmed the repaired `PolyQuest.Equipment.TransactionMatrix` run with `Success`; the later Shield Guard locomotion-pose twist was resolved by `TODO-03A6B`, not by changing the defense/equipment transaction.
- `TODO-03B-1` completed the first straight player Bow and Projectile slice; `TODO-03B-2` added constrained pointer-facing target assist and limited homing; and `TODO-03B-3` now gives an enabled Player Bow one extra Release-time target source: a current B2-validated lock. The arrow still begins along the pointer, snapshots its target once, and never responds to later lock switches, clears, or target death. No lock or an invalid lock retains B2's automatic query (including its 6% edge overscan), while disabled Target Assist remains straight flight. The user confirmed `PolyQuest.Projectile.Lifecycle`, `PolyQuest.Projectile.TargetAssist`, and `PolyQuest.Player.LockOn` Automation plus focused PIE; Bow/Projectile authored assets remain local `Content/**` WIP.
- `TODO-03A6A` initially introduced the five-way ordinary locomotion presentation enum, and `TODO-03A6B` supplied the distinct active Shield Guard branch. `TODO-03A6C` has now replaced the composition model with independent facts: MainHand-only `Default` / `LightSword` / `HeavySword` / `Bow = 4` locomotion, a committed OffHand Shield-presentation query, and exact active Shield Guard state. The obsolete `SwordShield` enum/OffHand composition fields are removed; raw ordinal `3` fails closed, existing Bow assets retain numeric value `4`, and `PolyQuest.Equipment.TransactionMatrix` covers the split plus rollback non-drift. The user confirmed Editor compilation/readback, fourteen Automation suites, and focused Scene01 PIE. The old `BS_SwordShield_Walk_Run` asset remains retained local Content after its AnimBP branch was removed; it is not a runtime selection path or an approved deletion target.
- `TODO-03AI3` and `TODO-03AI3B` complete the first Enemy Root-Motion-facing and bounded combat-target-retention closure. The Controller remains the single owner of target, Focus, Home leash, tactical Reposition pace, and StateTree events; a temporary visual-sight loss after initial acquisition no longer forces an Enemy to return Home while the Player remains inside the existing memory and leash bounds. Focused Scene01 PIE was confirmed for the player loop, and after the late-perception lifecycle repair the user confirmed all eighteen Editor Automation suites. No separately logged Development Editor build is claimed.
- `TODO-02B3` completes the bounded Player Lock-On retention refinement: acquisition, cycling, and death-handoff scans remain strict, while an already-owned target uses one fixed per-axis `15%` retention margin. A retained target outside strict candidates keeps its lock and makes cycle input a no-op; Bow's independent `6%` automatic Target Assist boundary is unchanged. The user confirmed the focused Lock-On/Projectile Automation and Scene01 PIE route; no separate Development Editor compile log is claimed, and authored `Content/**` plus Config WIP remain local.
- External source packages remain under `Content/Assets/`. Imported content is not a production integration merely because it is present locally; skeleton, weapon, socket, animation, and presentation decisions still require their named stage validation.

### Current Player-Combat Sequence

Before introducing the first ranged enemy, the player-combat experience gate is:

`TODO-03A3E → TODO-07B4 → TODO-03A7 → TODO-05A → TODO-05B → TODO-05C → TODO-03C`

`TODO-07B4` is intentionally placed here as a presentation gate; it does not alter the technical dependency that `TODO-03A7` precedes `TODO-05A`, and `TODO-05A` precedes `TODO-05B`.

## Test-To-PolyQuest Migration Contract Inventory

The old `Test` project is evidence for player-facing behavior, not a source tree to copy. The following contracts were implemented and manually validated there; PolyQuest will rebuild them through its own GAS, StateTree, data, asset, and save boundaries.

| Verified Test contract | PolyQuest reconstruction boundary | Owning stage |
| --- | --- | --- |
| Focused player profile plus small attack/action/reaction DataAssets | A PolyQuest character/ability manifest grants abilities and references focused authored data. It does not become a second runtime state machine. | `TODO-01A`, `TODO-01B` |
| Shared physical input intent across weapon and loadout combat styles, including press/hold/release semantics | A shared Gameplay Mapping Context emits stable input intent; GAS and the active weapon/loadout route that intent to the current melee, charged, ranged, or selected spell ability. `TODO-01C2` adds the deliberately adopted single-key Dodge/Sprint short-hold arbitration without making physical input a second combat state source. | `TODO-01C1`, `TODO-01C2` |
| One complete `Montage + EntrySection` per linear combo entry, with local damage, stamina, poise, and optional warp data | A GAS-owned linear combo-chain asset. Each entry remains a complete authored attack module; `Entry` / `Recovery` / `End` are semantic phases, and `TODO-02F` owns local playback-rate control rather than duplicate Sequences or rate-only Sections. Runtime continuation state remains inside the active attack ability; any Motion Warping is an explicit per-entry opt-in owned by `TODO-03A7`, never a global combo behavior. | `TODO-01D`, `TODO-02F`, `TODO-03A7` |
| One LMB pre-input during `ComboWindow`; early and late continuation during `ComboBranchWindow` | The active light-attack ability owns one continuation buffer. Notify states emit semantic begin/end events only; there is no global input buffer. | `TODO-01D` |
| Recovery `CancelWindow` permits Dodge, Parry, Block, or Potion only after each action's own preflight | A scoped cancellation opportunity exposed by the active attack ability. Higher-priority actions remain immediate ability requests, not buffered actions. | `TODO-01C`, `TODO-01C3`, `TODO-01D`, `TODO-02D`, `TODO-03E` |
| Weapon-motion trace window, swept trace, team filtering, one-hit protection, and centralized hit resolution | A single GAS damage path driven by semantic attack timing and a validated hit resolver. A Trace Window continuously sweeps between prior/current world-space Blade Base/Tip samples; animation timing never directly owns Health or Poise mutation. | `TODO-01A`, `TODO-01H` |
| Authored melee-weapon definition, equipped display/trace markers, weapon-granted Ability specs, and Combat Loadout selection | One player-owned equipment component performs a validated, rollback-capable equip/swap transaction. It changes future grants and input routing only; it does not create a parallel combat state or melee delivery path. | `TODO-03A` |
| World pickup proximity prompt and direct interaction hint | The existing `AWorldWeaponPickup` overlap/`CanInteract` and nearest-candidate route drives a local presentation-only prompt. `UWeaponEquipmentComponent::TryEquipWorldPickup` remains the sole equipment mutation and transaction owner; no generic inventory or interaction authority is introduced. | `TODO-03A3`, `TODO-03A3E` |
| Hold-to-charge, release attack, stamina exhaustion, and interruption cleanup | A dedicated charged-attack ability consumes the existing hold/release intent after Combo continuation arbitration; do not fold it into the linear combo asset. | `TODO-01C`, `TODO-01E` |
| Sprint state and Sprint Attack | Establish and validate a real Sprint movement/input/stamina contract before a Sprint Attack may consume it. | `TODO-01F` |
| Screen-space lock-on, target switching, camera/facing ownership, and free-run exception | `TODO-02B` retains the fixed elevated oblique camera and camera-relative movement. Completed `TODO-02B1`/`TODO-02B2` provide the Player-only lock, continuous eligible locomotion facing, Sprint free-run, and death-only retargeting without restoring over-the-shoulder orbit, camera recentering, or per-frame Root Motion rotation overrides. `TODO-02B3` keeps acquisition/cycle strict while adding bounded hysteresis only to retention. | `TODO-02B`, `TODO-02B1`, `TODO-02B2`, `TODO-02B3` |
| Enemy attack-entry DataAssets, poise/stance break, hit/death/reaction safety, melee/ranged delivery | StateTree selects high-level intent; enemy GameplayAbilities execute attacks and GameplayEffects own combat mutation. | `TODO-02A`, `TODO-02C1`, `TODO-02C2`, `TODO-02C3A`, `TODO-02C3D`, `TODO-02C3B`, `TODO-02C3C`, `TODO-02C3E`, `TODO-02C3F`, `TODO-02C3G`, `TODO-02C3I`, `TODO-02C3J`, `TODO-02C3M`, `TODO-02C3N`, `TODO-03C` |
| Checkpoint, transient Gold, fixed rewards, clear persistence, fog gate, and one-time defeat behavior | New PolyQuest persistence contracts with new stable IDs and validation fixtures. No old SaveGame schema or identifiers transfer. | `TODO-03D`, `TODO-03F` through `TODO-04A` |
| Front critical, backstab, and stagger execution use a valid punish target, animation timing, alignment, damage once-only, and teardown cleanup | Explicit GAS abilities after normal combat, stance break, bounded target reservation, and the selected-attack Motion Warping gate are proven. Stagger execution and stagger backstab remain two explicit actions sharing only narrow reservation/cleanup primitives; no generic finisher dispatcher is implied. | `TODO-03A7`, `TODO-05A`, `TODO-05B`, `TODO-05C` |

### Migration Rules

- Preserve the player-visible contract when it is validated; redesign its runtime ownership for GAS rather than reproducing old fields, booleans, `EActionState`, or montage-delegate code.
- Animation is the timing and presentation source. Notify and NotifyState implementations send narrow semantic events to the active ability; only abilities, effects, and the hit resolver mutate gameplay.
- A completed, cancelled, or interrupted Montage callback from a replaced combo entry must never end or clear the successor entry. The GAS combo stage owns an explicit stale-callback guard and one `EndAbility` cleanup route.
- DataAssets hold authored references and tuning, never active input buffers, current combo indexes, mutable targets, or save state.
- Motion Warping is an opt-in product presentation stage, not a foundation prerequisite. `TODO-03A7` may adopt it only for named attacks whose selected Montage has verified Root Motion and a matching Motion Warping window; absent a valid asset, the existing attack path remains unchanged. The old Test implementation and assets are reference evidence, not a drop-in contract.
- Do not migrate a behavior merely because Test contained it. Each stage must retain its explicit player benefit, prerequisites, and fresh PolyQuest validation fixture.

## Done Milestones

- [x] `TODO-00A: Repository And Documentation Bootstrap v1`
  - Established the UE 5.8 template baseline, Git LFS, generated-output ignores, project documentation, and verified official Unreal MCP/VibeUE read routes.
  - Committed as `f64fbd2`; no GAS gameplay code, production asset integration, or template retirement was included.
- [x] `TODO-00B: GAS Core Contract v1`
  - Added the public GAS module dependencies, established character-owned ASC and AttributeSet initialization on possession, and registered the initial config-first project Gameplay Tags.
  - The user compiled `PolyQuestEditor`, verified the active `BP_Player`/`BP_PlayerController` route in PIE, confirmed the four attributes through `ShowDebug AbilitySystem`, and validated movement, look, and jump. No ability, effect, cue, replication, or combat behavior was added.
- [x] `TODO-01A: Player Ability Lifecycle And One-Hit Attack v1`
  - Established the first local player attack path: Enhanced Input requests `Ability.Attack.Light`; the ASC activates `ULightAttackAbility`; `CommitAbility()` applies the authored cost; Montage timing emits `Event.Attack.Light.Hit`; one sphere sweep applies an authored damage effect through the target ASC; all completion and teardown paths converge through `EndAbility()`.
  - The user compiled `PolyQuestEditor` and verified the local PIE fixture: movement/look/jump remain intact, a successful light attack spends stamina once, damages the target once, rejects reactivation during the active Montage, and works again after normal recovery.
  - Main review repaired premature end-on-blend-out behavior. A fresh adversarial review found no remaining P0/P1 issue. The local authored GA/GE/Montage/AnimBP/player/input configuration is intentionally excluded from this commit while it evolves, so this records validated local behavior rather than a clone-ready fixture.
- [x] `TODO-00C: Template Cleanup v1`
  - Replaced the template map route with `/Game/Maps/Scene01`, moved the retained GameMode and PlayerController source roots into `Framework/` without changing their reflected class names, and reduced the PlayerController to local desktop mapping-context setup.
  - Retired `APolyQuestCharacter`, the ThirdPerson map/Blueprint/External Actor closure, the generated Variant source/content closures, and legacy Third Person redirects after scoped Editor-reference and static evidence.
  - The user compiled `PolyQuestEditor` and verified Scene01 startup, desktop input, movement, look, jump, LMB stamina cost, and target damage. Main normal review and an adversarial fallback found no blocking issue; the requested independent Reviewer could not start because the configured service returned HTTP 503.
- [x] `TODO-01B: First Stylized Player Asset Integration v1`
  - Integrated the local Dungeon Knight presentation fixture: `SM_Wep_Ornate_Sword_02` is display-attached through `Weapon_R`, and one retargeted root-motion sword attack plays through the existing GAS light-attack path.
  - The user verified in Scene01 PIE that the sword remains attached, the Montage drives Actor movement cleanly, a single attack spends Stamina and damages once, repeated activation is blocked, no-target recovery succeeds, and movement/look/jump do not regress. The retargeted Sequence has no hit Notify; the Montage has one `Light Attack Hit` on `DefaultGroup.DefaultSlot`.
  - No C++ gameplay contract changed. Socket, Blueprint, AnimBP, Montage, GA/GE, input, and retargeting authoring assets remain local WIP; the separate stable asset subset does not recreate the playable fixture from a clean checkout.
  - Main normal review and an adversarial fallback found no source-level blocker. The requested fresh independent Reviewer could not start because the configured service returned HTTP 503.
- [x] `TODO-01C: Dodge, Stamina, And Action Interruption v1`
  - Added `UStaminaActionAbility`, Stamina clamping/exhaustion ownership, a ground-only camera-relative `UDodgeAbility`, attack recovery cancellation, NotifyState-timed invulnerability, and unified ability cleanup for Montage tasks and temporary effects.
  - The user confirmed `PolyQuestEditor` compilation and Scene01 PIE validation for Root Motion Dodge direction, positive-Stamina overdraft to zero, exhaustion/recovery, repeated input blocking, movement/jump lock with camera control, recovery-only attack cancellation, invulnerability timing, teardown cleanup, and the repaired no-delayed-jump regression.
  - Main normal review found and repaired a latched Jump release during Dodge. Main adversarial fallback found no further source-level blocker. The requested fresh `gpt-5.6-luna / xhigh` Reviewer returned HTTP 503, so no independent-review result is claimed.
  - GA/GE, Montage, AnimBP, Blueprint, input, retargeting, map, and verification assets remain local mutable WIP and are not a clean-checkout fixture.
- [x] `TODO-01C1: Combat Input Intent Routing And Hold/Release v1`
  - Added one native `UCombatLoadoutDefinition` route table and unified `PrimaryAttack`, Aim, and direct Ability Slot input handling. A `Started` event publishes the physical `Input.*` intent before resolving the active Loadout to a GAS Ability Tag; release and cancellation publish distinct events and clear held state without launching another Ability.
  - The production local `DA_CombatLoadout_StraightSword` originally routed only `Input.PrimaryAttack -> Ability.Attack.Light`; `TODO-01E` now routes it to `Ability.Attack.Primary` for short/hold arbitration. Aim and unconfigured production slots remain intentional no-ops. Keyboard/mouse is the accepted validation scope; controller evidence is tracked under `Known Risks And Validation Debt`.
  - Removed the retired `IA_Attack_Light` route and config Tag after a scoped Editor/source/asset audit. `Event.Input.*` is documented as active-Ability event delivery rather than a generic `AbilityTrigger` source. Main review found no remaining source blocker; the requested fresh `gpt-5.6-luna / xhigh` Reviewer could not start because the provider returned HTTP 503.
  - Input, Loadout, Blueprint, GA/GE, Montage, AnimBP, and map assets remain local mutable WIP and are excluded from the focused source/config/document commit.

- [x] `TODO-01D: Data-Driven Combo, Branch Window, And Recovery Cancel v1`
  - Replaced the single-Montage light-attack configuration with `UComboChainDataAsset`: each entry is one unique complete Montage, while `ULightAttackAbility` owns the active entry, one buffered `Input.PrimaryAttack`, per-entry Cost/Hit consumption, and recovery Dodge cancellation.
  - Montage identity now guards all hit/window events and end callbacks. Source animation is carried through `FGameplayEventData::OptionalObject`, so a replaced Montage cannot damage, reopen a window, or end its successor.
  - The user compiled `PolyQuestEditor` and confirmed the authored Scene01 PIE combo route. Main normal review and a Main adversarial fallback found no confirmed P0-P2 source or semantic-event blocker; the requested fresh `gpt-5.6-luna / xhigh` Reviewer did not start because the provider returned HTTP 503, so no independent-review result is claimed.
  - Combo DataAsset, GA/GE, Montages, AnimBP, Blueprint, input, map, retargeting, and presentation assets remain local mutable WIP. The focused commit contains only native source, Gameplay Tag config, and documentation.

- [x] `TODO-01E: Charged Attack v1`
  - Added a no-cost `UPrimaryAttackAbility` that preserves Combo priority, then resolves a held `Input.PrimaryAttack`: a short normal release requests Light Attack, while a held release routes to Charged Attack. `Event.Attack.Charged.ReleaseHandoff` carries the original held duration when Released arrives before the `0.2 s` delay callback, so the Character's cleared held-input cache cannot lose the Charged release.
  - Added `UChargedAttackAbility` with HoldReady pause, release-time Stamina Cost, `1.0x` through `1.8x` SetByCaller damage, one identity-filtered hit, Root Motion resume from the same playhead, and unified task/delegate/tag cleanup. Primary, Light, Charged, and Dodge use shared movement/jump input-block tags; Dodge cancels Primary during arbitration and Charged while it is charging, then returns to the authored recovery CancelWindow after release.
  - The user confirmed `PolyQuestEditor` compilation and the authored Scene01 PIE route, including the repaired threshold timing behavior. Main normal review and Main adversarial review found no confirmed P0-P2 source blocker. The requested fresh `gpt-5.6-luna / xhigh` Reviewer did not start because the provider returned HTTP 503, so no independent-review result is claimed.
  - GA/GE, Montage, Sequence, Blueprint, AnimBP, Loadout, input, map, and imported assets remain local mutable WIP. The focused commit contains only native source, Gameplay Tag config, and documentation.

- [x] `TODO-01F: Sprint Foundation And Sprint Attack v1`
  - Added the `MoveSpeed` Attribute, one idempotent CharacterMovement synchronization delegate, a continuous ground Sprint, Stamina-costed Jump, Sprint Jump air-speed cleanup, and Loadout-owned Sprint Attack routing. Sprint is ASC-tag truth rather than an input-layer Boolean; a Sprint Attack commits its cost before ending Sprint, plays one Root Motion Montage, consumes one identity-filtered Sprint Hit event, and permits Dodge only during its authored recovery cancel window.
  - The user confirmed `PolyQuestEditor` compilation and Scene01 PIE coverage for Sprint start/stop/cost/exhaustion/re-entry, normal and Sprint Jump behavior, Sprint Attack hit/cost/damage, and the repaired Exhausted-tag recovery path. The current BlendSpace presentation is accepted temporarily; a dedicated Sprint Loop remains a separate `TODO-07B` presentation task.
  - Main normal review repaired repeated loose `State.Status.Exhausted` additions that could otherwise leave action abilities blocked after Stamina recovered. Main adversarial fallback found no additional confirmed source blocker. The requested fresh `gpt-5.6-luna / xhigh` Reviewer could not start because the provider returned HTTP 503, so no independent-review result is claimed.
  - GA/GE, Montage, Sequence, Blueprint, AnimBP, Loadout, input, map, and imported assets remain local mutable WIP. The focused commit contains only native source, Gameplay Tag config, and documentation.
- [x] `TODO-01G: Player Combat Foundation Health Review v1`
  - Audited `TODO-01A` through `TODO-01F` input, Loadout, Gameplay Tag, Stamina, interruption, Root Motion, Montage/Notify identity, and teardown contracts without changing the provisional actor-forward Sphere Sweep delivery. It established shared weapon motion traces and hit resolution as the mandatory prerequisite before the first enemy.
  - Repaired synchronous Montage-end reentry in Light and Charged startup. Dodge now filters completion and invulnerability events by its active Montage, retains persistent filtered event listeners, and clears its effect, delegate, tasks, and Montage through `EndAbility()`.
  - The user confirmed the requested Scene01 keyboard/mouse integrated PIE regression. The requested fresh `gpt-5.6-luna / xhigh` Reviewer could not start because the provider returned HTTP 503 with no available channel, so the second pass was a Main adversarial fallback; no independent review is claimed. Unreal Editor MCP readback was unavailable in this session, so the authored fixture remains local WIP rather than independently read-back or clean-checkout evidence.
  - The committed-cost playback atomicity, charged same-frame threshold ordering, controller hardware, task-level `ExternalCancel()`, and authored-fixture reproducibility debts remain tracked under `Known Risks And Validation Debt` with closure triggers.

- [x] `TODO-01H: Weapon Motion Trace And Hit Resolver Foundation v1`
  - Replaced the three provisional actor-forward sphere sweeps with one shared Trace Window delivery path. Light, Charged, and Sprint Attack retain their own state, Cost, cancellation, attack data, and active-Montage identity checks; animation sends only Trace Window Begin/End timing.
  - Each open window continuously sweeps the prior-to-current Blade Base/Tip path on `MeleeTrace`, applies an authored GameplayEffect through the shared GAS resolver, and records a target only after successful delivery so a target can receive at most one accepted hit per window. The resolver rejects self, the same minimal combat team, missing target ASC, and `State.Status.Invulnerable`.
  - At this stage the player used fixed `WeaponMesh` / `BladeTraceBase` / `BladeTraceTip` fixture names rather than equipment ownership. `TODO-03A` later replaced the player route with equipped-weapon trace markers while retaining the Trace Window task and shared resolver; the first enemy still uses the fixed fixture.
  - The user confirmed `PolyQuestEditor` compilation and Scene01 PIE validation. Main review plus an independent read-only source review found no P0-P2 C++/Gameplay Tag/config blocker. Blueprint, Montage, GameplayEffect, AnimBP, input, map, collision-fixture, and marker authoring remain deliberately excluded mutable WIP; this native shared-hit foundation is not a clean-checkout reproduction of the complete combat fixture.

- [x] `TODO-02A: First Enemy GAS Combat And StateTree Intent v1`
  - Replaced the provisional integer team identifier with exact `Team.Player` and `Team.Enemy` tags. Invalid or equal tags fail closed in the shared melee resolver. The Player registers as a Sight source; `AEnemyCharacter` inherits the existing ASC and trace fixture while `AEnemyAIController` owns Sight, target, focus, home, and StateTree lifecycle.
  - The first native StateTree contract selects Patrol, Alert, Chase, Combat, and Return intent. It requests one `UEnemyMeleeAbility` and observes its ASC-owned action tag; StateTree does not directly play Montages, apply damage, modify Health/Poise, or establish another enemy FSM. The enemy Ability identity-filters Trace Window events and reuses `UAbilityTask_MeleeTraceWindow` and `FMeleeHitResolver`.
  - The user confirmed the configured Scene01 PIE and visual route after the authored enemy fixture was completed. Main normal review and a Main adversarial fallback found no P0-P2 C++/Gameplay Tag/config blocker. A fresh `gpt-5.6-luna / xhigh` Reviewer was unavailable, so no independent-review result is claimed.
  - The focused commit intentionally excludes all mutable authored fixture WIP: Content assets, Blueprints, StateTree, GA/GE, Montage, AnimBP, marker/collision tuning, map, input, project settings, and imported resources. It is not a clean-checkout reproduction of the local combat fixture.

- [x] `TODO-02B: Oblique Perspective Camera And Combat Facing v1`
  - Replaced the free over-the-shoulder orbit with a fixed-world elevated oblique Perspective camera. Player movement resolves from the CameraBoom's world yaw, while native Look bindings are intentionally absent and authored Look assets remain mutable WIP.
  - Light, Charged, Sprint Attack, and Dodge each apply one planar action-facing yaw at startup. Existing ASC tag observation suppresses ordinary movement-facing while `State.Action.Attacking` or `State.Action.Dodging` is present, then restores it after both tags clear; Root Motion is not forcibly overwritten every frame.
  - The user confirmed the focused Scene01 PIE and visual route, including the `2000cm` position-lag configuration's empty-space Root Motion camera behavior. Main completed source/caller reads, UE 5.8 API/tag-order verification, CodeGraph, code-review-graph structural analysis, error-memory lookup, `git diff --check`, and a Main adversarial fallback with no confirmed P0-P2 native blocker. The requested fresh `gpt-5.6-luna / xhigh` Reviewer returned HTTP 503, so no independent-review result is claimed.
  - The focused native commit excludes all mutable authored WIP: `Content/**`, input mappings, Blueprints, maps, animation, Montage, material, imported resources, and generated directories. By explicit user approval it also includes the small project-policy/configuration set `AGENTS.md`, `Config/DefaultEditor.ini`, `Config/DefaultGame.ini`, and `PolyQuest.uproject`; it is still not a clean-checkout recreation of the local presentation fixture. `TODO-07B` remains the sole accepted owner for any later occlusion or presentation retune shown necessary by visual evidence.

- [x] `TODO-02C1: First Enemy Attack Profile And Reach Contract v1`
  - `UEnemyAttackProfile` is the static authored input for one Montage, one damage GameplayEffect, a positive attack range, and a non-negative post-attack cooldown. It is not a selector, queue, random table, or weapon-switching system.
  - The Controller validates the Profile before it starts StateTree logic, caches its only 2D actor-center reach as runtime `MeleeRange`, and owns cooldown expiration after an actually started attack cleans up. The enemy Ability snapshots Profile data and reuses the shared Trace Window/Resolver; the StateTree task observes the action tag and cooldown rather than creating a second combat state.
  - Live Editor MCP readback confirmed the Root/Alert/Chase/Combat/Return topology and Chase reach binding. The user confirmed `PolyQuestEditor` compilation and focused Scene01 PIE, including the reach boundary, cooldown, interruption, target loss/reacquisition, hit rejection, and Trace cleanup.
  - Main normal review and Main adversarial fallback found no P0-P2 C++/GAS/Tag/StateTree blocker. `gpt-5.6-luna / xhigh` was unavailable, so no independent review is claimed. All authored fixture assets remain excluded mutable WIP; this native commit is not a clean-checkout fixture.

- [x] `TODO-02C2: First Enemy Death And Teardown v1`
  - Established the first enemy-only terminal path: terminal Health is clamped to zero, `AEnemyCharacter` promotes zero Health or another legal Dead source to an exact loose `State.Status.Dead` tag, then one idempotent teardown stops the Controller, StateTree, movement, and active GAS work.
  - The shared melee resolver now rejects a dead source or target, so a same-frame residual Trace cannot damage after the terminal Tag is in place. Native C2 leaves the Actor/Capsule and current pose intact; it does not reference authored death-animation assets.
  - The user confirmed `PolyQuestEditor` compilation and Scene01 PIE: after Health reaches zero the enemy no longer acts, and later attacks leave Health at zero rather than below zero. Main normal review and Main adversarial fallback found no unresolved P0-P2 C++/GAS/Tag/StateTree blocker. `gpt-5.6-luna / xhigh` remained unavailable, so no independent review is claimed.
  - All authored Blueprints, AnimBPs, GameplayEffects, Montages, StateTree assets, maps, and imported resources remain excluded mutable WIP. `TODO-02C3A`, `TODO-02C3D`, `TODO-02C3B`, and `TODO-02C3C` are completed presentation/reaction/Poise slices; C3F owns the delivered grounded Root Motion Big contract, while Stance Break itself remains in place; `TODO-03D` owns player death, reload, and revival.

- [x] `TODO-02C3A: Enemy Death Presentation v1`
  - `ABP_Enemy_Goblin` now reads the native ASC-backed `AEnemyCharacter::IsDead()` query into a presentation-only `bIsDead` cache and enters a one-way `Dead` state. It does not mutate Health, Gameplay Tags, StateTree, Controller, movement, collision, or Ability state.
  - `To Land` is the authored alias for `Fall Loop` and `Jump`; `To Falling` is the authored alias for `Land` and `Locomotion`. Their `bIsDead == true` transitions cover those source states without duplicate direct death routes, and `Dead` has no exit transition.
  - The user confirmed the AnimBP compiled and the Scene01 PIE death presentation: the enemy reaches the terminal pose and does not revive or resume action. The compatible AnimBP, sequences, Blueprints, map, and imported assets remain excluded mutable WIP; C3A has no native source/config candidate.

- [x] `TODO-02C3D: Enemy Ragdoll Death Presentation v1`
  - After the existing C2 terminal teardown, `AEnemyCharacter` validates the Mesh Physics Asset, disables the inherited Capsule collision/overlaps, applies the engine `Ragdoll` profile, begins skeletal simulation, and wakes bodies. The ASC-owned Dead Tag remains the only gameplay survival state; disabled ragdoll or a missing Physics Asset retains the existing C3A terminal AnimBP fallback.
  - The fixed-v1 `WeaponMesh` display fixture is now visual-only at runtime: `ABaseCharacter` disables its collision and overlap generation at `BeginPlay()`, and the death path repeats that idempotent rule before Mesh physics starts. This removes the fixture from SpringArm and ragdoll physical interaction without changing the marker-driven Trace Window or shared resolver.
  - The user confirmed recompilation and the focused C3D test route passed. Main normal review and a separately performed Main adversarial fallback found no P0-P2 native/GAS blocker. `gpt-5.6-luna / xhigh` remained unavailable, so no independent review is claimed. All Physics Asset, Blueprint, AnimBP, map, animation, and imported Content remain excluded mutable WIP.

- [x] `TODO-02C3B: Enemy Hit Reaction And Safe Interrupt v1`
  - Established the first target-side hard-interrupt route: a living enemy accepts only a Health-reducing GameplayEffect whose Asset Tags include exact `Data.Reaction.Interrupt`, then sends `Event.Reaction.Enemy.Hit` to its own ASC. `UEnemyHitReactionAbility` owns the active reaction tag and Montage lifecycle; shared melee delivery remains unchanged.
  - Only an actually started reaction Montage locks movement and cancels `Ability.Attack.Enemy.Melee`, so the existing melee Ability still closes its Trace Window, clears `State.Action.Attacking`, and begins its established cooldown. Controller and StateTree Combat read `State.Action.HitReacting` and wait rather than requesting another attack; Dead teardown remains higher priority.
  - By explicit user acceptance, C3B closes on focused Scene01 reaction-animation playback. The accepted authored clip currently contains Root Motion, but C3B intentionally suppresses actor displacement through `DisableMovement()`; this is an in-place presentation fixture, not root-motion behavior evidence. `TODO-02C3C` delivered the same in-place Stance Break policy. C3F later delivered the separate grounded Root Motion Big contract without changing the Stance Break presentation rule.
  - Main normal review and Main adversarial fallback found no P0-P2 C++/GAS/Tag/StateTree blocker. `gpt-5.6-luna / xhigh` was unavailable, so no independent review is claimed. All GA/GE, Montage, Blueprint, AnimBP, StateTree, map, imported resources, and other `Content/**` remain excluded mutable WIP; this native commit is not a clean-checkout reaction fixture.

- [x] `TODO-01C2: Shared Dodge/Sprint Input Arbitration v1`
  - `APlayerCharacter` now temporally arbitrates one physical `DodgeSprintAction`: a keyboard Shift release before the authorable `0.15s` threshold requests the existing Dodge Ability, while reaching the threshold resolves the same held press to Sprint intent and never adds a late Dodge on release.
  - Input arbitration owns only press timing. The existing Sprint Ability remains the authority for MoveSpeed, Stamina, Gameplay Tags, and cleanup; action, movement-block, and airborne transitions cancel only an active Sprint, then existing movement/tag/landing paths may retry the resolved hold when conditions recover. Canceled input, physical release, teardown, and the existing post-exhaustion release gate retain their respective cleanup semantics.
  - The user confirmed the `0.15s` keyboard Scene01 PIE route. Main normal review and Main adversarial fallback found no P0-P2 C++/GAS/Enhanced Input lifecycle blocker; `gpt-5.6-luna / xhigh` was unavailable, so no independent review is claimed. `Content/**` input actions, mappings, Blueprints, and all other authored combat WIP remain excluded from the native commit.

- [x] `TODO-02C3C: Enemy Poise And Stance Break v1`
  - Added the enemy `Poise`/`MaxPoise` (100/100) attribute contract. Light `-15` and Sprint `-25` deplete through authored fixed GE Modifiers; Charged carries `Data.Damage.Charged` plus a linear `-25..-50` `Data.Poise.Charged` through one Damage GE Spec on the shared Trace Window/resolver multi-SetByCaller path; partial depletion restarts one `2.0s`-delayed `10/s` Instant-GE recovery.
  - A positive-to-zero Poise crossing defers one next-tick `Event.Reaction.Enemy.StanceBreak`; lethal damage promotes Dead first and clears the pending dispatch. `UEnemyStanceBreakAbility` owns `State.Status.Stunned`, locks movement, and cancels enemy melee plus C3B only after its Montage is confirmed active; its guarded `EndAbility()` restores walking and full Poise for a living enemy, while rejected or failed activations restore Poise instead of leaving an unrepeatable zero state.
- Health hit-reaction dispatch skips while Poise is zero, C3B does not restore walking while Stunned is owned, and Controller/StateTree reject or wait on Stunned. The user confirmed `PolyQuestEditor` compilation again after this stage's `OnPossess` Poise-configuration logging repair, and earlier confirmed a focused Scene01 PIE route; individual matrix subcases were not separately recorded. Main normal review plus Main adversarial fallback found no remaining P0/P1 defect; the sole P2 (`OnPossess` Poise-configuration gate logging) was repaired in the same stage. `gpt-5.6-luna / xhigh` remained unavailable, so no independent review is claimed.
- The delivered Stance Break is a full-body, functionally in-place presentation. Its current authored source may contain root translation, but `DisableMovement()` deliberately suppresses actor displacement; it is not a functional Root Motion stance-break implementation. C3F separately delivered the grounded Root Motion Big reaction with Montage-only displacement, collision/ledge safety, death teardown, and movement recovery; it does not alter the Stance Break rule. All GA/GE, Montage, Blueprint, AnimBP, StateTree, map, and imported Content remain excluded mutable WIP; this native commit is not a clean-checkout stance-break fixture.

- [x] `TODO-02D1: Directional Guard And Player Guard Break v1`
  - Established grounded front-arc Guard through the existing Combat Loadout route. `State.Action.Guarding` is the active GAS state; a confirmed Guard Montage applies the authored 70 percent movement and Stamina-regeneration multiplier effects, while the shared melee resolver consumes a valid front `120`-degree enemy hit as one Guard-Stamina contact instead of a Health-damage Spec.
  - A zero-Stamina successful Guard still absorbs its resolving Trace contact, then dispatches player Guard Break. The event-triggered Ability owns Stunned and only locks movement, cancels Guard/Sprint/attacks, and requires Guard release after its Montage actually starts; normal delayed Stamina recovery remains the only recovery path. Light, Charged, and Sprint Attack reuse their scoped cancel window for Defense and only resume a pre-held Guard after they really cancelled an active Guard.
  - The user confirmed `PolyQuestEditor` compilation and Scene01 PIE. Main's first normal review found no P0-P2 issue. A user-run GLM second review repaired only the Trace Request indentation noise and added a warning for an unaccepted Guard Break event while preserving its absorbed-hit behavior; final static evidence includes CodeGraph, Tag cross-check, C4458 scan, and `git diff --check`.
  - The focused native commit excludes all `Content/**` authoring WIP, including GA/GE, Montage, AnimBP, Blueprint, input assets, maps, and imported resources. It is not a clean-checkout recreation of the local Guard fixture. The Guard release presentation debt remains owned by `TODO-07B` with its existing focused closure gate.

- [x] `TODO-02D2: Timed Parry And Enemy Poise Counter v1`
  - Added Q-routed `UPlayerParryAbility`, an authored Notify-timed front `120`-degree Parry window, and Parry-first defense dispatch in the shared resolver. A valid contact is consumed without Health or Guard-Stamina damage, then applies authored `Data.Poise.Parry = -100` to the attacker through the existing C3C Poise/Stance Break route; no direct enemy Stun, second trace, or AI path was added.
  - Startup commits only the authored Parry Stamina Cost through `CommitStaminaCostOnly(...)`. The full Montage must complete naturally before the authored `0.5s` Granted-Tag `Cooldown.Parry` effect is committed; startup failure, interruption, death, and teardown do not grant cooldown. The Parry-owned `MOVE_None` lock preserves a pre-held Guard recovery qualification, while external Falling clears it.
  - The user confirmed post-repair `PolyQuestEditor` compilation and Scene01 PIE. Main's repair delta review found no P0-P2 issue after the configured Ability validation and external-Falling/Guard-recovery repairs. This record does not claim a fresh independent Reviewer result.
  - The focused native/config/documentation commit excludes all `Content/**` authoring WIP, including GA/GE, Montage, AnimBP, Blueprint, input assets, maps, and imported resources. It is not a clean-checkout recreation of the local Parry fixture.

- [x] `TODO-02D3: Enemy Notify-Timed Hyper Armor v1`
  - Added one timing-only `UAnimNotifyState_EnemyHyperArmor` and the three tags `Event.Attack.HyperArmor.Begin`, `Event.Attack.HyperArmor.End`, and `State.Status.HyperArmor`. The active `UEnemyMeleeAbility` alone owns the exact loose-tag count during a matching authored attack-Montage window and clears it through every natural, interrupted, Stance Break, death, and teardown path.
  - Hyper Armor blocks only C3B `UEnemyHitReactionAbility`; it leaves Health, Poise, the shared Trace/Resolver delivery path, C3C Stance Break, Dead teardown, Controller cooldown, and StateTree topology unchanged. v1 permits one continuous non-overlapping window per attack Montage, authored to cover the existing Trace Window.
  - The user confirmed `PolyQuestEditor` compilation and Scene01 PIE. Main normal review and Main adversarial fallback found no P0-P2 C++/GAS lifecycle issue; `gpt-5.6-luna / xhigh` was unavailable, so no independent review is claimed.
  - The focused native/config/documentation commit excludes all `Content/**` authoring WIP, including the Montage NotifyState placement, GA/GE, Blueprint, AnimBP, StateTree, maps, and imported resources. It is not a clean-checkout Hyper Armor fixture.

- [x] `TODO-02D4: Combat Animation Notify Ownership And Naming Audit v1`
  - Audited the native combat timing Notify group and made reflected ownership explicit: Shared `ActionDodgeCancelWindow`, `DodgeInvulnerability`, and `AttackTraceWindow`; Player `PlayerChargedAttackHoldReady`, `PlayerComboInputWindow`, `PlayerComboBranchWindow`, and `PlayerParryWindow`; Enemy `EnemyHyperArmor`.
  - Renamed only the four Player reflected classes and their Editor display names. Existing Gameplay Event Tags, `SendGameplayEvent` payload identity, Ability listeners, damage, AI, and Montage timing remain unchanged. No `CoreRedirects` or legacy aliases exist; old serialized Player Notify references are intentionally migrated by the user in Editor.
  - The user confirmed the post-migration test route, including the focused Charged HoldReady smoke. This closes the `TODO-01H` Notify-relocation validation debt. Main normal review and Main adversarial fallback found no P0-P2 issue; `gpt-5.6-luna / xhigh` remained unavailable, so no independent review is claimed.
  - The focused source/documentation commit excludes all `Content/**` migration WIP, Montage, Blueprint, AnimBP, map, input, `.uproject`, and unrelated user changes. It is not a clean-checkout authored-asset fixture.

- [x] `TODO-02E: Bidirectional Melee Combat Health And Lean Review v1`
  - Audited the completed bidirectional melee loop across eight surfaces (damage chains both ways, defense dispatch, team/invulnerability/dead rejection, teardown matrices, StateTree-to-GAS boundaries, camera/facing, HyperArmor lifecycle, and the validation-debt register) using the prior stage reviews plus three read-only explorations; no P0-P2 blocker was found and no new non-blocking finding needed reassignment.
  - Lean pass results: the retired legacy `DodgeAction`/`SprintAction` input properties and the unreferenced `Input.Dodge` tag were removed after Source and Content package-string scans proved zero referencers; `ProjectName` now reads `PolyQuest`; zero redirects, orphan source files, orphan classes, and plugin residue were verified and recorded. The Look input surface stays retained per the 02B future-ownership contract.
  - Documentation synchronization advanced the README through C3C/D1/D2/D3/D4 with `TODO-02F` as the next stage and repaired the Light Attack Parrying/Defense-cancel/tag-inventory drift plus the D4 Player Parry Notify class name.
  - The user confirmed the post-lean `PolyQuestEditor` compile, the `BP_Player` re-save without missing properties, and the Scene01 PIE bidirectional regression smoke. A fresh review found and repaired only the next-stage and stale-Notify documentation drift; no source-level health defect was found. All `Content/**` and unrelated user WIP remain excluded from the focused commit.

- [x] `TODO-02F: Montage Rate Window And Action Timing v1`
  - Added `UAnimNotifyState_MontageRateWindow` plus `Event.Action.RateWindow.Begin/End`. An authored positive `RateMultiplier` travels in the Begin event magnitude, while the source Montage remains the identity payload; no Notify directly changes action state, damage, or Attributes.
  - Light, Charged, and Sprint Attack each own persistent matching event listeners and a guarded baseline restore. A matching End, every `EndAbility()` path before the Montage stop, and Light's Combo-entry handoff restore `1.0`, preventing a local rate override from leaking into an interruption or successor Montage.
  - Rate windows are authoring-only, non-overlapping intervals. They have no supported stacking or priority behavior; downstream animation-timed Trace, Combo, cancel, and HoldReady semantics follow the rated Montage timeline. Dodge, defense, and enemy abilities deliberately remain outside v1.
  - The user confirmed the authored `PolyQuestEditor` compile and Scene01 PIE validation. Main review found no P0-P2 source lifecycle blocker after the plan correction; `Content/**` Notify placements, Montages, Blueprints, AnimBPs, maps, and all unrelated user WIP remain excluded from the focused native/config/documentation commit.

- [x] `TODO-03A: Equipment And Combat Loadout Foundation v1`
  - Added `UMeleeWeaponDefinition` for authored single-melee-slot display, local trace markers, sweep shape, weapon Ability grants, and optional Combat Loadout data. `UWeaponEquipmentComponent` is player-only in v1 and owns all runtime display, marker, and granted-spec state.
  - `EquipWeapon` is a fail-closed transaction: it rejects combat actions and Primary arbitration, validates socket/ASC/definition/duplicate-spec prerequisites before mutation, then swaps only component-owned state with rollback after a post-preflight failure. It neither cancels active combat abilities nor exposes a public unequip or sustainable bare-handed state.
  - The player trace source now consumes equipped marker positions and weapon-owned sweep shape; the original fixed-name lookup and fixed-name collision guard remain only for the first enemy fixture. The shared Trace Window task, resolver, tags, and damage path are unchanged.
  - The user confirmed the post-repair `PolyQuestEditor` compile and Scene01 PIE equipment route. The focused native/documentation commit excludes DataAssets, Blueprints, temporary swap-debug bindings, maps, all other `Content/**`, project settings, and unrelated WIP; it is not a clean-checkout authoring fixture.

- [x] `TODO-07B1: Combat Hit Feedback v1`
  - Completed the narrow damage-path-owned feedback slice. A real nonlethal Health GameplayEffect decrease asks `ABaseCharacter` to own one temporary global Mesh Overlay; it caches/restores the prior Overlay, refreshes one Timer on repeat hits, and leaves a newer external Overlay untouched. Guard/Parry remain excluded because their successful resolver paths never apply Health damage.
  - Player-only feedback starts the authored local single-instance `UCameraShakeBase` through `PlayerCameraManager`; it does not move the Pawn, CameraBoom, operating-system window, or Enemy camera. Healing, direct Attribute writes, Poise-only effects, lethal/dead state, and teardown do not flash. No Hit Stop, audio, Niagara, Gameplay Tag, replication, or second damage route was added.
  - The user confirmed focused PIE and the new `PolyQuest.Combat.HitFeedback` suite plus all eleven existing Automation regressions. Remaining logs are only the established negative assertions for invalid reaction tags, Stance Break fallback, equipment rejection/rollback, and invalid static trace geometry. Main's single defect-first fresh review found no P0-P2. Material/Camera Shake assets remain local `Content/**` WIP and are excluded from the focused source/test/document commit.

- [x] `TODO-07B1A: Reaction-Tier Player Hit Camera Shake v1`
  - Completed the Player-only three-tier extension of B1. The existing fail-closed `Data.Reaction.Small`, `Data.Reaction.Big`, and `Data.Reaction.Launch` classification now selects exactly the matching authored Camera Shake class. Legal no-tag and invalid multi-tier damage keep B1's red Overlay but neither start, stop, nor replace a Shake; Stunned valid-tier damage still presents the matching Shake while retaining reaction-event suppression.
  - The Player tracks only its exact started instance, originating `APlayerCameraManager`, and class via weak references. Same-tier hits rely on `bSingleInstance`; a tier change stops only the stored old instance through its originating Manager. `UnPossessed()` and `EndPlay()` clear the state idempotently. The reviewed Manager-expiry fallback deliberately does not invoke `StopShake()` or `TeardownShake()` directly, so `CameraModifier` remains the sole owner of active-list removal and pooling.
  - The shared native fixture injects three transient test Shake classes before `FinishSpawning()`. `PolyQuest.Combat.HitFeedback` covers strict selection, same-tier reuse, cross-tier replacement, no-tag/Invalid preservation, Stunned presentation, Enemy non-participation, and teardown. The user confirmed focused PIE and all twelve Automation suites. Main's first fresh review found and repaired one Manager-expiry lifecycle defect; the post-repair delta fresh review found no P0-P2. Authored Camera Shake assets and their Blueprint assignments remain local `Content/**` WIP and are excluded from the focused source/test/document commit.

- [x] `TODO-03B-4: Player Mobile Bow Draw v1`
  - Completed mobile Player Bow Draw/Hold/Release/Recovery without changing Bow targeting, facing, projectile snapshot, Dodge-window, or Jump-block contracts. `UBowDrawFireAbility` no longer owns movement-input blocking; after its authored Montage is confirmed active, it applies one exact handle from the required authored MoveSpeed GameplayEffect and then cancels only an active Sprint. The handle persists for the whole active Bow lifecycle and every existing `EndAbility()` route removes only that handle.
  - `PolyQuest.Player.MobileBow` covers the real Bow Ability's Tag contract, controlled native MoveSpeed fixture, CharacterMovement synchronization, Sprint cancellation, idempotent handle ownership, normal/cancelled `EndAbility()` cleanup, and external MoveSpeed isolation. The fixture's numeric multiplier is an isolated test control, not a statement of mutable production balance. The user confirmed focused PIE and the fourteen-suite Editor Automation matrix; Main's defect-first fresh review found no P0-P2.
  - The authored Bow GE, Montage, AnimBP, Blueprint, input, and maps remain local `Content/**` WIP. The configured Bow pace is asset-owned tuning and must be read back from the Editor/PIE after a retune; it is deliberately not repeated as a durable Roadmap number.

## Milestones

### Player Loadout, Rewards, And Encounters

- [x] `TODO-03A1: Equipment Domain, Weapon Definition, And Hand-Slot Contract v1`
  - Delivered the initial equipment-domain contract: the abstract `UWeaponDefinition` base (hand slot, display, action candidates, default prepared layout, Defense Profile, `AssociatedLoadout` retained in place as the MainHand Base Input Profile), the reference-list `UCombatActionDefinition`, the two-tag `UDefenseProfileDefinition`, and `UMeleeWeaponDefinition` re-derived as the compatible melee subtype whose `GrantedWeaponAbilities` was the coexisting BaseGrantedAbilities source for the LMB/Sprint base chain. TODO-03A2 later retired that reference-list/legacy-grant form in favor of direct ability classes.
  - `UWeaponEquipmentComponent` owns both hand slots, the keep-if-compatible prepared `1-4` layout with exact-handle activation (triple-validated), and the single input resolution: Guard/Parry through the Effective Defense Profile chain (OffHand override, MainHand fallback, built-in defaults), Primary through the MainHand Base Input Profile, and Sprint Attack through the equipment-applied active profile. The composition transaction rejects TwoHanded/OffHand conflicts in preflight with zero handle changes and restores a failed apply by re-granting the old composition and prepared identities on fresh handles.
  - The user confirmed the `PolyQuestEditor` compile, the data-retention readback across `DA_MeleeWeapon_Sword`, `DA_MeleeWeapon_Axe`, and `BP_Player`, and the Scene01 PIE route after the all-null-prepared-layout preflight repair: byte-equivalent Sword combat, `1-4` empty-slot no-ops, and swap refusal intact. Main static review found no further P0-P2 defect; the enemy fixture, Trace Window, resolver, and damage path are untouched. That closeout handed Unarmed hand contact and the class-based grant migration to TODO-03A2, which has now completed them; combined drop-swap, enemy Attack Sets, and rest-site persistence remain with TODO-03A3/03A5/03D1.

- [x] `TODO-03A2: Player Main-Hand And Unarmed Combat v1`
  - Retired `UCombatActionDefinition` and the legacy `GrantedWeaponAbilities` source after the class-based cutover. `UWeaponDefinition` now owns direct `BaseGrantedActions`, Reusable/Exclusive candidate lists, and `DefaultPreparedActions`; duplicate or incompatible authored classes fail definition/equipment preflight rather than producing parallel specs. `DA_MeleeWeapon_Axe` and `DA_CombatLoadout_Axe` remain deferred authoring WIP, outside this fixture and to be authored directly against this class contract later.
  - Delivered `DA_Weapon_Unarmed` as a legal `MainHandOneHanded` definition: its null display mesh is paired with `bUseOwnerMeshSocketForTrace`, so the current component attaches the ordinary base/tip marker pair to the configured character SkeletalMesh socket. It shares the existing Trace Window and resolver with equipped melee weapons; no second damage or trace route exists.
  - Added the first prepared-slot combat skill, `UPlayerMeleeSkillAbility` / `GA_Skill_Whirlwind`. A slot activates its exact current spec handle; the native `Ability.Skill.Melee` category is restored on Blueprint Ability CDO load/compile for Dodge/Guard/Parry/GuardBreak cancellation, while `Ability.Skill.Whirlwind`, Montage, Cost, Cooldown, Damage, and timing remain authored on the GA/GE assets. The user confirmed final Editor readback and Scene01 PIE, including Whirlwind playback from `1`.
  - No world pickup/drop-swap, Shield override, enemy preset, Bow, inventory, persistence, action-selection UI, or per-hand unarmed trace selection was added. Authored `Content/**` fixtures remain local WIP and are excluded from the focused native/config/documentation commit.

- [x] `TODO-03A3: World Equipment Pickup And Drop-Swap v1`
  - Added `AWorldWeaponPickup` and the atomic `TryEquipWorldPickup` transaction to `UWeaponEquipmentComponent`. Pressing `E` (bound to native `InteractAction`) queries the nearest `QueryOnly` pickup and performs a fail-closed transaction: normalizes target composition via `BuildTargetCompositionForIncoming` (TwoHanded clears OffHand; Shield while holding TwoHanded clears main to `UnarmedFallbackDefinition`), rejects same-definition picks as no-ops, snapshots old state, tears down and applies the new composition, stages displaced dropped pickups with interaction collision temporarily disabled (`NoCollision`), and only upon complete success activates interaction collision (`QueryOnly`) on all provisional drops, calls `SourcePickup->OnPickupConsumed()`, and destroys the source.
  - Added `UOffHandWeaponDefinition` as the concrete OffHand equipment subtype validating `HandSlot == OffHand`. Fail-closed ground projection traces downward `300cm` using `ECC_Visibility` with `2cm` normal offset. Dropped pickups reject their `FormerOwner` for `0.5s` of world time via `TWeakObjectPtr<APlayerCharacter>`. `State.Status.Dead` hard gate is enforced across `CanSwapNow`, `CanInteract`, and `HandleInteractStarted`.
  - Delivered comprehensive native automation test suite `PolyQuest.Equipment.TransactionMatrix` (11 test sections) verifying: real fixtures (`SK_Character_Hero_Knight_Male`, `SM_Wep_Straightsword_01`, `SM_Wep_Shield_Round_01`), floor collision & ground projection, bidirectional TwoHanded/OffHand conflict preflight rejection with zero changes, successful TwoHanded and Shield world pickup transitions with exact dropped pickup counts (2 drops / 1 drop) and definitions, zero-leak drop cleanup on injected drop failure, and full ASC AbilitySpec / handle ownership restoration on both Apply failure and Drop failure rollbacks verified via `VerifyPreparedSlotBinding`.
  - The user confirmed `PolyQuest.Equipment.TransactionMatrix` passed with `Success`, then confirmed the focused Scene01 manual route: `E` equips the selected fixture and the displaced weapon drops visibly near the player's previous ground position without becoming an implicit inventory item. Main normal review and independent Fresh Reviewer subagent (`e5aa2404-efe1-4ff0-958c-1525870affb2`) completed with "No findings." All authored Blueprints, DataAssets, input mappings, maps, and imported content remain local authoring WIP.

- [x] `TODO-03A3B: Melee Weapon Sweep Socket Authoring And Calibration v1`
  - Replaced blind raw FVector marker tuning for displayed StaticMesh melee weapons with an explicit Static Mesh socket authoring path: `UMeleeWeaponDefinition` added `BladeBaseSocketName` and `BladeTipSocketName` (default `NAME_None`) and `UsesDisplayMeshTraceSockets()`. `IsValidWeaponDefinition()` enforces pure fail-closed all-or-nothing pair validation against `WeaponMesh->FindSocket` (both `NAME_None` for legacy vector mode, or both non-`None`, distinct, resolvable on the Static Mesh, and non-coincident in local `RelativeLocation`).
  - `UWeaponEquipmentComponent::ApplyComposition` attaches transient Blade Base/Tip `USceneComponent` markers directly below `NewMainHandDisplay` at the named sockets using `FAttachmentTransformRules::SnapToTargetNotIncludingScale` with zero secondary offsets. Unarmed remains owner-SkeletalMesh relative marker contact; TraceSource, AbilityTask, HitResolver, GAS grants, and transaction rollbacks are untouched.
  - Extended `PolyQuest.Equipment.TransactionMatrix` automation test suite: loaded real fixture `SM_Wep_Ornate_Sword_02`, verified `Trace_Base` / `Trace_Tip` socket existence, configured socket-mode `SwordDef` and `TwoHandedDef`, asserted that `TryGetBladeMarkers` returns markers attached to the authored sockets with distinct world coordinates, and verified preflight rejection for incomplete, missing, and distinct-name but coincident-location socket pairs. The latter also proves that direct equipment preserves the prior TwoHanded composition.
  - The user compiled `PolyQuestEditor`, authored `Trace_Base` and `Trace_Tip` on `SM_Wep_Ornate_Sword_02`, and configured `DA_Weapon_Sword`. Gemini completed the requested strict review. Codex's initial fresh review found the coincident-location Socket P2; the repair added preflight rejection plus the direct-equipment preservation assertion. The user reran `PolyQuest.Equipment.TransactionMatrix` with `Success`, and Codex delta review found no P0/P1/P2. No fresh compile or PIE result is claimed after that repair.
  - Accepted migration debt: legacy displayed-weapon vector mode remains available for non-adopted definitions whose socket names are unset; complete removal gate is `TODO-06C`.

- [x] `TODO-03A4: Off-Hand Shield And Composite Defense Loadout v1`
  - Added `Ability.Defense.Guard.Shield` and `Ability.Defense.Parry.Shield` plus the first Shield use of the existing tag-only `UDefenseProfileDefinition`. Effective defense resolution remains OffHand override -> MainHand fallback -> generic default. A non-null profile fails closed unless its provider's own `BaseGrantedActions` supplies exactly one distinct Guard and Parry class, each carrying the exact profile tag plus a hierarchical match in the corresponding generic category. This matches Unreal Editor serialization, where a specialized child may be the only stored tag.
  - Shield's valid Guard/Parry specs are component-owned; existing native ability lifecycles, cancellation categories, Guard Break, held-input recovery, and cleanup remain unchanged. Sword Primary/Charged/Sprint ownership remains in the MainHand. Shield has no trace, damage, collision-hit, or persistent quick-slot behavior in this slice; Shield `1-4` preparation and rest-site persistence remain owned by `TODO-03D1`.
  - Extended `PolyQuest.Equipment.TransactionMatrix` with valid/malformed profile cases, exact Shield tag resolution, component-owned Guard/Parry binding checks, stale-spec removal after Sword + Shield -> TwoHanded, and correct re-grant after TwoHanded -> Shield normalizes to `UnarmedFallbackDefinition + Shield`. The user reran the named Automation test after the P3 assertion repair and reported `Success`; expected warnings exercise intentional rejection and rollback branches. No separate final compile, Editor readback, or full Scene01 regression claim is made here.
  - All Shield DataAssets, GA Blueprints, Montages, NotifyState placements, pickups, AnimBP work, maps, and input assets remain local `Content/**` authoring WIP and are excluded from the focused native/documentation commit.

- [x] `TODO-03A5: Ordinary Enemy Weapon Presets And Weighted Attack Sets v1`
  - Replaced the single direct `UEnemyAttackProfile` on `AEnemyCharacter` with `UEnemyAttackSet`, an immutable authored DataAsset holding `EngagementRange` and a collection of `FEnemyAttackSetEntry` (Profile + positive `SelectionWeight`).
  - Added pure fail-closed validation `IsAttackSetValid` (rejecting empty entries, non-positive/non-finite engagement range, null/invalid profiles including non-finite Profile scalars, non-positive/non-finite weights, duplicate profile references, aggregate weight overflow, and engagement ranges exceeding all entries' attack ranges) and pure deterministic distance-filtered weighted selection `SelectAttackProfile`.
  - `AEnemyAIController` validates the Set and caches `EngagementRange` into `MeleeRange` on possession, providing `TryGetCurrentTargetDistance2D` as the single authoritative distance query. `UEnemyMeleeAbility` dynamically selects and snapshots exactly one Profile during `ActivateAbility()` before committing, and resets snapshots and starts cooldown upon `EndAbility()`.
  - Delivered comprehensive automation test suite `PolyQuest.Enemy.AttackSetSelection` covering all validity error branches (including +INF/NaN and total weight overflow) and deterministic boundary weighted selections; reran `PolyQuest.Equipment.TransactionMatrix` with 100% success.
  - The user confirmed Editor authoring of `DA_EnemyAttackProfile_GoblinAxe_02` (with Notify-timed `AttackTraceWindow`) and `DA_EnemyAttackSet_GoblinAxe`, followed by Scene01 PIE validation where the Goblin randomly executes both axe attack montages at melee range. Independent Fresh Review repaired 2 P2s (stale single-profile documentation in ARCHITECTURE.md and missing FMath::IsFinite checks across Profile, AttackSet, and Controller cooldown) and 1 P3 (extended non-finite automation tests), with 0 remaining defects.
  - All authored DataAssets, montages, Blueprints, and maps remain local authoring WIP and are excluded from the focused native/documentation commit.

- [x] `TODO-03A5B: Static Enemy Weapon Geometry Binding And Display Collision v1`
  - Extended `UMeleeWeaponDefinition` with pure geometry validator `IsValidStaticMeshTraceGeometry` (rejecting missing mesh, unarmed socket mode, missing/duplicate/unfound sockets, non-finite/coincident socket locations, non-positive/non-finite radius, and out-of-range subdivisions).
  - Extended `UMeleeTraceSourceComponent` with optional `StaticMeleeWeaponDefinition` and strict three-tier resolution: (1) player dynamic equipment markers, (2) configured static geometry definition with fail-closed rejection on invalid configuration (never silently falling back), (3) legacy `BladeTraceBase` / `BladeTraceTip` fallback only when `StaticMeleeWeaponDefinition == nullptr`.
  - Updated `ABaseCharacter` to set `ECC_Camera = ECR_Ignore` on `CapsuleComponent` and `SkeletalMeshComponent` in constructor and `BeginPlay()`, and updated `DisableFixedWeaponDisplayCollision` to enforce `NoCollision`, ignore all collision channels including explicit `ECC_Camera` ignore, and disable overlap generation, ensuring top-down SpringArm maintains a stable view distance without character or weapon obstruction.
  - Refactored automation test suite `PolyQuest.Melee.TraceSourceGeometry` using transient `UStaticMesh`/`UStaticMeshSocket` fixtures and `FinishSpawning` lifecycle (avoiding uncommitted Content asset dependency and UB downcasting) to verify pure geometry validation, static socket endpoint resolution, fail-closed branches, null fallback preservation, and display/body camera collision policy; reran `PolyQuest.Enemy.AttackSetSelection` and `PolyQuest.Equipment.TransactionMatrix`.
  - The user authored `Trace_Base` and `Trace_Tip` sockets on `SM_Wep_Axe_01`, configured `DA_Weapon_Axe`, bound `StaticMeleeWeaponDefinition = DA_Weapon_Axe` on `BP_Enemy_Goblin`, and confirmed in Scene01 PIE that axe hits register damage correctly while the camera maintains a stable view distance.
  - Codex Main final fresh/adversarial delta review completed after the transient-fixture and camera-policy repairs with no P0/P1/P2 defect found. The five display/socket branches and explicit `NaN`/`+Inf` `TraceRadius` assertions pass with transient fixtures; the invalid-static-definition and invalid-Poise-recovery messages are expected negative/test-fixture warnings, with the latter non-blocking. The user confirmed the targeted Automation runs, Editor authoring, and Scene01 PIE route. All authored DataAssets, StaticMesh sockets, Blueprints, and maps remain local authoring WIP and are excluded from the native/documentation commit.

- [ ] `TODO-03A4B: Weapon Defense Outcome Profiles v1`
  - Before the first weapon is authored with a successful Guard that intentionally takes partial damage, replace the current binary incoming-melee result with one explicit defense outcome: no defense, full absorb, or a bounded partial-damage result. Keep Parry as a distinct full-contact/counter outcome; do not turn this into a generic armor, resistance, projectile, or elemental system.
  - `UDefenseProfileDefinition` continues to select the effective Guard/Parry Ability through OffHand override -> MainHand fallback -> default. The active Guard Ability owns its authored outcome, Montage, front-arc gate, Stamina consequence, and Guard Break behavior. `FMeleeHitResolver` must apply any remaining damage exactly once through the existing damage GameplayEffect path rather than treating a successful partial Guard as a terminal hit.
  - The current successful `UPlayerGuardAbility` is already a full absorb: it consumes the Resolver contact before the damage GameplayEffect is applied. A Shield may therefore reuse that behavior for its first perfect-block form. GameplayEffects remain appropriate for Stamina cost, movement, cooldown, and the eventual remaining-damage magnitude, but a GE-only asset swap cannot create partial blocking while the Resolver exits early.
  - This is required only before a real Bow, Staff, or other weapon is intentionally authored with non-full Guard behavior. Until then, its Defense Profile may deliberately reuse the current full-absorb Guard/Parry lifecycle; do not pre-implement per-weapon mitigation fields or a generic damage-modifier framework.

- [x] `TODO-03AI: Enemy Combat AI Architecture And Behavior Boundaries v1`
  - Completed the planning and boundary gate after `TODO-03A5B`: StateTree remains high-level intent, the Controller owns target/navigation/cooldown, GAS owns action execution and cleanup, and authored AI profiles contain no runtime state.
  - Locked the ordinary melee `Combat -> Attack/Reposition/Wait` contract, finite Leash/target-loss behavior, ranged prerequisites, elite reuse, Boss separation, and the `TODO-04A` encounter-coordination boundary. This stage added no runtime AI behavior or generic framework.
  - Gemini's read-only plan review returned `READY` with no P0-P3 findings. Runtime implementation and its compile/Editor/Automation/PIE gates belong to the separately bounded slice below.

- [x] `TODO-03AI1: Ordinary Melee Cooldown Spacing And Reposition v1`
  - Delivered the ordinary-melee spacing slices defined by `TODO-03AI`: `TODO-03AI1A` adds immutable `UEnemyAIProfile` tuning, target-centered point generation clamped to `EngagementRange`, Controller-owned Leash/MoveTo/request-interval state, alternating side selection, one same-side failed-request retry, and fail-closed lifecycle cleanup; `TODO-03AI1B` adds the native `Enemy Reposition During Cooldown` StateTree task and the `Attack -> Reposition -> Wait` integration.
  - Reposition requests may continue for the entire active attack cooldown, with at most one active `MoveTo` and no hard two-request cap. A successful request alternates side; a failed request retries its current side once and then forces the next side. Cooldown expiry, target loss, Leash violation, death, stun, hit reaction, teardown, invalid data, and navigation failure stop or fail closed without a second state machine or damage path.
  - The authored `ST_Enemy_Goblin_Melee` keeps `Wait` at `0.5s`. Its `Wait -> Reposition` transition is gated only by `Enemy Is Attack On Cooldown`; the native `Enemy Can Request Reposition` condition remains available but is intentionally not wired there because the task/Controller owns temporary minimum-interval gating and must be allowed to retry during the same cooldown.
  - The user confirmed `PolyQuest.Enemy.AttackSetSelection`, `PolyQuest.Enemy.CombatSpacing`, `PolyQuest.Equipment.TransactionMatrix`, and `PolyQuest.Melee.TraceSourceGeometry` all completed successfully after the StateTree asset adjustment. Logged rejection/rollback/profile/socket messages are expected negative branches from the regression matrix and fixtures. Gemini's strict review and Main's separate fresh/adversarial review found no remaining P0-P2 source defect.
  - Authored StateTree, AI Profile, Blueprint, Montage, DataAsset, map, and other `Content/**` changes remain user-owned WIP and are excluded from the focused native/documentation commit. `TODO-03AI2` remains the separate distance-aware weighted approach/attack stage; `TODO-03H1` remains the later health/lean gate.

- [x] `TODO-03AI2: Distance-Aware Weighted Melee Approach And Attack Execution v1`
  - Delivered distance-aware weighted melee selection and dynamic approach for ordinary enemies: `UEnemyAttackSet::SelectAttackProfile` performs pure weighted selection across all valid attacks within `EngagementRange` without pre-filtering individual `AttackRange`; `AEnemyAIController` retains the selected profile snapshot and governs dynamic actor-following Approach until reaching `AttackRange` or timing out (`ApproachTimeout`, default `3.0s`); and `UEnemyMeleeAbility` consumes the Controller's prepared profile after range verification.
  - Added native StateTree tasks `Enemy Prepare Melee Attack` (`FEnemyStateTreeTask_PrepareMeleeAttack`) and `Enemy Approach Selected Melee Attack` (`FEnemyStateTreeTask_ApproachSelectedMeleeAttack`), plus conditions `Enemy Has Pending Melee Attack`, `Enemy Pending Melee Attack In Range`, and `Enemy Pending Melee Attack Out Of Range`.
  - The authored `ST_Enemy_Goblin_Melee` formalizes the `Decision -> Approach -> Attack -> Reposition -> Wait` topology. `Decision` selects a weighted profile and branches to `Attack` (if in range) or `Approach` (if out of range); `Approach` transitions to `Attack` upon reaching attack range, or to `Wait` on timeout/failure; `Wait` (0.5s) transitions back to `Decision` upon attack cooldown expiry when in range.
  - Pure short-range attack sets (all `AttackRange < EngagementRange`) are valid and rely on Approach; Controller and Ability enforce unconditional decision cleanup on any failure, interruption, or abort path, with authoritative `EngagementRange` validation. Decision preparation is blocked while an attack cooldown is active; both Controller and StateTree clear a stale pending profile during cooldown and fail closed.
  - The user confirmed `PolyQuest.Enemy.AttackSetSelection`, `PolyQuest.Enemy.CombatSpacing`, `PolyQuest.Equipment.TransactionMatrix`, and `PolyQuest.Melee.TraceSourceGeometry` automation test Success across all suites, including CombatSpacing 4.10 for cooldown-time Controller/StateTree Prepare rejection and cleanup. Gemini completed the strict executor self-review; Main completed normal review and adversarial fallback after the cooldown-gate repair with no remaining P0-P2 source defect. Luna/gpt-5.6-luna was not started because its service returned HTTP 503, so no independent Reviewer result is claimed.
  - Authored StateTree, AI Profile, Blueprint, Montage, DataAsset, map, and other `Content/**` changes remain user-owned WIP and are excluded from the focused native/documentation commit. `TODO-03H1` remains the next cross-stage health/lean review gate.

- [x] `TODO-03H1: Weapon And Combat Architecture Health And Lean Review v1`
  - Completed cross-stage architecture health and lean audit across weapon equipment, melee sweep trace geometry, static enemy display bindings, GAS abilities, melee hit resolution, and AI intent boundaries before the player Bow stage (`TODO-03B`).
  - Verified single-source-of-truth invariants: `UWeaponEquipmentComponent` is player-exclusive without generic component abstraction; `AWorldWeaponPickup` is a map interaction container without combat logic; enemy static geometry binding (`StaticMeleeWeaponDefinition`) enforces strict fail-closed validation; `UAbilityTask_MeleeTraceWindow -> FMeleeHitResolver -> Damage GE` is the sole melee damage path; and `UEnemyAttackSet` / `UEnemyAIProfile` data assets remain immutable.
  - Lean review confirmed 0 dead compatibility symbols and 0 required C++ modifications. Legacy `BladeTraceBase / BladeTraceTip` fallback is intentionally retained as documented validation debt.
  - The user confirmed `PolyQuest.Equipment.TransactionMatrix`, `PolyQuest.Melee.TraceSourceGeometry`, `PolyQuest.Enemy.AttackSetSelection`, and `PolyQuest.Enemy.CombatSpacing` automation test suites all passed with `Success`. Gemini completed strict executor self-review; Main completed normal review and adversarial review with zero P0-P2 defects. No independent Reviewer (Luna) result is claimed. All authored Content fixtures remain local authoring WIP.

- [x] `TODO-03B-1: Player Straight Bow And Projectile v1`
  - Delivered a `MainHandTwoHanded` `UBowWeaponDefinition`, immutable `UProjectileDefinition`, and one concrete `ACombatProjectile`. Bow validation requires a valid display mesh/socket, a default projectile definition, `Input.PrimaryAttack -> Ability.Attack.Primary`, and exactly one Bow-derived Primary grant; it rejects `UPrimaryAttackAbility` and any duplicate/unrelated Primary grant.
  - The existing equipment component now exposes only the narrow, fail-closed `TryGetEquippedMainHandDisplaySocketTransform(...)` query. `UBowDrawFireAbility` validates held input and the equipped Bow, starts its input/event tasks before the Draw montage, converts an early release into one minimum Draw-ready release, and permits spawning only from a matching current-montage `Event.Attack.Bow.Release` event. Existing Primary cancellation routes cover Dodge and Guard Break; a spawned arrow remains independent of the Ability lifetime.
  - The concrete projectile uses a QueryOnly `USphereComponent` and `UProjectileMovementComponent` with zero gravity, no bounce, no homing, and explicit `ECC_Camera` ignore. It applies at most one hostile-hit GameplayEffect through `FCombatProjectileHitResolver`; same-team, self, dead, invulnerable, invalid, and repeated contacts continue or fail closed as appropriate. The launch snapshots the Damage GE class in a transient Actor property, while source Actor/ASC remain weak references, so post-launch delivery never needs to dereference the authored Projectile Definition.
  - The user confirmed the current `PolyQuestEditor` compile/load, `PolyQuest.Projectile.Lifecycle` plus the four existing regression Automation suites as `Success`, and manual validation with no obvious issue. Gemini completed strict executor self-review; Main completed normal review and a Main adversarial fallback. Luna was unavailable, so no independent Fresh Reviewer result is claimed. All Bow/Projectile DataAssets, Montages, Notifies, Blueprints, Input, map, and imported assets remain local `Content/**` WIP.

- [x] `TODO-03B-2: Player Bow Target Assistance And Limited Homing v1`
  - The Bow registers a scoped requester during Draw/Hold/Release. `APlayerCharacter` converts the local mouse ray to horizontal yaw on the actor-center reference plane, retaining the last valid yaw when deprojection or intersection fails; it does not alter ordinary movement, melee, Dodge, camera, or free vertical aim.
  - On the validated Bow Release event, `FCombatProjectileTargeting` selects at most one hostile living `ACharacter` using team/ASC/Dead/Invulnerable, distance, angle, elevation, pitch, `ECC_Visibility`, camera-forward, and local viewport tests. The candidate rectangle deliberately includes a fixed 6% edge overscan. Projection, viewport, or local-controller absence fails closed to pointer-direction straight flight rather than blocking Bow release.
  - The launch snapshots pointer direction, weak target, steering limits, and target constraints. `ACombatProjectile` first flies straight, then adjusts Velocity only while its target and budget remain valid; it never re-searches, switches targets, rechecks the viewport, or hand-moves position. A turn budget over 90 degrees deliberately enables the accepted casual U-turn behavior. A selected target leaving the screen later does not alter the projectile.
  - The user confirmed `PolyQuest.Projectile.TargetAssist`, `PolyQuest.Projectile.Lifecycle`, `PolyQuest.Equipment.TransactionMatrix`, `PolyQuest.Melee.TraceSourceGeometry`, `PolyQuest.Enemy.AttackSetSelection`, and `PolyQuest.Enemy.CombatSpacing` as `Success`, plus the Standalone visual check showing aligned `ActorYaw` / `AimYaw`. Main fresh review found no P0-P2 blocker. Luna was unavailable, so no independent Fresh Reviewer result is claimed. Bow/Projectile DataAssets, Montages, Notifies, Blueprints, Input, map, and imported assets remain local `Content/**` WIP.

- [x] `TODO-01C3: Player Dodge Recovery Cancel Window, Bow Action Windows, And Continuous Chaining v1`
  - **Slice A - Bow/Charged authored action windows and dynamic Charging:** Bow Draw/Hold/Release/Recovery and Charged Hold accept only matching authored `UAnimNotifyState_ActionDodgeCancelWindow` events for interruption. `State.Action.Charging` is state only, not an unconditional Dodge permission. Bow owns dynamic Charging, scoped CancelWindow/`UAnimNotifyState_MontageRateWindow` listeners, and one `EndAbility()` cleanup; Charged locks only a window that was already valid when `Montage_Pause` emits its pseudo-End, then the resumed timeline's real End clears it.
  - **Slice B - Dodge recovery RateWindow and continuous chaining:** UE 5.8 `bRetriggerInstancedAbility` first evaluates successor `CanActivateAbility()` and Stamina preflight, then lets the engine end the old active Dodge through its own `EndAbility()`; insufficient Stamina does not terminate the old segment. `UDodgeAbility` removes its static self-Dodging blocker and explicitly requires `State.Action.CanCancel.Dodge` whenever Attack or Dodge is active. Matching Dodge recovery `ActionDodgeCancelWindow` and `UAnimNotifyState_MontageRateWindow` events own only the current Dodge tag/rate state. Per-Montage-instance `UAbilityTask_PlayMontageAndWait` callbacks replace the global AnimInstance end delegate, and normal completion waits for `OnCompleted` rather than prematurely ending at Blend Out.
  - The user confirmed the final Slice-B Automation and Scene01 PIE route. Main fresh review found and repaired the early-BlendOut cleanup regression, then confirmed no P0-P2 remaining issue; Luna was unavailable, so the second pass is recorded as Main adversarial fallback rather than an independent Reviewer result. The focused source/documentation commit excludes mutable authored Content, Blueprint, Montage, AnimBP, input, map, Config, and project-file WIP. No new Roadmap debt was accepted; player terminal Ability cancellation remains owned by `TODO-03D`.

- [x] `TODO-03A6A: Weapon-Aware Player Ordinary Locomotion Presentation v1`
  - Historical stage record: it initially introduced `EWeaponLocomotionMode` with a `SwordShield` composition mode and corresponding OffHand composition fields. `TODO-03A6C` later removed that Cartesian-product design; this record is not the current API contract.
  - The retained durable result is an authored MainHand base locomotion family plus an OffHand requirement that its inherited base `LocomotionMode` remains `Default`. The 192 locomotion-sequence Force Root Lock/Root Motion authoring evidence remains local `Content/**` WIP.
  - The original broad mode-matrix and rollback coverage became the Section 13 base extended by `TODO-03A6C`; its original 7/7 Automation and focused PIE evidence remains historical validation for that stage.

- [x] `TODO-03A6B: Shield Guard Locomotion, Pace, And Facing Alignment v1`
  - Completed the Shield Guard presentation closure without changing native Guard/Parry, Stamina, defense resolution, input, or B2 lock-facing authority. The accepted shared `GE_Guard_MoveSpeed` stays Infinite/non-periodic with its existing `0.7` `MoveSpeed` multiplier; the rejected `Override = 300` tuning is not part of the product contract.
  - `GA_PlayerShieldGuard` owns `Ability.Defense.Guard.Shield` and active `State.Action.Guarding.Shield`; `GA_Guard_Sowrd` remains generic `State.Action.Guarding`. The child active tag hierarchically satisfies existing generic Guard checks, but is not an equipped-Shield state.
  - `ABP_Player_Dungeon` derives `IsShieldGuarding` from that child tag and uses full-body `BS_Shield_Walk_Run`. It sets the existing `DefaultGroup.UpperBody` visual blend to zero only for Shield Guard, retaining the upper-body route for single-Sword Guard and keeping Reaction Overlay downstream. The Guard Montage remains for native Ability lifecycle even when its visual slot is bypassed.
  - The user confirmed the focused authored/PIE visual route, including the corrected full-body Shield Guard presentation and retained lock-facing. These authored assets and the new tag Config entry remain local WIP and are excluded from a documentation-only commit.

- [x] `TODO-02C3E: Hit Reaction Tier Classification And Small Reaction v1`
  - Replaced the provisional interrupt-only reaction marker with one native, fail-closed Hit Reaction Tier classifier (`FHitReactionClassifier` in `Source/PolyQuest/Public|Private/Combat/Reaction/`) categorizing applied `FGameplayEffectSpec` AssetTags into `Data.Reaction.Small`, `Data.Reaction.Big`, `Data.Reaction.Launch`, `None`, and fail-closed `Invalid` (multi-tier conflict applies health damage but sends no reaction).
  - Added authoritative Health attribute value change delegates (`OnHealthAttributeChanged`) in `APlayerCharacter` and `AEnemyCharacter` with strict priority: lethal health first, non-damage / null `GEModData` filter, Dead / Stunned / Poise-Broken filter, and target-specific event dispatch (`Event.Reaction.Player.Small`, `Event.Reaction.Enemy.Small`, `Event.Reaction.Enemy.Big`).
  - Added `UPlayerSmallHitReactionAbility` and `UEnemySmallHitReactionAbility` as native `InstancedPerActor`, `ServerOnly` abilities owning `State.Action.SmallHitReacting` for self-reentry guard with no movement or input blocking, playing `AM_SmallHitReaction` on `ReactionOverlayGroup.ReactionOverlay` with unified `EndAbility()` cleanup via `UAnimInstance::OnMontageEnded`.
  - Re-mapped existing `UEnemyHitReactionAbility` internally to Enemy Big (`Ability.Reaction.Enemy.Big`, `Event.Reaction.Enemy.Big`); updated `UEnemyStanceBreakAbility` and `UPlayerGuardBreakAbility` to cancel active Small reactions before their Stunned movement locks.
  - Authored `Data.Reaction.Small` on `GE_Sword_LightAttack_Damage`, `GE_Light_Unarmed_Damage`, and `GE_EnemyMelee_Damage`; migrated `Data.Reaction.Interrupt` on Charged damage GEs to `Data.Reaction.Big`.
  - Configured `ABP_Player_Dungeon` and `ABP_Enemy_Goblin` with decoupled two-stage `Layered blend per bone (spine_01)` overlay for `ReactionOverlayGroup.ReactionOverlay`.
  - Added and passed `PolyQuest.Combat.HitReaction` automation test suite (classifier matrix, CDO tag contracts, and target-side Health dispatch with transient GE fixtures); 8/8 automation suites passed (`Success`). User confirmed Scene01 PIE verification.
  - Fresh Review identified two P2 issues: negative branch health isolation in automation tests, and stale C3B tag references in `ARCHITECTURE.md`. Both were remediated: `HitReactionAutomationTests.cpp` now isolates Health per test branch covering `None`, `Invalid`, `Stunned`, `PoiseBroken`, `AlreadyDead`, and `Lethal`; and `ARCHITECTURE.md` was updated with the C3E contracts. The user verified full 8/8 automation suite passing with 100% success; Main delta review confirmed zero remaining P0-P2 defects.

- [x] `TODO-02C3F: Big Hit Interruption And Grounded Root Motion Knockback v1`
  - Completed the nonlethal grounded `Data.Reaction.Big` route for Player and Enemy. Player now dispatches `Event.Reaction.Player.Big`; native Player Big and retained Enemy Big abilities own `State.Action.HitReacting`, use Montage Root Motion as the sole planar displacement owner, reject airborne activation, prevent ledge walk-off, and converge Falling, cancellation, interruption, teardown, and natural completion through one cleanup path.
  - Player Big blocks and cancels the accepted combat matrix, including the shared `Ability.Attack.Primary` tag that covers both Primary arbitration and Bow. Enemy Big stops path movement only after Montage startup, cancels Enemy Melee/Small, and keeps existing StateTree recovery through `State.Action.HitReacting`. Guard Break and Stance Break remain higher-priority Stunned routes.
  - Added pure `FHitReactionImpactResolver` for a fail-closed target-local attacker-direction snapshot. It does not steer the current local-backward Root Motion; no launch, directional selection, force/impulse, Motion Warping, or movement-mode reset was introduced.
  - Remediated the stale Unarmed Charged GE: migrated its remaining `Data.Reaction.Interrupt` Asset Tag to `Data.Reaction.Big`, verified zero asset references to legacy Interrupt/Enemy.Hit tags through Rider offline asset search, then removed the three retired config Tags. The focused Automation test now covers conflicting ImpactNormal/fallback precedence and `NaN`/`Inf` failures.
  - User confirmed `PolyQuest.Combat.HitReaction` Automation success and Scene01 PIE, including the migrated Unarmed Charged Big reaction route. Main fresh review found and remediated the stale asset/tag P2; `gpt-5.6-luna / xhigh` remained unavailable, so the required second pass was a Main adversarial fallback rather than an independent review. Mutable authored assets remain excluded from the source/config/document commit.

- [x] `TODO-02C3G: Launch, Airborne, And Landing Reaction v1`
  - Delivered the nonlethal `Data.Reaction.Launch` route for Player and Enemy: source Health dispatch, target-specific Launch Events, matching ServerOnly native abilities, and `UAnimNotify_ReactionLaunchCommit` complete `Takeoff -> AwaitingAirborne -> Airborne -> LandingRecovery` without creating a second gameplay state source.
  - C3G uses CharacterMovement as the sole physical movement owner. Commit pauses a root-locked full-body Takeoff pose, `LaunchCharacter` owns flight, a bounded `0.10s` Falling watchdog prevents a pending launch from retaining reaction state, and actual landing clears residual horizontal velocity before the in-place LandingRecovery route. Stale montage/delegate/task callbacks, early interruption, renewed Falling, and teardown converge through `EndAbility()`.
  - `FHitReactionImpactResolver` now uses the valid actor-center relative line as the canonical `Target -> Attacker` direction, with ImpactNormal only as fallback; Launch negates it for attacker-away spacing. Guard Break and Stance Break remain higher-priority routes and cancel active Launch.
  - The user confirmed the final Automation and focused PIE route after the lifecycle repair. Main strict review found no P0-P2; `gpt-5.6-luna / xhigh` was unavailable, so the required second pass is recorded as a Main adversarial fallback rather than an independent Reviewer result. Mutable Launch DataAssets, GE/GA children, Montages, Notifies, AnimBPs, Blueprints, Input, and map fixtures remain local `Content/**` WIP.

- [x] `TODO-01C4: Player Reaction Recovery Dodge Cancel Windows v1`
  - Completed the Player-only authored `LandingRecovery -> Dodge` route. Player Launch creates persistent Dodge CancelWindow listeners before Takeoff, but accepts a Begin/End only during an active matching LandingRecovery Montage or Slot sequence. Its one scoped `State.Action.CanCancel.Dodge` contribution is removed at window End and before every Montage/task cleanup; it never grants Defense cancellation.
  - Player Launch now excludes `Ability.Dodge` only from `BlockAbilitiesWithTag` while retaining it in `AbilitiesToCancel`, so an incoming Launch still interrupts a live Dodge. `UDodgeAbility` requires `CanCancel.Dodge` for Attacking, Dodging, or HitReacting; after normal grounded/Stamina `CommitAbility()` succeeds, it cancels Player Launch. Current normal-Dodge soft overspend remains unchanged: any positive Stamina can start Dodge and the authored cost may clamp it to zero.
  - User-confirmed Automation and focused PIE cover the completed route. Main normal review and Main adversarial fallback found no P0-P2; `gpt-5.6-luna / xhigh` was unavailable, so no independent reviewer is claimed. The mutable CancelWindow placement, Montage, GA/GE, Blueprint, AnimBP, input, and map fixture remain excluded local `Content/**` WIP. No new C4 debt was accepted; `TODO-02C3H` remains the owner of landing-deferred Stance/Guard Break resolution.

- [x] `TODO-02C3H: Landing-Deferred Enemy Stance Break Resolution v1`
  - Completed the Enemy-only legal Launch-and-break outcome. A nonlethal Launch damage GE may use the existing Poise-delivery contract; once Enemy Launch Takeoff has actually started, an Enemy at zero Poise owns one deferred Stance Break intent and cannot dispatch `Event.Reaction.Enemy.StanceBreak` during Takeoff, AwaitingAirborne, Airborne, or LandingRecovery.
  - The pre-Launch ordinary next-tick Stance Break timer is cleared only when the confirmed Launch begins; a zero-Poise transition during an active Launch records the same intent without making that timer. Natural LandingRecovery end releases Launch reaction state through `Super::EndAbility()` before one shared current-state eligibility check dispatches at most one Stance Break.
  - Positive Poise recovery clears the intent. Death, destruction, EndPlay, failed setup, interruption, cancellation, renewed Falling, and failed recovery never issue a late break. A surviving Enemy still at zero Poise after abnormal Launch cleanup restores Poise through the existing recovery GameplayEffect instead of waiting for a later ordinary break.
  - Player Guard/Parry absorption and immediate Player Guard Break intentionally remain their existing routes; this stage does not create a Player pending-Guard state or an implied follow-up TODO. The user confirmed `PolyQuest.Combat.HitReaction` Automation and focused PIE. Main normal review and Main adversarial fallback found no P0-P2; `gpt-5.6-luna / xhigh` and code-review-graph were unavailable, so no independent Reviewer or graph-impact claim is made. Mutable authored assets remain local `Content/**` WIP.

- [x] `TODO-07A1: Core Vital HUD And Enemy Bars v1`
  - Completed the passive vital-display baseline: one local Player viewport HUD shows current/max Health and Stamina, and every living Enemy owns one always-visible Screen Space overhead Health bar with no numerical text, Poise, target marker, fade, or damage number.
  - Existing ASC Attributes remain the sole source of Health, MaxHealth, Stamina, and MaxStamina. `APolyQuestPlayerController` owns idempotent local HUD creation plus re-possession-safe four-attribute binding; `AEnemyCharacter` owns its Health/MaxHealth UI subscription and hides/cleans it on death and teardown. The HUD does not change cursor, Game-and-UI input, focus, mappings, camera, or gameplay routing.
  - The user confirmed `PolyQuest.UI.VitalHUD` Automation, manual `PolyQuestEditor` compilation, Editor asset readback, and PIE for initial values, Player damage and Stamina spend/recovery, multiple independent Enemy bars, death hiding, restart/re-possession, and unchanged mouse/input behavior. No playable Player-healing source exists yet; Automation directly exercises the same ASC positive-Health refresh branch, while `TODO-03E` remains the first gameplay healing loop.
  - The user-authored UMG/Blueprint closure and all other `Content/**` assets remain local WIP and are excluded from the focused source/test/document commit. `TODO-07A2` owns the later idle-hidden/damage-or-lock visibility policy; `TODO-02B1` may highlight the existing bar of its active locked target.

- [x] `TODO-02B1: Screen-Space Player Lock-On And Target Cycling v1`
  - Builds on the completed fixed-world oblique camera and on `TODO-07A1`; it does not restore over-the-shoulder orbit, camera recentering, camera yaw ownership, or free-look. Movement remains camera-relative.
  - A new middle-mouse Enhanced Input action toggles one Player-owned weak locked-target reference. On acquisition it considers only hostile, living, non-invulnerable candidates whose aim point projects strictly inside the current viewport, then selects the candidate nearest to the mouse cursor. The Bow-only 6 percent viewport overscan and launch-cone ranking do not apply here.
  - A new wheel-axis action cycles the existing lock through one deterministic screen-space order; wheel input with no lock is a no-op. Death, destruction, invalid ASC/team state, or failure of the then-strict validation route clears the lock without automatic retargeting; the later `TODO-02B3` milestone refines only already-owned target retention. The active target's existing overhead Health bar is highlighted while locked.
  - A valid lock supplies action-facing only at a legal action start and must never continuously overwrite Root Motion rotation. It does not change normal camera-relative movement, alter Enemy AI, add a GAS action state, buffer input, or change Bow pointer-facing/homing; Bow integration belongs only to `TODO-03B-3`.
  - The user confirmed `PolyQuest.Player.LockOn` Automation, manual `PolyQuestEditor` compilation, Editor input/Widget readback, and focused PIE for no target, cursor-nearest acquisition, middle-click clear/reacquire, wheel direction/order, viewport loss, death/destruction clear, action-facing, camera stability, target-bar highlight cleanup, directional/no-input Dodge, and the Bow mouse-facing/Release target-assist/Homing regression.
  - Main completed the default single defect-first fresh review with no P0-P2 source finding. The focused implementation commit is `e1d340b`; it deliberately excludes all `Content/**`, Input/Widget Blueprint, map, Config, project-file, and unrelated WIP. `TODO-02B2` exclusively owns continuous lock-facing, Sprint free-run, and death-only retargeting; `TODO-03B-3` exclusively owns Bow lock-target preference.

- [x] `TODO-02B2: Locked Locomotion Facing And Death Retarget v1`
  - Keeps the completed B1 local weak lock, strict in-viewport candidate rule, camera-relative movement, fixed elevated oblique camera, and existing Enemy Health-bar highlight. It adds no camera orbit/recentering, replication, Gameplay Tag, or generic targeting framework.
  - A valid lock continuously turns grounded Idle/Walk/Run, Guard, and non-Root-Motion `MOVE_None` Parry toward the target at the native `800 degrees/second` yaw default. Sprint deliberately retains the lock/highlight but restores movement-facing; Attack, Dodge, Small/Big/Launch reaction, Bow, Root Motion, death, and Stunned retain yaw ownership.
  - The Player caches the last valid screen candidate. Only a confirmed current-target death scans once and hands off to the next valid strict-viewport target in clockwise order; no successor clears the lock. Before the later `TODO-02B3` retention refinement, viewport exit, invulnerability, missing ASC/team validity, destruction, Player death, projection failure, and explicit middle-click clear did not auto-retarget.
  - The user confirmed `PolyQuest.Player.LockOn` Automation and focused PIE for target-relative locomotion, Guard/Parry, Sprint free-run/resume, action/Root-Motion protection, death handoff/highlight transfer, non-death clear, Bow regression, the `800 degrees/second` retune, and fixed `WeaponMesh` camera-collision behavior. Main's single defect-first fresh review found no P0-P2 source issue.

- [x] `TODO-03B-3: Player Bow Locked-Target Preference v1`
  - Completed the narrow Player Bow-to-lock bridge. With `bEnableTargetAssist=true`, `UBowDrawFireAbility::SpawnProjectile()` first asks the Player's C++-only `ResolveValidLockedTarget()` to run the existing B2 validation once. A valid lock, including B2's one-time death handoff, becomes the existing Projectile target snapshot; it does not alter mouse-derived initial flight direction, Draw/Hold facing, camera, collision, Homing limits, or flight-time target selection.
  - No valid lock falls through to the exact B2 automatic candidate query, including current geometry, visibility, and 6% viewport-edge overscan. With Target Assist disabled, Bow reads neither lock nor automatic candidate and keeps direct-flight semantics. A projectile only owns the target snapshot made at Release, so later Player cycling/clearing/death handoff cannot retarget it.
  - `PolyQuest.Projectile.Lifecycle` now exercises the real equipment -> display socket -> Release chain with a transient Bow definition and the existing read-only Hero Mesh socket fixture. It covers lock preference, pointer-direction preservation, no-lock fallback, Target Assist-disabled direct flight, invalid-lock fallback, death handoff, and post-Release snapshot stability. The user confirmed `PolyQuest.Projectile.Lifecycle`, `PolyQuest.Projectile.TargetAssist`, and `PolyQuest.Player.LockOn` Automation plus focused PIE. Main's default single defect-first fresh review found no P0-P2 source defect; code-review-graph did not recognize Unreal Automation macro coverage, so direct test/source review was the coverage evidence. All authored assets remain local `Content/**` WIP and are excluded from the focused commit.

- [x] `TODO-03H2: Core Combat Runtime Health And Lean Review v1`
  - Completed the read-first health gate over Player combat, lock-on, Bow/Projectile, hit reactions, equipment, Enemy combat/AI, and vital HUD. Direct source/caller review found no P0-P2 teardown, cancellation, delegate, target-snapshot, camera-collision, or ownership defect; retained compatibility paths were not removed without a proven replacement closure.
  - The one confirmed signal-obscuring fixture defect was B3 Release coverage directly calling `GetAbilityLevel()` on a transient Bow Ability without its ASC Spec Handle. The test now obtains the real `UBowDrawFireAbility` Spec granted by the equipment transaction, asserts its valid level-one Handle, and injects that Handle through a `WITH_DEV_AUTOMATION_TESTS` helper. Runtime Bow Release, lock preference, Projectile snapshots, and authored data remain unchanged; the five `Invalid AbilitySpecHandle` warnings are gone.
  - The user confirmed manual `PolyQuestEditor` compilation, all ten focused Automation suites, and focused `Scene01` PIE. Main's required defect-first fresh review and Gemini's separate read-only audit both found no P0-P2. Remaining deterministic native-fixture configuration warnings are classified below as test signal-hygiene debt, not production configuration failures.

- [x] `TODO-03H3: Automation Fixture Signal Hygiene v1`
  - Completed the H2 warning-debt closure without suppressing logs or changing product runtime behavior. The private deferred-spawn fixture supplies lean Player Loadout/owner-mesh Trace/Stamina Regen setup and passive Enemy AI/Ragdoll/Poise setup before `BeginPlay`; it remains entirely under `WITH_DEV_AUTOMATION_TESTS`.
  - The user confirmed `PolyQuestEditor` compilation and all ten existing Automation suites through the Editor front-end. Missing-fixture success-path warnings are gone. Remaining logs are deliberate negative coverage for reaction-tag fail-closed behavior, Stance Break fallback, equipment preflight/active-swap/rollback, and static Trace geometry. H3 has no PIE gate. Main's single defect-first fresh review found no P0-P2.

- [x] `TODO-01I: Player Exhaustion Recovery And Walk-Pace v1`
  - Replaced the shared AttributeSet's immediate Exhausted-tag side effect with one authority-only `APlayerCharacter` lifecycle: first Stamina depletion adds one Player loose `State.Status.Exhausted` contribution, applies one exact Infinite `MoveSpeed x0.7` handle, and starts one fixed three-second timer. Normal recovery continues; only the original timer plus positive Stamina clears the action lock, and repeated depletion cannot extend the cycle.
  - The local `BP_Player` reference reuses the existing Guard-speed GameplayEffect. Jump keeps valid zero-cost Cost/Delay authored references but bypasses the shared positive-Stamina action gate and skips the generic regeneration-delay application, so it remains available without spending Stamina or clearing/pausing Exhaustion. Attack, prepared skills, Bow, Guard, Parry, Dodge, and Sprint remain blocked; committed actions retain their existing cleanup.
  - The required matrix also found and repaired a separate Enemy Poise/Launch transaction bug: no callback-local `FGameplayEffectSpec*` is retained across effect application; Definition, EffectContext, and the current executed Poise modifier prove a same-transaction Launch. New `PolyQuest.Player.Exhaustion` coverage and the ten existing suites passed. The user confirmed manual `PolyQuestEditor` compilation, focused `Scene01` PIE, and all eleven Automation suites. Main's single defect-first fresh review found no P0-P2; authored assets remain local `Content/**` WIP.
  - `TODO-03A3C`, `TODO-03A3D`, `TODO-02C3K`, `TODO-02C3L`, `TODO-02C3M`, and `TODO-02C3N` are now complete. With `TODO-02B3` closed, the player-combat-experience-first sequence is `TODO-03A3E -> TODO-07B4 -> TODO-03A7 -> TODO-05A -> TODO-05B -> TODO-05C`; `TODO-03C` follows that sequence as the ranged-enemy gate. `TODO-05A -> TODO-05B -> TODO-05C` remains the technical dependency order inside the punish family.

- [x] `TODO-07B2: Melee Weapon Trail v1`
  - Completed one Character/root-owned `UMeleeWeaponTrailComponent` backed by the private Engine `Niagara` dependency. It receives only the existing world-space `User.BladeBase` / `User.BladeTip` positions and never attaches to, moves, or alters the combat marker components.
  - The exact active `UAbilityTask_MeleeTraceWindow` owns Start/Update/End through a weak requester token: initial/current samples are reused before the unchanged Sweep/Resolver path, stale Task teardown cannot stop a successor, and a missing System remains a silent no-op. Normal `Deactivate()` preserves authored particle fade; no GameplayCue, generic `State.Action.Attacking` trigger, Bow/Projectile trail, hit-logic, or target-selection route was added.
  - `PolyQuest.Melee.WeaponTrail` covers a real ASC-hosted Task, requester arbitration, endpoint invalidation/recovery, Player/Enemy teardown, and no-asset behavior. The user confirmed focused Scene01 PIE visual validation and the thirteen-suite Editor Automation matrix; authored Niagara and Blueprint assignments remain local `Content/**` WIP and are excluded from the focused commit.

- [x] `TODO-03A6C: OffHand Presentation And Shield Guard Locomotion Decoupling v1`
  - Completed the Player-facing state split without changing equipment transactions, DefenseProfile precedence, Guard/Parry routing, input, Stamina, Guard Break, B2 lock-facing, Bow aim, or drop-swap behavior. `GetResolvedLocomotionMode()` is now MainHand-only (`Default = 0`, `LightSword = 1`, `HeavySword = 2`, `Bow = 4`), and `HasShieldEquipped()` separately exposes only a committed `UOffHandWeaponDefinition` with `bProvidesShieldPresentation = true`.
  - Removed `SwordShield`, `RequiredMainHandLocomotionMode`, and `CompositionLocomotionMode` from native DataAsset contracts and OffHand preflight. Bow explicitly retains serialized numeric value `4`; retired ordinal `3` and every other invalid locomotion enum value fail closed. No compatibility alias, enum redirect, new Tag, or replacement composition field was added.
  - `ABP_Player_Dungeon` uses the conjunction `HasShieldEquipped() && State.Action.Guarding.Shield` for full-body `BS_Shield_Walk_Run`. Generic `State.Action.Guarding` and `Ability.Defense.Guard.Shield` do not express Shield-equipped state, so single-Sword Guard remains on its existing upper-body branch. The regular enum branches are now `Default`, `LightSword`, `HeavySword`, and `Bow`; the obsolete SwordShield branch was removed from the AnimBP.
  - The user confirmed the Shield DataAsset readback, Editor compilation, fourteen-suite Automation matrix, and focused Scene01 PIE. `PolyQuest.Equipment.TransactionMatrix` covers invalid ordinal `3`, preserved Bow value `4`, Shield/non-Shield OffHand facts, TwoHanded clearing, and Apply/Drop rollback non-drift. Main fresh review found no P0-P2.
  - `BS_SwordShield_Walk_Run` remains a retained local Content asset after its AnimBP branch was removed. It was deliberately excluded from this code/documentation commit; a future deletion requires separate Reference Viewer evidence and user approval.

- [x] `TODO-03A6D: Multi-Source Melee Trace And Per-Window Selection v1`
  - Replaced the single MainHand Blade Base/Tip assumption with a bounded authored Trace Source profile (`FOwnerMeshMeleeTraceSource`) in `UMeleeWeaponDefinition` and source-keyed marker storage in `UWeaponEquipmentComponent`.
  - Added per-window source selection via `UAnimNotifyState_AttackTraceWindow::TraceSourceNames` and `OptionalObject2` event routing; `UAbilityTask_MeleeTraceWindow` performs Phase 1 all-or-nothing endpoint validation, Phase 2 per-source trail updates, and Phase 3 multi-source sweeping with shared target deduplication in a single window.
  - Added source-keyed lazy child component lifecycle to `UMeleeWeaponTrailComponent`. The user confirmed the final fifteen-suite Automation matrix and focused PIE. Main fresh review found one P2 right-only test gap, repaired it, and the final targeted/full Automation evidence passed; no P0/P1 production defect remained.

- [x] `TODO-07B3: Projectile Flight Trail v1`
  - Added optional Definition-authored `FlightTrailSystem`, `FlightTrailSocketName`, and terminal timeout data to `UProjectileDefinition`; no Arrow Blueprint subclass, GameplayCue, second projectile Actor, new Tag, or new module was introduced. `ACombatProjectile` owns one inactive `FlightTrailComponent`, resolves it after successful initialization, and preserves movement, collision, Target Assist, Homing, and GAS hit delivery.
  - A null System is silent and a missing requested Socket falls back to the display-component root with one focused warning. Pawn hit, blocking hit, and lifespan expiry immediately stop gameplay state, then detach the still-owned component with world transform, hide only the projectile mesh, stop emission through `Deactivate()`, and destroy the Actor only after `OnSystemFinished` or the Definition timeout fallback. A non-finite/non-positive configured timeout normalizes to `0.35s`; the timer is armed before `Deactivate()` to remain safe if Niagara completes synchronously.
  - `PolyQuest.Projectile.FlightTrail` covers component defaults, null/valid/missing Socket routes, hit/lifespan terminal behavior, duplicate-hit prevention, external destroy, ordinary timeout, non-finite timeout fallback, and null-System invalid-timeout silence without a renderer/GPU dependency. The user confirmed focused PIE and the sixteen-suite Editor Automation matrix. Main Fresh Review found two P1 and one P2 issue, which were repaired; delta review found no remaining P0-P2. User-authored Niagara, Mesh Socket, Definition assignment, Blueprints, and other `Content/**` WIP remain excluded from the focused commit.

- [x] `TODO-03AI3: Enemy Root-Motion Facing Ownership And Tactical Pace v1`
  - While an Enemy has active Root Motion, `AEnemyAIController` retains its combat target but clears Gameplay Focus and returns before Controller-driven ControlRotation/Pawn-yaw writes. When Root Motion ends, a valid target restores focus once and Pawn yaw converges through a bounded `800 degrees/second` handoff; no navigation restart, Montage cancellation, or Root-Motion rotation override is introduced.
  - Cooldown Reposition captures the exact prior `MaxWalkSpeed`, applies its fixed `300` tactical pace only during that MoveTo, and restores on success, failure, stop, UnPossess, and death. Normal Approach/pursuit speed stays unchanged. The user confirmed focused Scene01 PIE and Editor Automation; no separately logged Development Editor build is claimed.

- [x] `TODO-03AI3B: Enemy Combat Target Retention v1`
  - Initial Sight acquisition remains 140 degrees. Once the Player is `CurrentTarget`, a negative Sight stimulus preserves it only while the Player remains within existing 2D `LoseSightRadius` and the Enemy remains inside Home `LeashRadius`; pre-`Super::Tick` revalidation clears through the existing one-time `TargetLost` route when either condition fails. No global 360-degree awareness, Search/LastKnownLocation, hearing/damage sense, timer, DataAsset field, Tag, target swap, or StateTree edit was added.
  - Main Fresh Review found and repaired the late-callback P1: an unpossessed Controller can no longer write `CurrentTarget` through a delayed positive perception because `SetCurrentTarget()` now fails closed without a Pawn. The native regression exercises `UnPossess -> late positive perception -> re-Possess -> fresh positive perception` through the production helper path. The user confirmed all eighteen Editor Automation suites; Main delta Fresh Review found no remaining P0/P1/P2. No separately logged Development Editor build is claimed.

- [x] `TODO-03H4: Combat Delivery And Presentation Health Review v1`
  - Main completed a read-first integration audit over delivery ownership, equipment transactions, Bow/projectile terminal paths, Enemy Root-Motion-facing/target-retention, and Automation signal handling. Source/static review found no P0-P2 lifecycle, authority, data-integrity, or success-path logging blocker; no production code, asset, Tag, Input, StateTree, or authored-content change was made.
  - H4 interpreted the existing user-confirmed eighteen-suite Editor Automation matrix and focused Scene01 PIE evidence from the completed owning stages; it did not rerun compile, Automation, Editor readback, or PIE because the audit changed no runtime surface. A report that counted seventeen `*AutomationTests.cpp` files omitted `EnemyCombatSpacingTests.cpp`; the repository has eighteen Automation macro declarations, so this was corrected as an audit-coverage issue rather than a failed test.
  - `TODO-03H4B` completed the two header-hygiene micro-slices: it moved the `CombatLoadoutDefinition.h` include from `MeleeWeaponDefinition.h` to its direct-use `.cpp` while retaining the `AssociatedLoadout->IsRouteTableValid()` dependency, and it audited the four Public Ability `GameplayAbilityTypes.h` includes as required for self-sufficient reflected declarations. The current symmetric definition-wide Niagara System and Trace Radius remain accepted; do not pre-add per-source overrides until a real asymmetric source needs them.
  - Fixed Controller-owned `300` cooldown Reposition pace remains conditional future AI debt, not a slimming change: migrate to `UEnemyAIProfile` authored pace plus a dedicated Enemy MoveSpeed GameplayEffect only if a second Enemy needs a distinct pace or Enemy MoveSpeed effects become real; never reuse Player `GE_Walk_MoveSpeed`.
  - **Enemy spacing test-fidelity P3:** `EnemyCombatSpacingTests.cpp` simulates parts of Approach/Reposition locally instead of invoking the production Controller route. Current direct Root-Motion-facing and target-retention coverage remains intact. Owner/closure trigger: if `TODO-03C` changes shared Approach/Reposition behavior, add a production `AEnemyAIController` / StateTree-facing fixture before accepting that change.

- [x] `TODO-03H4A: Authoring Tooltip Metadata v1`
  - Completed the Native authoring-legibility slice without changing combat behavior or tuning: 145 concise Chinese `ToolTip` metadata entries now cover the approved editable DataAsset, Gameplay Ability, Character, Trace, Projectile, Enemy AI, and presentation fields across 34 Public Headers. Existing English C++ comments remain intact; no field shape/default, API, Tag, Input, Config, module, asset, or runtime path changed.
  - Main Fresh Review corrected author-facing ambiguity in Enemy AttackRange, Physics Asset/ragdoll fallback, retained Sight, owner-mesh multi-Trace constraints, Target Assist pitch, and hit-feedback scope. Main static checks found no UHT-style metadata collision, zero Rider Errors, and a clean `git diff --check`; warning-level hygiene findings on untouched declarations remain owned by the separate slimming/health route.
  - The user confirmed the current-stage tests passed and authorized closeout. Blueprint-local Tooltip/Description authoring remains user-owned `Content/**` WIP and is deliberately excluded from the focused source/documentation commit; no separate compile, PIE, or Editor-hover log is asserted here beyond the user's confirmation.

- [x] `TODO-02C4: Enemy Death Ragdoll Impact v1`
  - A first lethal Enemy Health GameplayEffect now copies only its current finite attacker-away direction into a value-only velocity-change candidate before the existing Dead-tag teardown begins. `StartDeathRagdoll()` consumes the candidate before every early return, retains the established ragdoll setup, and applies one `AddImpulse(..., bVelChange = true)` only to an explicitly authored named body that exists and is simulating physics. No callback-local Spec/Context pointer, prior nonlethal hit, Tag, death Ability, damage path, Root Motion route, or corpse lifecycle is retained or added.
  - `NAME_None` deliberately means ordinary no-force ragdoll. Invalid/non-simulated non-None bodies retain ordinary ragdoll and produce one configuration Warning; missing/invalid Context, zero/non-finite direction, invalid speed, disabled ragdoll, missing Physics Asset, repeat death, and teardown remain fail-closed. Final body identity and mutable force tuning belong to each Enemy Blueprint/Physics Asset, not a shared AI or damage DataAsset.
  - `PolyQuest.Enemy.DeathRagdoll` now covers valid current-context capture, attacker A -> attacker B isolation, invalid/empty Context, zero direction, invalid velocity, direct Dead-tag receipt, once-only consumption, and the silent disabled-ragdoll fixture. Main delta Fresh Review found no remaining P0/P1/P2 after hardening setup assertions. The user confirmed `BP_Enemy_Goblin` readback, focused PIE, and all nineteen Editor Automation suites; authored Blueprint and Physics Asset WIP remain excluded from the focused commit.

- [x] `TODO-03H4B: Native Slimming, Reuse, And Decoupling v1`
  - Completed two bounded native slimming slices before `TODO-03C`, without changing public API, GAS/ASC ownership, Gameplay Tags, Input, serialized assets, Blueprint class paths, Config, or Content. This is not a generic melee framework or a broad cleanup pass.
  - Slice A introduced the private, non-reflected `FMeleeTraceWindowLifecycle` and moved only the repeated Task open/keep/close mechanics from Light Attack, Charged Attack, Sprint Attack, and Player Melee Skill. Per-Ability montage identity, stale-Notify arbitration, damage/cost, Charged SetByCaller map, cancellation, and terminal `EndAbility()` ownership remain local. `PolyQuest.Melee.TraceWindowLifecycle` covers active-Ability scalar/map opening, active-task retention, closed-task replacement, scalar/map synchronous endpoint fail-closed cleanup, and Trace/Trail requester release.
  - `UEnemyMeleeAbility` deliberately remains outside the helper: it already shares the Task/source pipeline, but snapshots attack-profile Damage GE and Guard Stamina from AI state and has no direct real-Ability trace-lifecycle fixture. Its conditional adoption trigger and required test contract are canonicalized under Recommendations; no Enemy-only helper or AI/AttackProfile ownership change was added.
  - Slice B moved `CombatLoadoutDefinition.h` from the public `MeleeWeaponDefinition.h` surface to the direct-use `.cpp`, retaining the type forward declaration and the unchanged `AssociatedLoadout->IsRouteTableValid()` validation. The four Public Ability `GameplayAbilityTypes.h` includes were audited and intentionally retained because their reflected `FGameplayEventData` by-value declarations need the complete type.
  - User evidence: Slice A's complete twenty-suite Editor Automation matrix and focused `Scene01` PIE passed; the user also confirmed `PolyQuestEditor (Development Editor)` compilation after Slice B. Main Fresh/Delta Fresh Review found no P0-P2. Slice B has no new runtime behavior, so no repeat PIE or Automation result is claimed. Mutable authored `Content/**`, Config, project-file, map, Blueprint, AnimBP, GA/GE, Montage, and imported-asset WIP remain excluded from the two scoped commits.

- [x] `TODO-02C3I: Launch Reaction Facing Ownership And Presentation v1`
  - Completed the Player/Enemy Launch-facing correction before `TODO-03C`: a finite planar attack-source context now makes the victim face the attacker exactly once at `Event.Reaction.Launch.Commit`, while `CharacterMovement` launches it away from that attacker. Hit classification, damage, Poise, launch speed, input, and root-motion navigation remain unchanged.
  - `FHitReactionImpactResolver` freezes the canonical target-local `Target -> Attacker` direction with a pre-turn Actor Yaw and derives both world-facing Yaw and attacker-away velocity from that same pair. Invalid, zero, NaN, or Inf input fails closed without changing the Actor; the legacy velocity helper remains a compatibility forwarding wrapper.
  - Player `State.Action.HitReacting` continues to suppress ordinary locomotion/lock yaw. Enemy `AEnemyAIController` preserves Gameplay Focus and yields Controller yaw only while the exact `Ability.Reaction.Enemy.Launch` Spec is active; Enemy Big remains outside this gate, and Root Motion retains its existing higher priority and recovery behavior.
  - `PolyQuest.Combat.HitReaction` now covers cardinal and rotated resolver cases, the `+180/-180` boundary, fail-closed outputs, and wrapper regressions. `PolyQuest.Enemy.RootMotionFacing` proves loose `State.Action.HitReacting` still permits ordinary target-facing while an active real Launch Spec preserves yaw and Focus, then restores normal facing. The user confirmed all twenty Editor Automation suites and focused `Scene01` PIE; no separate Development Editor build log is claimed. Mutable GA/GE/Montage/AnimBP/Blueprint assets remain local WIP.

- [x] `TODO-02C3J: Launch Facing Smoothing v1`
  - Completed the Player/Enemy Launch presentation refinement before `TODO-03C`: the prior Commit-time yaw snap is now a bounded fixed-rate pre-launch turn. `FLaunchFacingSmoothingState` freezes the C3I-derived target yaw and attacker-away velocity once, while the C++-only `UAbilityTask_TurnToFacing` advances yaw on the shortest arc, preserves Pitch/Roll, and never writes rotation after launch.
  - Commit and turn completion are two independent gates. Whichever arrives first only records readiness; `CommitFrozenLaunch()` consumes the frozen velocity exactly once after both are ready, then hands movement back to the existing `CharacterMovement` launch/landing route. Cancellation, death, invalid state, Root Motion, premature Falling, and unexpected paused-Takeoff termination all converge fail-closed through the established Ability cleanup.
  - The non-reflected state type is physically Public with `POLYQUEST_API` because exported Public Launch Ability headers hold it by value; it remains an internal C++ lifecycle detail with no Blueprint/reflection, Tag, Config, asset, Controller, Character, Resolver, or generic turning-framework expansion. Main Fresh Review found and repaired that header-boundary P2; delta review found no remaining P0-P3.
  - The user confirmed `PolyQuest.Combat.LaunchFacingSmoothing`, `PolyQuest.Combat.HitReaction`, and the complete combat/reaction regression matrix passed, plus focused Scene01 PIE covering smooth turning, launch, and landing recovery. No separate Development Editor build log is claimed; authored GA/GE/Montage/AnimBP/Blueprint assets remain local WIP.

- [x] `TODO-03A3C: World Pickup Drop Grounding And Presentation v1`
  - Completed the direct drop-swap presentation repair: `UWeaponDefinition` now owns the finite `WorldPickupDisplayTransform`, and `AWorldWeaponPickup` applies it to both placed and deferred-spawned visual meshes without reusing equipped hand-socket offsets. `FWorldPickupGrounding` projects all eight transformed mesh-bounds corners along the actual ground normal and places only the Actor Root, preserving authored world orientation while keeping the lowest visual point `2cm` clear.
  - `StageDisplacedDrops` retains its scatter, zero Root rotation, provisional `NoCollision -> QueryOnly` commit, former-owner cooldown, and atomic `TryEquipWorldPickup` rollback. An invalid visible-mesh bounds/transform calculation destroys the current and prior provisional drops before the existing transaction rollback; a valid no-mesh definition retains the previous `ImpactPoint + Normal * 2cm` fallback. No Tick, simulated physics, throw/bounce, inventory, persistence, actor hierarchy, GAS, Tag, or input route was added.
  - The user confirmed the A3C Automation validation and focused Scene01 PIE passed. Main fresh review found no P0-P2. The then-local Shield GA fixture-path relocation was intentionally excluded from the A3C commit and is adopted as the narrow test-asset closure of `TODO-03A3D`; no separately reported new Development Editor compilation result is claimed.

- [x] `TODO-03A3D: Equipped Weapon Display Scale v1`
  - Completed one finite, strictly-positive `DisplayScale` vector on `UWeaponDefinition`, defaulting to `(1,1,1)`. The shared `UWeaponEquipmentComponent` display-spawn path applies it after the existing socket attachment, location, and rotation rules for both MainHand and OffHand. It remains deliberately independent from `WorldPickupDisplayTransform.Scale`, so held and world-pickup presentation can be authored to match without a hidden runtime coupling.
  - The selected meaning of "同步放大" is now enforced at the runtime geometry boundary: display-mesh sockets, display-attached Blade Base/Tip markers, and the Bow's live `LaunchSocketName` transform inherit the display scale, keeping visual size, melee sample positions, and projectile origin coherent. Owner-SkeletalMesh sources, including Unarmed body-contact markers, remain unscaled; `TraceRadius` remains an authored collision thickness.
  - `PolyQuest.Equipment.TransactionMatrix` covers valid/invalid scale preflight, MainHand/OffHand scale application without location/rotation drift, scaled marker and Bow launch-Socket positions, Unarmed non-display behavior, and rollback restoration. The test's two Shield GA paths now use `Content/_Abilities/Weapon/Shield/GA_PlayerShieldGuard.uasset` and `GA_Guard_Sowrd.uasset`; these authored fixtures are the only adopted Content closure. The user confirmed Automation and focused PIE passed, and Main fresh review found no P0-P2. No separately reported Development Editor compile result is claimed.

- [x] `TODO-02C3K: Combat Impact Feedback v1`
  - Completed the three-channel Combat Impact Feedback v1 contract on confirmed Player-to-Enemy Health-damage events, including the first lethal hit before Enemy Dead teardown:
    - Global Hit-Stop is owned solely by `APolyQuestPlayerController`, requested via `RequestCombatImpactHitStop(DurationSeconds, TimeDilation)` with monotonic arbitration (`min` dilation, `max` expiry), real-time clock tracking against `UWorld::GetRealTimeSeconds()` in `Tick()`, and unified idempotent restoration on expiry, `EndPlay()`, `Destroyed()`, and `BeginDestroy()`. `UGameplayStatics` is the only write route; `KINDA_SMALL_NUMBER` comparisons preserve an intervening external time-dilation owner, including teardown before the next Controller tick.
    - `AEnemyCharacter` owns authorable Small/Big/Launch feedback presets (user-approved freeze: `Small = 0.03s/0.1`, `Big = 0.05s/0.03`, `Launch = 0.05s/0.05`), one shared `USoundBase` played at `ImpactPoint` (with `GetActorLocation()` fallback), and one unattached, auto-destroyed world-space `UNiagaraSystem` blood burst aligned with `ImpactNormal`. The first lethal hit dispatches this feedback before `SetDeadState()` begins terminal teardown; subsequent Dead callbacks remain silent. Hits with missing, non-finite, zero-normal, or cross-actor HitResults safely skip blood without suppressing sound or hit-stop. One GE execution with multiple negative Health modifiers dispatches once through that execution's `ModifiedAttributes`; independent same-frame GE applications, including a shared Context, each dispatch once.
  - GameplayCue is deliberately excluded in single-player v1; feedback is driven directly by the authoritative Health delegate and GE context.
  - Native Automation suite `PolyQuest.Combat.HitFeedback` previously passed its 23 numbered coverage sections, including preset mapping, monotonic arbitration, real-time tick restoration, exact external-dilation preservation, HitResult validation, multi-modifier and same-Context independent-GE coverage, Team filtering, lethal/death exclusions, and Player/Controller destruction cleanup. The user confirmed PIE visual/audio verification with low-poly blood material `M_Blood_LowPoly`, Niagara system `NS_EnemyBloodImpact`, and hit sounds. The post-closeout lethal-feedback correction updates the regression assertions to cover first-lethal/dead separation; its updated test run remains a user gate. Main fresh review found two P2 boundaries and no remaining source blocker. Authored assets remain local `Content/**` WIP.

- [x] `TODO-02C3L: Four-Directional Small/Big Hit Reactions v1`
  - Completed attacker-relative four-direction presentation selection for Player and Enemy Small/Big reactions. The private non-reflected `FHitReactionFourWayMontageSet` requires a complete Front/Back/Left/Right Montage set; the legacy single `SmallHitReactionMontage`, `BigHitReactionMontage`, and `HitReactionMontage` properties were removed rather than retained as a generic fallback. `FHitReactionFourWayMontageSelector` consumes only target-local `Target -> Attacker` XY, uses four equal cardinal sectors with X-axis priority at exact 45-degree ties, and returns `nullptr` for incomplete, zero/near-zero, NaN, or Inf input.
  - Each Ability validates the same complete-set contract and fails closed before it can create a Montage task. Small remains the non-interrupting in-place overlay route. Big retains grounded full-body Root Motion, its pre-task impact snapshot, cancellation, ledge/falling cleanup, and existing AI behavior; it does not pre-turn the target. Launch remains exclusively owned by `TODO-02C3I` / `TODO-02C3J` and C3L changes no Tags, Context routing, damage, Poise, hit-stop, sound, or blood feedback.
  - `PolyQuest.Combat.HitReaction` now covers cardinals, diagonal ties, both sides of all sector boundaries, XY projection with non-zero Z, zero/near-zero/NaN/Inf directions, and every incomplete set. The user confirmed focused Editor readback/PIE and the combat/reaction Automation matrix; the final post-repair `PolyQuest.Combat.HitReaction` Editor run succeeded. Main defect-first review and an independent Fresh Reviewer found no P0-P2. Eight-direction expansion remains deferred until compatible diagonal assets exist and focused PIE demonstrates that nearest-cardinal presentation is visually inadequate.

- [x] `TODO-03B-5: Bow Hold Locomotion Presentation v1`
  - Completed the mobile Bow presentation split without changing GAS action ownership. `State.Action.Attacking` remains present for the full Draw -> Hold -> Release Ability lifetime and continues to drive the authored `Is Bow Aiming` Bow-locomotion selector; `UBowDrawFireAbility` now owns the existing dynamic `State.Action.Charging` only after a valid `Event.Attack.Bow.DrawReady` transition into Hold and removes it before Release plus every `EndAbility()` route.
  - The authored `ABP_Player_Dungeon` now keeps `Is Bow Aiming = (ResolvedLocomotionMode == Bow && State.Action.Attacking)` for `BS_Bow_Aim_Walk_Run`, while the separate `Is Bow Holding = (ResolvedLocomotionMode == Bow && State.Action.Charging)` controls the UpperBody `AllowBlend` gate. `AllowBlend = NOT (Is Shield Guarding OR Is Bow Holding)`, so Draw and Release retain their UpperBody Montage poses and Hold alone presents the full-body Bow hold while the approved mobile MoveSpeed GameplayEffect still permits movement.
  - `ProjectileLifecycle` and `PlayerMobileBow` Automation cover invalid DrawReady identity, normal Hold release, early release, Charging cleanup, and existing MoveSpeed isolation. The user confirmed the focused Automation/regression matrix and Scene01 PIE. Main's independent defect-first fresh review found no P0-P2 or actionable P3. The user's adjacent CameraBoom tuning (`CameraLagSpeed 8.0f`, `CameraLagMaxDistance 0.0f`) is recorded as independent presentation work; authored AnimBP/GA/Montage assets remain local `Content/**` WIP and are excluded from the source/docs commit.

- [x] `TODO-02C3M: Parry Success Impact Feedback v1`
  - Completed one explicit Player Parry-success presentation path at the existing `FMeleeHitResolver -> APlayerCharacter::TryResolveIncomingDefense -> UPlayerParryAbility::TryParryMeleeHit` boundary. It fires only after the current Parry window/front-arc success gate returns true; Guard, failed or invalid Parry, ordinary Health damage, Poise-only effects, and non-Parry contacts remain silent. The existing contact-consumption and optional attacker-counter semantics remain intact, including the case where a missing attacker ASC skips only the counter.
  - Reused `APolyQuestPlayerController::RequestCombatImpactHitStop()` for the approved `0.05s / 0.03` freeze and the Player's existing Big camera-shake lifecycle, plus an optional Parry-owned `USoundBase` at the validated contact point or Player location fallback. Projectile defense explicitly passes `bAllowParry = false`; melee passes `true`.
  - Kept the feature single-player and native C++/GAS-owned: no new Gameplay Tags, Input routes, replication, generic feedback framework, or GameplayCue. Existing `DeliveredTargets` remains the Trace Window de-duplication owner; the C3M suite covers resolver-level feedback while `PolyQuest.Melee.MultiTraceSource` covers repeated-window delivery.
  - The user confirmed `PolyQuest.Combat.ParrySuccessFeedback`, the relevant regression Automation matrix, Development Editor compilation, and focused Scene01 PIE. Main's independent defect-first fresh review found no P0-P2 source defect; the approved C3K preset tuning was frozen in the same closeout commit.

- [x] `TODO-02C3N: Guard Success And Player Hit Audio Feedback v1`
  - Completed the two optional native audio routes at their existing ownership boundaries. `UPlayerGuardAbility::TryGuardMeleeHit` now carries the resolver `FHitResult` through the Player defense entry and dispatches `GuardSuccessSound` once only after the Guard-Stamina GameplayEffect succeeds; valid contact points are used when they belong to the Player, otherwise the finite Player location is used. Projectile Guard and a Guard-break absorption count as one successful Guard contact, while failed/invalid Guard and a missing sound remain silent.
  - `APlayerCharacter::OnHealthAttributeChanged` now dispatches the optional `ReceivedHitSound` once for the first Health modifier of each GameplayEffect Spec, only for authoritative nonlethal decreases from an exact `Team.Enemy` instigator. It runs after the existing Camera Shake and before Stunned/invalid-reaction suppression, uses a valid Player HitResult point or finite actor-location fallback, and leaves Overlay, Camera Shake, reaction, Guard, Parry, and Enemy `ImpactSound` ownership unchanged.
  - The stage remains direct and single-player: no GameplayCue, audio-bus/mixer framework, variation/pooling, replication, new damage route, or asset migration was added. Sound assets and assignments remain user-owned local `Content/**` WIP. Test-only dispatch seams stay behind `WITH_DEV_AUTOMATION_TESTS`.
  - `PolyQuest.Combat.DefenseAudio` covers Guard success/failure, projectile and Guard-break absorption, location fallbacks including NaN/Inf, hostile-team filtering, Small/Big/Launch nonlethal Player damage, Stunned/invalid-reaction continuation, lethal/heal/Poise/direct-write exclusions, same-Spec de-duplication, independent Specs, and teardown. The user confirmed the Automation test as `Success` and focused Scene01 PIE; the latest log's missing HUD, missing Stamina-delay fixture, absent Guard-break receiver, and invalid reaction-tag messages are expected fixture/negative-path signals. No separate Development Editor compile result is claimed here.
  - Main's single defect-first fresh review found no P0-P2 or remaining actionable P3 after moving test counters behind final validity checks and removing the unused test include. `Config/Automation/Presets/1.json` remains a user-owned aggregate test preset and is intentionally excluded from this focused commit; no new C3N debt was accepted.
  - The later UE Unity-build helper collision in the C3N Automation translation unit was repaired by giving its cleanup/tick helpers file-specific names (`fdf8fa2`). The user reconfirmed the focused Automation and PIE route after that test-only fix; no runtime contract changed.

- [x] `TODO-02B3: Lock-On Retention Hysteresis v1`
  - Completed the fixed-world Lock-On retention refinement. Acquisition, cycle candidate construction, and death-handoff scans keep a strict `0%` viewport margin; only the already-owned target projection in the normal validation path uses one fixed per-axis `15%` expansion with strict outer boundaries. The Player anchor remains strict, and the helper fails closed for non-finite coordinates, invalid viewports/margins, camera/controller/projection failure, and the existing target-qualification failures.
  - A target that is valid only inside the retention band remains highlighted, continues to satisfy `ResolveValidLockedTarget()` and the existing facing consumers, and makes both directions of Cycle a strict no-op. Exiting the `15%` band still clears without replacement; confirmed target death still performs exactly one strict clockwise handoff. No timer grace, second cap, auto-retarget, global projectile overscan, new Tag/Input/Config field, or alternate damage path was added.
  - Bow's existing `6%` automatic Target Assist boundary and Release-time projectile snapshot remain independent. `PlayerLockOnAutomationTests.cpp` covers margin mathematics, strict acquire/cycle, retention-only no-op, boundary clear, lifecycle failures, and death handoff; `ProjectileLifecycleAutomationTests.cpp` covers a retained `X=-150` lock outside both strict and Bow-6% bounds plus a clearing `X=-300` case.
  - The user confirmed the relevant Lock-On/Projectile Automation suites and focused `Scene01` PIE. This record has no separate Development Editor compile log or new Editor asset readback and does not infer either from those results. Main's single defect-first fresh review found no P0/P1/P2 source blocker. The focused closeout includes only the six approved Source/test paths and documentation; `Config/Automation/Presets/1.json`, all `Content/**`, and other WIP remain excluded.

- [ ] `TODO-03A3E: World Pickup Interaction Prompt v1`
  - Add one local, event-driven interaction prompt for the existing `AWorldWeaponPickup` overlap, `CanInteract`, and nearest-candidate route. The initial presentation is a clear authored key hint (for example, `E 拾取`) while a living Player has one valid candidate; it refreshes when the candidate changes and hides on leaving range, invalidation, transaction completion/failure, death, destruction, possession change, and teardown.
  - The prompt and the existing `E` interaction must consume the same Player-owned candidate snapshot/resolver, so the object named by the UI is exactly the object passed to `TryEquipWorldPickup`; do not create a UI-only distance query or a second interaction authority.
  - Keep `UWeaponEquipmentComponent::TryEquipWorldPickup` as the only equipment mutation and `AWorldWeaponPickup::CanInteract` as the validity gate. The prompt is presentation-only: no auto-pickup, inventory, generic interactable interface, second collision/damage route, or interaction-time gameplay state is introduced. The old Test `WBP_InteractionPrompt` may be used only as a visual/reference aid; it is not copied or treated as a PolyQuest asset dependency.
  - The user owns creation/configuration of the PolyQuest UMG widget and any key/icon styling. Editor readback and focused Scene01 PIE must cover enter/leave, multiple-pickup nearest arbitration, successful and refused swaps, and cleanup after death/destroy/re-possession. Native tests should cover candidate/prompt state where the existing source boundary permits; authored UI remains local `Content/**` WIP unless a stable closure is explicitly approved.

- [ ] `TODO-03A7: Selected Melee Motion-Warp Contact Assist v1`
  - After an Editor asset audit, add Motion Warping only to explicitly named melee attacks whose selected Montage has verified Root Motion and a matching Motion Warping window/target name. Start with the smallest useful Player attack set; extend to an ordinary Enemy melee attack only when a concrete authored leap/contact asset proves the same need in this stage.
  - The active GAS Ability remains the owner of target validity, a release/start snapshot, distance/angle/grounding limits, and every cleanup path. The current valid lock is the default target source; without one, the attack stays on its existing path unless this stage explicitly proves a bounded attack-local query. A valid snapshot writes one bounded Warp Target and never continuously chases, re-queries, or changes damage delivery. Invalid target, excessive correction, missing component/window, cancellation, death, montage failure, or teardown fails closed to the existing attack/root-motion path.
  - Add the `MotionWarping` module dependency only if the selected native integration actually requires it. Do not warp locomotion, hit reactions, projectiles, or every attack; do not copy the old Test config/framework. If the chosen production animations lack valid Root Motion/warp windows, close this stage with an evidence-backed no-adoption decision rather than inventing assets or code; otherwise completion requires the selected C++/asset contract, lifecycle Automation, manual compile, and focused PIE contact/readability checks.

- [ ] `TODO-03C: Ranged Enemy v1`
  - Add one StateTree-driven ranged enemy only after the completed `TODO-02B3` and the player-combat-experience-first pre-ranged closure sequence `TODO-03A3E -> TODO-07B4 -> TODO-03A7 -> TODO-05A -> TODO-05B -> TODO-05C` have passed their own adoption/compile/Automation/PIE gates (or have an explicit evidence-backed no-adoption decision for unavailable Motion Warping assets), in addition to the existing `TODO-03B-3` -> `TODO-03B-4` -> `TODO-03B-5`, `TODO-03H2`, `TODO-03H3`, `TODO-03AI`, `TODO-03AI1`, `TODO-03AI2`, `TODO-03AI3`, `TODO-03AI3B`, `TODO-03H1`, the completed `TODO-03A6C`, the completed `TODO-02C3E` -> `TODO-02C3F` -> `TODO-02C3G` -> `TODO-02C3I` -> `TODO-02C3J` -> `TODO-02C3K` -> `TODO-02C3L` -> `TODO-02C3M` -> `TODO-02C3N` reaction/presentation sequence, the user-priority `TODO-02C4` Enemy death-ragdoll gate, `TODO-03A3C` -> `TODO-03A3D`, and the `TODO-03H4` -> `TODO-03H4A` -> `TODO-03H4B` gates. The completed `TODO-03A5` Attack Set pattern and `TODO-03A6D` -> `TODO-07B3` bilateral delivery/projectile-presentation sequence remain supporting prerequisites rather than a second ranged-AI framework.
  - Reuse the 03B projectile runtime, immutable projectile definition, and projectile GAS delivery path; add only the enemy ranged attack Profile/Ability/StateTree timing and AI-owned target snapshot. Enemy projectiles are straight or otherwise explicitly authored non-homing by default, so 03C does not silently add a global lock-on or player-style target-assist system.

- [ ] `TODO-03D: Checkpoint, Rest, Death Reload, And Save Foundation v1`
  - Establish new PolyQuest checkpoint/rest, death reload, and SaveGame ownership before durable pickups or rewards. Define stable Save IDs, a single transaction owner, failed-save behavior, and a reload-validation fixture.
  - Establish the player terminal-action contract at the same time: when player death/reload becomes real, every active player Ability must be canceled through its own `EndAbility()` cleanup. `State.Status.Dead` is currently an activation gate for player actions, not a terminal cancellation route for an already-active Bow or other action.
  - Do not migrate old SaveGame identifiers, old world actor names, or raw actor references.

- [ ] `TODO-03D1: Rest-Site Combat Action Preparation And Loadout Persistence v1`
  - Build on the proven `TODO-03D` rest site: at a save/checkpoint interaction, the player may arrange `1-4` from compatible MainHand/OffHand ability classes registered by the equipped weapon and shield definitions instead of changing them during combat or through a general inventory screen. This stage also owns the real exclusive-selection rules deferred from the v1 candidate union. `UWeaponEquipmentComponent` remains the runtime owner of the current prepared layout; SaveGame stores only a serializable selection snapshot, and every equip/load route revalidates it against the current candidate union before retaining or reseeding entries.
  - A TwoHanded Bow MainHand keeps a distinct `1-4` projectile-selection layout sourced from its compatible projectile definitions; it does not convert Fire/Ice Arrow selection into melee CombatAction activation. Filter unavailable choices before committing, restore only valid prepared entries after a load, and define the disposition when the currently equipped weapon or item cannot use a saved selection.
  - This is focused preparation UI and persistence, not a generic backpack, skill-tree, crafting, or mid-combat loadout system.

- [ ] `TODO-03E: Potion And Consumable Loop v1`
  - Add the first healing consumable after ownership, save, action interruption, and cancellation contracts exist.
  - Preserve notify-timed healing and interruption semantics where the chosen animation needs them; do not add a backpack or general consumable framework without a player-loop need.

- [ ] `TODO-03F: World Drops, Fixed Rewards, And Reward Persistence v1`
  - Rebuild transient enemy currency drops separately from persistent fixed-item rewards and one-time elite defeat state.
  - Equipment direct-pickup/drop-swap is deliberately owned by `TODO-03A3`; this stage owns durable rewards and reward persistence. Each durable reward requires a stable authored ID, one transaction owner, failed-save behavior, and a reload-validation fixture.

- [ ] `TODO-04A: Encounter, Fog Gate, And Clear Persistence v1`
  - Build a new encounter clear-persistence loop, fog boundary, and participant ownership after the new save/checkpoint boundary is verified.
  - Fixed rewards remain the independent `TODO-03F` special-enemy contract; an encounter does not create a second reward or one-time-defeat system.
  - Add multi-enemy attack coordination only if the first real encounter proves that ordinary StateTree/GAS spacing cannot preserve readable combat.

- [ ] `TODO-04B: Persistence And Encounter Health Review v1`
  - Audit the completed player-loop and encounter path after the pre-`TODO-03C` punish gate: stable Save IDs, transaction ownership, failed-save behavior, reload/re-entry, reward delivery, encounter cleanup, fog-gate state, and persistence boundaries between actors, subsystems, and authored data. This review does not reorder or own the already-scoped `TODO-05A` -> `TODO-05B` -> `TODO-05C` abilities.
  - Do not add inventory, encounter, or combat features. Fix confirmed data-integrity or lifecycle blockers; record every remaining accepted risk under its owning milestone or `Known Risks And Validation Debt` with evidence and a closure trigger.
  - This is a persistence-safety review, not a broad asset-cleanup pass. Any removal still requires a proven zero-reference or replacement path.

### Punish And Level Completion

- The following punish stages are promoted into the pre-`TODO-03C` melee-closure gate. Their numerical IDs remain in the Punish family so existing references stay stable; implementation order is `TODO-05A -> TODO-05B -> TODO-05C`.

- [ ] `TODO-05A: Front Critical v1`
  - Rebuild a front-punish ability after `TODO-03A7` proves the selected Root Motion/Motion-Warp boundary. It establishes the narrow target reservation, front-geometry, alignment, damage-once, and teardown primitives that the later Backstab stage may reuse.
  - Do not add Backstab, a generic finisher framework, or Boss-specific critical behavior in this stage.

- [ ] `TODO-05B: Backstab v1`
  - Rebuild Backstab as a distinct Player ability with its own rear-geometry and target-validity contract: the attacker's bounded horizontal position relative to the Enemy's facing must be behind the target at the authoritative activation snapshot. It may reuse only the proven reservation, alignment, hit, and cleanup primitives from `TODO-05A`; it must not reinterpret an ordinary Small/Big reaction or Stunned tag as a backstab.
  - A valid target is aligned once (using `TODO-03A7` only when the authored Montage qualifies), receives one existing damage GameplayEffect at the authored hit event, and releases its reservation through natural completion, cancellation, target death/range loss, montage failure, and teardown. No continuous target chase, second damage route, or generic finisher dispatcher is added.

- [ ] `TODO-05C: Stagger Execution And Backstab v1`
  - During the existing Enemy `State.Status.Stunned` / Stance Break-owned legal window, expose two explicit player actions: a front Execution and a rear Stagger Backstab. They are separate GAS ability/event contracts with separate authored Montages; shared reservation/alignment/cleanup helpers are allowed, but no mode enum, universal `FinisherAbility`, or automatic execution is introduced.
  - A successful action must reserve one living Enemy, validate range/relative geometry and the current Stunned owner, align once (optionally through the bounded Motion Warping contract), and apply one authored damage GameplayEffect through the existing GAS delivery boundary at its semantic hit event (a dedicated lethal magnitude is allowed; direct Health writes are not). A failed preflight or interrupted Montage leaves the Stance Break recovery path intact.
  - Stance Break, target death, Player death/cancel, range loss, Montage interruption/failure, and teardown must converge through explicit cleanup that releases the reservation and never restores stale movement/Poise/Dead state. The stage must define deterministic front-vs-rear eligibility and prove exactly-once damage/death ownership in Automation plus focused PIE.

- [ ] `TODO-06A: First Stylized Level Route And Integration v1`
  - Integrate validated systems into a deliberate level route: checkpoint, ordinary combat space, elite/fixed-reward branch, encounter, fog gate, Boss approach, and focused route validation.
  - This stage proves level composition and existing-system integration. It does not introduce the first Boss phase, completion state, or a second encounter/reward implementation.

- [ ] `TODO-06B: First Boss Phase And Completion v1`
  - Add one Boss StateTree/combat profile, Phase 1 -> transition -> Phase 2 intent, authored ability patterns, completion persistence, reward handoff, and return/continue behavior using the level route from `TODO-06A`.
  - Boss phase selection remains StateTree intent plus GAS abilities; a Boss does not justify a separate AI framework merely because it has no patrol.

- [ ] `TODO-06C: Route Health And Lean Review v1`
  - Before polish, exercise the integrated checkpoint-to-completion route and audit cross-system ownership, dead references, temporary test fixtures, duplicate or obsolete configuration/assets, teardown after interruption/death/reload, and drift between implementation, authored data, and project documentation.
  - Add no new player loop or combat mechanic. Fix confirmed route blockers in this stage; put any remaining accepted risk in its concrete owning milestone or `Known Risks And Validation Debt` with evidence and a release-gate closure condition.
  - Lean removal remains evidence-led: source/config inspection plus Asset Registry/Reference Viewer must show zero referencers or an explicit replacement. Keep high-frequency authored WIP outside a cleanup commit unless its stable dependency closure is separately approved.

- [ ] `TODO-07A2: Extended HUD And Combat Observability v1`
  - Builds on `TODO-07A1` after the core route is stable. Add only the additional HUD and debug/observability surfaces needed to make validated Poise, target, damage, and later route state legible.
  - This is not a vehicle for new combat mechanics or a generic UI framework; it must retain `TODO-07A1`'s ASC-backed read-only data ownership and cleanup rules.
  - `TODO-07A1` deliberately keeps living Enemy bars always visible. After `TODO-02B1` supplies a valid lock state, this milestone owns any idle-hidden policy: show on valid damage and while locked, define any timeout/fade behavior, and verify cleanup on death, destruction, and lock clear. Do not pre-author that visibility policy in 07A1 or infer it from Widget visibility alone.

- [ ] `TODO-07B: Feedback, Presentation Retune, And Demo Polish v1`
  - Add the VFX/SFX, camera, navigation readability, and presentation feedback needed to make the validated loop readable and presentable.
  - Its first four bounded slices are delivered: `TODO-07B1` combat-hit feedback, `TODO-07B1A` reaction-tier Player received-hit camera shake, `TODO-07B2` melee weapon trails, and `TODO-07B3` data-driven projectile flight presentation. The remaining umbrella retains later presentation retuning and demo-polish work; the attacker-impact slice `TODO-07B4` is registered below as a deliberate pre-`TODO-03C` player-experience gate and does not reopen frozen runtime ownership contracts.
  - [ ] `TODO-07B4: Attack Impact Camera Shake And Reaction-Tier Mapping v1`
    - Address the current feel gap where a successful Player attack already produces the existing hit-stop but the attacking Player camera has no impact shake. At the authoritative successful hostile-contact boundary, select exactly one authored Player Camera Shake from the existing `Data.Reaction.Small`, `Data.Reaction.Big`, and `Data.Reaction.Launch` tiers, preserving the one-to-one Small/Big/Launch mapping and the existing target-side reaction classification.
    - Reuse the established melee Trace/Resolver and projectile delivery completion signals; trigger at most once per accepted damaging contact and never create a second damage or hit-resolution path. Misses, invalid or dead targets, Guard/Parry absorption, Poise-only/non-damaging effects, death, cancellation, and teardown remain no-shake paths unless a separately accepted contract changes them. Existing hit-stop timing and ownership remain intact.
    - Prerequisites are the stable `TODO-02C3E` reaction-tier classifier, current `TODO-02C3K` impact/hit-stop boundary, `TODO-07B1`/`TODO-07B1A` camera-shake lifecycle conventions, and a readback-confirmed authored set of three Camera Shake classes. The user owns Camera Shake assets and Blueprint/ability assignments; `Content/**` remains local WIP by default.
    - Success requires native Automation for exact tier mapping, exactly-once dispatch, hit-stop coexistence, miss/defense/teardown exclusion, and weak-reference cleanup; manual `PolyQuestEditor` compilation, Editor readback, and focused `Scene01` PIE must show readable Small/Big/Launch attacker feedback without changing damage, reaction, or camera ownership. It is now scheduled before `TODO-03C` for player-experience closure, while remaining technically independent of the `TODO-03A7 -> TODO-05A -> TODO-05B -> TODO-05C` punish chain.
    - Non-goals: Enemy-camera shake, global camera transforms, GameplayCue or feedback-dispatcher frameworks, new Reaction Tags, audio/Niagara work, replication, camera-system rewrite, damage/Poise changes, or broad shake tuning across unrelated actions.
  - `TODO-03A6C` has completed the MainHand-only ordinary locomotion, durable OffHand Shield-presentation, and exact active Shield Guard state split. It removed the old SwordShield composition model rather than generalizing it into weapon-pair variants; `TODO-03A6B` remains the authored full-body Shield Guard presentation closure. Later 07B work may retune approved presentation assets only with focused evidence and must not reopen this state-source contract.
  - **Cross-Weapon Locomotion Cadence And Foot-Slide Mitigation:** a user-authored shared Stride Warping pass on the pure base-locomotion pose has passed focused PIE validation and remains local `Content/**` WIP. After the completed `TODO-01I` exhaustion and scheduled `TODO-03B-4` mobile-Bow movement routes settle, compare each BlendSpace/sample cadence against the authoritative gameplay `MoveSpeed` first and calibrate incompatible samples before treating that pass as a stable closure. Keep it excluded for falling, Root Motion, and action/reaction ownership, and verify its graph source remains valid without changing ordinary locomotion's CharacterMovement ownership. Existing foot IK addresses terrain contact, not horizontal cadence mismatch; Motion Warping is not the solution for in-place locomotion. Close this debt only after focused visual validation across the affected weapon families and Guard.
  - **Deferred Guard Release Presentation:** `AM_PlayerGuard` authors `Begin -> Loop -> End`, but D1's `UPlayerGuardAbility` treats Released/Canceled as immediate teardown and calls `Montage_Stop(0.0f)`, so the authored `End` section is intentionally not a runtime lowering-sword exit. The accepted D1 fixture proves only `Begin -> Loop` while RMB is held. Revisit this only when focused visual validation shows that the immediate release blend is visibly inadequate: preserve immediate removal of guard gameplay, Stamina effects, and input-resume eligibility; then define a section handoff that cannot leave stale Guard tags/tasks or delay Dodge, attack, death, or interruption cleanup. Close this debt only after focused compile and PIE coverage for Released, Canceled, attack/Dodge interruption, Guard Break, death, and montage failure.
  - `TODO-02C3E` owns the first Small Reaction and its source-category contract. This stage may only retune that accepted presentation after focused visual evidence; it must not introduce a second hit-reaction route, alter the no-interrupt/no-displacement rule, or infer reaction strength from visual effects.
  - After the core player-combat framework and selected weapon animation set are stable, run one focused combat-presentation retune over provisional Montages: final source-animation selection, per-weapon Montage composition, root-motion feel, and final authored `TODO-02F` RateWindow values. Do not return to duplicate/cropped Sequence or Section-based speed workarounds.
  - Re-author `AttackTraceWindow`, Combo Input/Branch, Dodge Cancel, and Dodge Invulnerability timing against the final motions while retaining the proven one-buffer, Branch consumption, one-hit-per-entry, and ability-owned gameplay contracts.
  - Reuse the proven `TODO-02F` rate lifecycle rather than creating another playback-rate system. Any later request for overlapping, stacked, or cross-Montage rate behavior requires a separate accepted stage.
  - Re-run focused PIE visual and teardown checks for contact readability, root-motion continuity, hit/cancel timing, and stale tags/tasks after interruption.

- [ ] `TODO-07C: Full Route Regression v1`
  - Establish the repeatable manual validation matrix for New Game, Continue, checkpoint/rest, ordinary enemy reset, cleared encounter restore, equipment and consumables, drops/fixed rewards, player/enemy ranged combat, punish abilities, Boss completion, and completion Continue.
  - Fix regressions in previously completed systems only; this stage does not defer their original implementation or add new gameplay systems.

## Accepted Technical Direction

- **Test migration boundary:** migrate verified player-facing behavior and acceptance cases, not the old FSM, `EActionState`, save identifiers, or authored asset topology.
- **Gameplay authority:** GAS owns ability activation, costs, blocking/cancellation tags, combat state, damage, poise, hit reaction, and death. Animation owns timing and presentation; it is not a second gameplay state source.
- **Input intent:** Physical controls express stable player intent; the active weapon/loadout and GAS determine the concrete ability. Mapping Contexts change for control modes, not for profession labels or weapon inventory alone.
- **Camera and facing:** PolyQuest uses an elevated oblique-perspective follow camera, not a Souls-style over-the-shoulder orbit. Movement remains camera-relative. Unlocked ordinary locomotion faces movement; a valid local screen-space lock continuously faces eligible grounded locomotion/Guard/non-Root-Motion Parry at `800 degrees/second`, while Sprint preserves the lock but restores free movement-facing. Completed `TODO-02B3` keeps acquisition and cycling strict to the viewport while applying a fixed, bounded 15% edge hysteresis only when retaining an already-owned target; a retained target outside strict candidates makes cycle input a no-op rather than clearing or auto-retargeting. Active attack, Dodge, hit reaction, Bow, Root Motion, death, and Stunned retain yaw ownership. Confirmed locked-target death alone hands off once clockwise; a target beyond retention or any other invalidation clears. Lock-on adds no camera orbit, camera yaw ownership, free look, or global projectile-targeting role; Bow and future projectile spells retain the weapon-family target-assist contract in `TODO-03B`.
- **Data ownership:** a PolyQuest character/ability manifest composes focused authored assets. Combo entries, action settings, enemy attack profiles, and reaction data are modular tuning inputs; none stores mutable gameplay state or replaces ability/effect ownership.
- **Authored tuning source of truth:** balance values such as an authored MoveSpeed GameplayEffect multiplier live in the current Editor asset and are validated by Editor readback/PIE, not duplicated as durable README, architecture, or Roadmap constants. Native no-Content fixtures may use controlled numbers to prove lifecycle and synchronization, but their numbers do not declare production tuning.
- **Weapon presentation axes (completed `TODO-03A6C`):** MainHand base locomotion and durable OffHand presentation are separate facts. A Shield-equipped state derives only from the current committed OffHand definition's `bProvidesShieldPresentation`; active Shield Guard additionally requires exact `State.Action.Guarding.Shield`. Neither `Ability.Defense.Guard.Shield` nor generic `State.Action.Guarding` is a valid Shield-equipped signal. Bow retains its independent aim-state route. This does not redesign equipment transactions or authorize unbounded weapon-pair BlendSpace variants.
- **Equipment evolution:** `TODO-03A` remains a completed player-only single-StaticMesh melee foundation, not a generic weapon framework. The accepted expansion starts with an immutable `UWeaponDefinition` base while retaining `UMeleeWeaponDefinition` as the compatible melee subtype, then uses per-weapon definitions such as `DA_Weapon_Sword`, optional OffHand Shield definitions, TwoHanded MainHand definitions such as Bow/Staff/Greatsword, direct per-weapon ability-class grants, and Enemy Attack Sets; shared Sword/Katana behavior is expressed by shared ability and combo assets, not a mandatory Player Combat Style DataAsset. MainHand/OffHand composition with TwoHanded exclusivity, full Unarmed fallback, direct drop-swap, Shield defense overrides, ordinary weighted enemy attacks, static-enemy shared socket geometry, and the bounded defense-outcome extension are dependency-ordered `TODO-03A1` through `TODO-03A5B`, then `TODO-03AI` -> `TODO-03AI1` -> `TODO-03H1`, plus `TODO-03A4B` when partial Guard is first required (`TODO-03A1` has delivered the domain contract). A Blueprint pickup/preset is presentation and level authoring, not an alternate combat authority or collision-damage path: a generic world-pickup Blueprint may reference an authored weapon definition with optional `BP_Pickup_Sword`/`BP_Pickup_Axe` preset children, `BP_Player_Sword`/`BP_Enemy_Sword` gameplay classes are forbidden, and any authoring preview that visualizes markers/sweep may not use a static collision box or overlap events as melee damage truth.
- **Mode-specific quick slots:** physical `1-4` intents are stable, but their semantic selection is equipment-specific: prepared compatible ability classes registered by one-handed MainHand/OffHand equipment in close combat, and compatible projectile definitions when a TwoHanded Bow is the active MainHand. The Bow directly uses LMB draw/charge/fire plus its authored RMB Guard/Q Parry fallback; the current Guard is full absorption, while an intentional partial-block weapon is gated by `TODO-03A4B`. Rest-site preparation and persistence belongs to `TODO-03D1`; no equipment item or Blueprint grants an implicit permanent physical-slot ownership.
- **Top-down ranged/magic targeting:** Bow and future projectile spells use action-facing for launch presentation, not free vertical mouse aim. The active Ability may acquire one valid hostile target and pass a 3D target snapshot to the spawned projectile; that projectile owns homing after launch and its invalid/lost-target fallback. Held Bow/Staff meshes remain equipped display components, while only independently travelling arrows, bolts, or persistent area effects become Actors. Target selection, visibility/elevation policy, and homing limits are weapon-family stage contracts, not persistent state on a WeaponDefinition or a general lock-on prerequisite.
- **Projectile reuse boundary:** Player Bow and future ranged enemies share the narrow projectile runtime/data and projectile-specific GAS delivery contract, not their input, target acquisition, AI, or equipment ownership. Avoid an abstract player/enemy projectile inheritance tree until a second projectile lifecycle demonstrates a real subtype boundary; arrows/bolts may share the concrete travelling-projectile path, while beams and persistent area effects remain separate actors/effects.
- **Combo contract:** one active light-attack ability owns at most one buffered LMB continuation. `ComboWindow` accepts it, `ComboBranchWindow` consumes early input or retries late input, and `CancelWindow` exposes only a scoped immediate-cancel opportunity for preflight-valid actions.
- **Montage lifetime:** a successor combo entry must explicitly reject stale completion/interruption events from a prior entry. Natural completion, cancellation, hit/death teardown, invalid timing events, and asset failure converge through one ability cleanup path.
- **Combat timing:** weapon collision, potion heal, projectile release, parry active frames, hyper armor, combo windows, and cancellation windows are semantic animation events. Their receiver validates the active ability and current target/context before applying gameplay. Light, Charged, and Sprint Attack use shared `AttackTraceWindow` Begin/End events; the retired action-specific one-shot hit events are not a second damage path.
- **Enemy AI:** StateTree is the default high-level behavior brain for both ordinary enemies and Bosses. It owns patrol, alert, chase, combat intent, return-home, phase selection, and high-level transitions; it requests GAS abilities rather than directly applying combat mutations.
- **Bosses:** Bosses use a separate StateTree and combat data profile, not a different AI framework merely because they do not patrol. Encounter, fog-gate, persistence, and rewards remain outside the Boss StateTree.
- **No parallel FSM:** do not retain an enemy HFSM or `EEnemyState` as a competing runtime truth beside StateTree and GAS tags.
- **Behavior Tree escalation gate:** do not introduce Behavior Trees unless concrete production behavior requires deeply nested reactive arbitration, dynamic subtrees, or broad concurrent services that remain unclear and untestable in StateTree.
- **External resources:** `Content/Assets/` is a raw source reservoir. A resource becomes an imported product integration only when an approved stage names it and validates its exact Skeleton, socket, animation, or gameplay surface.

## Recommendations

- **Behavior Trees:** keep Behavior Tree tooling available but unadopted. Re-evaluate it only at the documented escalation gate; do not split ordinary enemies and Bosses across AI frameworks preemptively.
- **Lock-On retention:** keep acquisition/cycle strict and use hysteresis only for an already-owned target. `TODO-02B3` v1 freezes the per-axis margin at 15%; focused PIE validates the boundary and regression behavior but does not silently retune it. Do not turn the margin into global projectile targeting, target prediction, a grace timer, or an automatic retarget rule.
- **Motion Warping:** `TODO-03A7` is the adoption gate. Use the plugin only for explicitly selected Root Motion attacks with a verified Montage window, finite correction limits, and a proven cancel/death/teardown cleanup path; a missing or unsuitable asset is a valid reason to leave the plugin unused. It is not a solution for in-place locomotion, hit reactions, projectiles, or global auto-targeting.
- **Shared Dodge/Sprint key:** `TODO-01C2` establishes one temporal physical-input route: a short release requests Dodge and a `0.15s` hold resolves to Sprint intent. Do not rebind independent legacy `IA_Dodge` and `IA_Sprint` actions to the same key, because their independent Started callbacks would race rather than share the arbiter's release/cancel semantics.
- **Future backpack interaction:** only after a backpack has an accepted ownership, capacity, transaction, and SaveGame contract may a short `E` place a compatible pickup into that backpack while a held `E` preserves the direct-equipment/drop-swap behavior from `TODO-03A3`. It must use one shared input arbiter rather than independent short- and long-press bindings, and is not part of the no-inventory 03A3 implementation.
- **Conditional Enemy Melee Trace lifecycle adoption:** `UMeleeTraceSourceComponent` and `UAbilityTask_MeleeTraceWindow` are already shared; only `UEnemyMeleeAbility`'s local open/close wrapper remains outside `FMeleeTraceWindowLifecycle`. Do not migrate it merely because an additional Enemy weapon DataAsset exists. Re-evaluate when a second Enemy melee Ability/archetype repeats that wrapper, or when a planned Enemy-combat change must touch it, and only after a real Enemy ASC/AI fixture proves AttackProfile damage/Guard-Stamina snapshotting, ordinary open/close, closed-task replacement, synchronous endpoint fail-closed behavior, stale Notify isolation, and terminal Trace/Trail cleanup. Reuse the existing scalar helper then; do not create an Enemy-only helper or change AI/AttackProfile ownership.

## Known Risks And Validation Debt
- `UAbilityTask_PlayMontageAndWait::ExternalCancel()` can end a task without an explicit guarantee that the Montage stops. Static inspection found no current PolyQuest caller. When `TODO-01C` or a later Stun/explicit-interrupt path needs task-level cancellation, it must define montage-stop behavior, converge through `EndAbility()`, and add focused PIE coverage before relying on that path.
- `TODO-01A` deliberately withholds its mutable authoring assets from this commit. A curated stable asset baseline is required before the local fixture can be reproduced from a clean checkout; this is a versioning boundary, not a failure of the local compile/PIE validation.
- `TODO-01B` intentionally skips the sword/sequence Reference Viewer dependency-closure audit by user decision. Its direct stable-asset commit may therefore omit material or Skeleton dependencies and must not be treated as a clean-checkout asset baseline; rerun the closure audit before promoting it as one.
- `TODO-01C1` has accepted keyboard/mouse PIE evidence only. Right Shoulder and Left Trigger mapping behavior and physical controller operation are not verified controller support. Before presenting controller support, adding controller-specific UX, or producing a controller-facing build, read back the intended Mapping Context entries and pass focused hardware validation for every intended controller action; until then, do not claim gamepad support.
- `TODO-01C2` has accepted keyboard Shift Scene01 PIE only. Controller parity was deliberately not authored or tested; before assigning the shared Dodge/Sprint route to a controller input or claiming controller support, read back the Mapping Context and pass focused hardware coverage for threshold release, Canceled input, exhaustion release-gate, and temporary action/airborne blockers.
- Light entry/continuation, Dodge, Sprint Attack, and Parry can commit a Cost before authored Montage startup is confirmed; Charged can commit before its release/resume path is known usable. Dodge can also cancel an eligible prior action after Commit but before its own startup is confirmed. Parry intentionally withholds its cooldown after a failed startup, but the already-committed Cost still shares this atomicity gap. A rare playback failure after valid preflight can therefore consume Stamina, end the Ability, or prematurely interrupt the prior action without an automatic recovery contract. `TODO-02F` established the Rate Window lifecycle but deliberately did not close this risk; no-refund remains a v1 direction rather than a verified atomicity contract. Close it only when a controlled **post-commit** playback-failure matrix covers Light initial/continuation, Dodge, Sprint Attack, Charged release, and Parry, before any networking/prediction or frame-exact Cost-accounting adoption.
- `TODO-01E` has manual threshold validation, including the repaired normal-release-before-delay route, but no deterministic same-frame input/timer injection test. Before changing input-event dispatch order, introducing prediction/networking, or relying on frame-exact charge thresholds, add a deterministic automated fixture or controlled logging harness that exercises both callback orders and verifies exactly one Light or Charged activation.
- **Legacy BladeTraceBase / BladeTraceTip fallback retention (TODO-03H1 canonical debt):** `UMeleeTraceSourceComponent` retains the legacy component-name lookup (`BladeTraceBase` / `BladeTraceTip`) for unconfigured static definition owners because it is currently still referenced by `BP_Enemy_Goblin` and `Scene01`. Closing condition: remove this fallback only after all enemy fixtures have explicitly migrated to `StaticMeleeWeaponDefinition` and Unreal Editor Reference Viewer confirms zero references across all project assets.
- **Player terminal Ability cleanup (`TODO-03D`, P3 from `TODO-03B-1`):** `UBowDrawFireAbility` correctly blocks new activation while `State.Status.Dead` is present, but current player tag handling only clears Sprint; it does not cancel an already active Bow Draw/Release. Player death/reload is not implemented yet, so this is not a 03B-1 blocker. Before adding any player death source or presenting death interruption as supported, `TODO-03D` must establish one terminal route that cancels all active player specs and verify Bow, Light, Charged, Guard, Parry, Dodge, and prepared skills each converge through `EndAbility()` without firing or restoring stale state.
- **Hierarchical Primary-tag positive fixture (conditional P3 from `TODO-03B-1`):** `UBowWeaponDefinition` intentionally uses hierarchical `AbilityTags.HasTag(Ability.Attack.Primary)` so a future concrete child tag remains valid. The current negative automation uses `UDodgeAbility` and does not construct a real `Ability.Attack.Primary.*` child-tag CDO. Before the first production child Primary tag is adopted, add a focused isolated CDO/definition test proving the child tag is accepted while an unrelated sibling remains rejected; do not add a fake production tag merely to close this observation.
- **Fixed Bow viewport overscan (conditional P3 from `TODO-03B-2`):** the current no-lock Release-time target query accepts a fixed 6% screen-edge overscan. This is deliberate current tuning, backed by the `TargetAssist` Automation and Standalone visual route, not an unbounded off-screen lock-on. `TODO-02B1`/`TODO-02B2` keep acquisition/cycle strict, while completed `TODO-02B3` uses 15% only for retaining an already-owned Lock-On target; `TODO-03B-3` may prefer that retained lock and must otherwise preserve B2's 6% no-lock behavior. Before a second projectile type demonstrates a different screen-edge capture need, retain the shared C++ value; at that adoption point, introduce a finite, validated `UProjectileDefinition` field with focused editor/Automation/PIE evidence instead of adding a generic targeting settings system.
- **Position-relative eight-direction Small/Big presentation (`TODO-07B` adoption gate):** Big currently uses one authored local-backward Root Motion Montage. `FHitReactionImpactResolver` now canonically snapshots the finite target-local `Target -> Attacker` actor-center line, so future eight-direction selection must consume that same vector; it must not introduce a second weapon-tangent or ImpactNormal-primary direction source. An attacker directly in front remains a front/back reaction and the accepted Big/Launch spacing intent remains away from that attacker, not a sideways displacement. Close this debt only after eight compatible directional Small/Big presentation assets are authored, a bounded selector contract is accepted, and focused Player/Enemy PIE visual validation proves front, back, left, and right readability without stale reaction state.
- **Enemy Launch abnormal-Poise disposition (`TODO-02C3H` design fork):** current C3H behavior restores a living Enemy's zero Poise through the existing recovery GameplayEffect and emits no Stance Break when its pending Launch recovery ends abnormally. The user-confirmed Automation and PIE evidence establishes this as a deliberate forgiving v1 outcome, not a lifecycle accident. The tradeoff is that interruption can remove the expected post-launch break feedback instead of retaining zero Poise until a later safe-ground ordinary break. Revisit only when focused playtesting shows that this recovery feels unsatisfying or loses expected combat feedback; any change then needs a separately accepted Enemy Launch/Poise refinement slice plus Automation and PIE coverage for interruption, recovery, death, and exactly-once break dispatch.
- **Parry/Trace Window combined assertion (`TODO-02C3M`, P3):** C3M's resolver-level Parry feedback test and the existing `PolyQuest.Melee.MultiTraceSource` `DeliveredTargets` test cover the two halves separately, while the user-confirmed Automation/PIE runs show no runtime symptom. Close this coverage debt only when a focused test drives repeated `UAbilityTask_MeleeTraceWindow` samples into an active Parry and asserts one feedback dispatch; do not add a second global de-duplication owner.
- **Retained legacy SwordShield BlendSpace asset (`TODO-03A6C` Content cleanup boundary):** the user removed the `BS_SwordShield_Walk_Run` branch from `ABP_Player_Dungeon`, and the current native enum/runtime path cannot select it. The `.uasset` remains intentionally untouched in local `Content/**`; without a Reference Viewer zero-referencer readback, deleting it would be an unsupported asset cleanup. Its impact is repository/authoring clutter only, not a current gameplay path. Close this only in a separately approved Content-cleanup slice after Reference Viewer confirms no referencers and the user explicitly approves deletion.

## Deferred TODOs

- **Deferred Sprint Loop Presentation (`TODO-07B`):** `TODO-01F` validates Sprint speed, Stamina, cancellation, and Sprint Jump independently of a dedicated loop. The current locomotion BlendSpace remains intentional temporary presentation while Sprint is active. When the selected locomotion set is stable, retarget and validate an In-Place Dungeon Knight Sprint Loop, let `ABP_Player_Dungeon` read `State.Movement.Sprinting` only for presentation, and run focused PIE checks for cadence, foot sliding, Root Motion isolation, and transitions back to ordinary locomotion. Do not describe a dedicated Sprint Loop as integrated before that work passes.

## Stage Completion Standard

A roadmap stage is complete only after its approved plan, focused source/asset validation, user-owned compilation/PIE evidence where applicable, review, documentation synchronization, and explicit commit approval are complete.

## Pre-Maintenance ARCHITECTURE.md Chronological State Snapshot

## Current Verified State

PolyQuest is a UE 5.8 Windows C++ project created from the Third Person template. The only runtime module is `PolyQuest`.

The game module currently declares these public dependencies:

```text
Core, CoreUObject, Engine, InputCore, EnhancedInput, AIModule,
StateTreeModule, GameplayStateTreeModule, GameplayAbilities,
GameplayTags, GameplayTasks, UMG, Slate
```

The live UE 5.8 editor resolves the GameplayAbilities plugin, and the module links the GAS runtime dependencies. PolyQuest has completed its first single-player GAS foundation: the active player exposes an ASC, an eight-attribute AttributeSet, and a project-owned Gameplay Tag config source.

`TODO-01A` adds the first locally compiled and PIE-verified player ability path: LMB requests a native light-attack ability, the ability commits an authored stamina cost, a Montage Notify emits a semantic Gameplay Event, one pawn sweep finds a target character, and an authored damage GameplayEffect is applied through the target ASC. The local fixture has passed the user-owned recovery, repeated-input, cost, no-target, and target-damage checks.

`TODO-01C` extends that local fixture with a ground-only, camera-relative Root Motion Dodge. A shared Stamina-action lifecycle permits a positive remainder to overdraw to zero, gates later actions through exhaustion, delays periodic recovery after committed actions, and uses semantic NotifyState events for attack cancellation and Dodge invulnerability timing.

`TODO-01D` extends the light-attack fixture with a data-driven two-entry linear combo. The active ability owns one buffered `Input.PrimaryAttack`, identity-filtered semantic animation events, per-entry cost and hit consumption, recovery Dodge cancellation, and one teardown path across both Montages.

`TODO-01E` keeps `Input.PrimaryAttack` as one hold/release route. After an active Combo has first received the input, the no-cost Primary Ability arbitrates short release to Light Attack and a held release to Charged Attack. Charged pauses its Root Motion Montage at a semantic HoldReady event, commits its Stamina cost only on normal release, resumes the same playhead, and consumes one identity-filtered hit event. Primary, Light, Charged, and Dodge own shared movement/jump input-block tags while active.

`TODO-01F` establishes ground Sprint, Stamina-costed Jump, Sprint Jump air speed, and one optional Loadout-owned Sprint Attack. `MoveSpeed` is an Attribute consumed by CharacterMovement; Sprint state is an ASC-owned tag rather than a Player boolean or a transient speed comparison. The user has compiled and PIE-validated the configured Scene01 route, while the dedicated Sprint locomotion loop remains presentation work deferred to `TODO-07B`.

`TODO-01G` health-reviewed the player-combat foundation and hardened the shared Montage lifetime contract: synchronous startup completion must not access state already cleared by `EndAbility()`, while Dodge identity-filters Montage and invulnerability events and removes its effect, delegate, tasks, and active Montage through the same cleanup path.

`TODO-01H` replaces the provisional actor-forward sweep with one native shared melee-delivery contract for Light, Charged, and Sprint Attack. The active Ability retains state, Cost, cancellation, attack-specific data, and active-Montage identity validation; `UAnimNotifyState_AttackTraceWindow` sends Begin/End timing only. `UAbilityTask_MeleeTraceWindow` owns prior/current Blade Base/Tip samples and a per-window set of successfully delivered targets, while `FMeleeHitResolver` rejects self, invalid or equal exact `Team.*` tags, missing target ASCs, and `State.Status.Invulnerable` before applying the authored GameplayEffect through GAS. The fixed `WeaponMesh`, `BladeTraceBase`, and `BladeTraceTip` names remain the fallback whenever no live equipped markers resolve; the first enemy is the only valid authored fixture using that path.

`TODO-02A` establishes the first enemy native endpoint without adding a parallel AI or combat state machine. The Player is an explicit Sight source; the enemy Controller owns perception, current target, focus, home location, and StateTree lifetime; StateTree selects Patrol, Alert, Chase, Combat, and Return intent; and one server-only enemy melee Ability reuses the existing Trace Window and GAS resolver delivery path.

`TODO-02B` establishes a fixed-world elevated oblique Perspective camera rather than an over-the-shoulder orbit. `APlayerCharacter` derives movement and one-time action-facing yaw from the actual CameraBoom world yaw. ASC-owned `State.Action.Attacking` and `State.Action.Dodging` temporarily suppress ordinary movement-facing; root-motion rotation is never overwritten every frame. Native input intentionally leaves Look actions unbound, while their authored pointers remain available for future UI ownership.

`TODO-02B3` completes the Lock-On retention refinement without widening acquisition or cycling. `APlayerCharacter` keeps one local weak target reference; `BuildLockOnCandidates()` and the confirmed-death handoff remain strict-viewport (`0%`), while the existing-target validation projects only the target point with one fixed per-axis `15%` retention margin and keeps the Player anchor strict. A target valid only in that retention band remains highlighted and usable by `ResolveValidLockedTarget()` and existing facing consumers; Cycle is a no-op until the target returns to the strict candidate set. Exiting retention, invalid target state, projection/controller/camera failure, or teardown clears according to the existing contract; confirmed death alone performs one strict clockwise handoff. Bow's independent `6%` automatic Target Assist boundary is unchanged. The user confirmed the relevant Automation and focused Scene01 PIE route; no separate Development Editor compile log or new authored-asset readback is recorded here.

`TODO-02D1` establishes directional Guard and Guard Break through the existing Combat Loadout and shared melee resolver. Guard state, movement/recovery modifiers, front-arc stamina absorption, and Guard Break cleanup remain GAS-owned; the user confirmed the native compile and Scene01 PIE route.

`TODO-02D2` establishes a Q-triggered, Notify-timed Parry that pays its authored Stamina cost at startup, consumes one valid front-arc melee contact, and sends authored `-100` Poise to the attacker through the existing resolver and C3C Stance Break path. The user confirmed the repaired native compile and Scene01 PIE route; authored Parry assets remain local WIP.

`TODO-02D3` establishes one authored enemy Hyper Armor window. A matching enemy attack Montage NotifyState temporarily writes an EnemyMelee-owned loose `State.Status.HyperArmor` tag, which blocks only C3B Charged Interrupt; Health, Poise, Stance Break, and death remain on their existing contracts. The user confirmed `PolyQuestEditor` compilation and the Scene01 PIE route; the Montage asset remains local WIP.

`TODO-02D4` establishes the reflected combat-Notify ownership contract without changing any runtime event semantics. `UAnimNotifyState_ActionDodgeCancelWindow`, `UAnimNotifyState_DodgeInvulnerability`, and `UAnimNotifyState_AttackTraceWindow` remain Shared timing-only types; Player-only timing types are explicitly named `UAnimNotify_PlayerChargedAttackHoldReady`, `UAnimNotifyState_PlayerComboInputWindow`, `UAnimNotifyState_PlayerComboBranchWindow`, and `UAnimNotifyState_PlayerParryWindow`; `UAnimNotifyState_EnemyHyperArmor` remains Enemy-only. All still route their existing Gameplay Event Tags through `SendGameplayEvent` with the source Animation in `OptionalObject`. Ownership names do not rename Tags or imply that Player and Enemy must share a Montage. There are deliberately no `CoreRedirects` or legacy aliases; authored Montage references are migrated in Editor and remain local WIP.

`TODO-03AI2` establishes distance-aware weighted melee selection and dynamic approach: within `EngagementRange`, `UEnemyAttackSet` performs pure weighted selection across all valid attacks without pre-filtering individual attack range; `AEnemyAIController` retains the selected profile and governs dynamic actor-following Approach until reaching `AttackRange` or timing out (`ApproachTimeout`, default `3.0s`); `UEnemyMeleeAbility` consumes the Controller's prepared profile after range verification; and the Combat StateTree topology formalizes `Decision -> Approach -> Attack -> Reposition -> Wait`. Decision preparation is permitted only after attack cooldown expiry; the Controller and StateTree both clear a pending profile during cooldown and fail closed.

`TODO-03AI3` / `TODO-03AI3B` keep all Enemy targeting, facing, and tactical-pace runtime state on `AEnemyAIController`; StateTree continues to select intent rather than own perception or rotation. Active Enemy Root Motion owns yaw: `UpdateControlRotation()` clears Gameplay Focus and returns without Controller-driven ControlRotation or Pawn-yaw writes. When Root Motion ends, a valid `CurrentTarget` restores Gameplay Focus once, then only Pawn yaw moves toward the Controller rotation through `FMath::FixedTurn` at `800 degrees/second`, preserving current Pitch and Roll. The Controller's cooldown Reposition captures the current `MaxWalkSpeed`, applies fixed `300` only for that tactical MoveTo, and restores the captured value on completion, immediate failure, interruption, UnPossess, and death; normal Approach keeps its previous speed. Sight acquisition remains the configured 140-degree cone. After a Player becomes `CurrentTarget`, a negative Sight stimulus only sets Controller-local retained-without-sight state while the target remains valid and within `LoseSightRadius` in 2D and the Enemy remains inside Home leash. Pre-`Super::Tick` revalidation uses the existing idempotent `ClearCurrentTarget(true)` / `TargetLost` route when either condition fails. Positive sight clears only that retention state. `SetCurrentTarget()` requires an owned Pawn, preventing a delayed perception callback after `UnPossess()` from restoring a stale target into the next possession. No Search state, last-known location, hearing/damage sense, generic target memory timer, new Tag, or StateTree topology was introduced.

`TODO-03H1` health-reviews the weapon, melee trace, static enemy geometry, and GAS hit resolver boundaries with zero C++ modifications. `UWeaponEquipmentComponent` remains player-exclusive without generic weapon component abstraction; `AWorldWeaponPickup` manages map interaction and dropped staging without combat logic; enemy characters resolve shared static geometry via `UMeleeTraceSourceComponent -> StaticMeleeWeaponDefinition` with fail-closed protection; and `UAbilityTask_MeleeTraceWindow -> FMeleeHitResolver -> Damage GE` remains the sole melee damage path. The legacy `BladeTraceBase` / `BladeTraceTip` fallback is intentionally retained until all enemies migrate to static socket definitions and Reference Viewer confirms zero references.

`TODO-03H2` confirms the completed combat runtime's current lifecycle and ownership closure: character, controller, equipment, Projectile, AI, hit-reaction, and HUD teardown paths retain their existing single owners and no runtime contract changed. Its sole native change is a `WITH_DEV_AUTOMATION_TESTS` Bow helper that supplies the actual equipment-granted `FGameplayAbilitySpecHandle` to the B3 Release fixture, allowing `GetAbilityLevel()` to resolve the existing level-one Spec without a fixture-only invalid-handle warning. It does not alter Ability activation, Bow Release selection, Projectile launch requests, Homing, locks, Tags, or authored data.

`TODO-03H3` establishes the native Automation fixture startup contract without changing product runtime behavior. `FCombatAutomationFixture` deferred-spawns ordinary Player and passive Enemy actors, injects their test-only setup before `FinishSpawning()`, then dispatches BeginPlay only when the World has not already done so. Player setup supplies an empty legal `UCombatLoadoutDefinition`, a valid owner-mesh unarmed `UMeleeWeaponDefinition` on the read-only Hero `Weapon_R` socket, and an infinite no-op `UTestStaminaRegenGE`; Enemy setup disables auto AI possession and Ragdoll while supplying `UTestPoiseRecoveryGE`. The two narrow actor setup methods and all fixture calls are guarded by `WITH_DEV_AUTOMATION_TESTS`; the GE types remain private test-source classes with no product-runtime callers. No Gameplay Tag, production fallback, authored asset, Config, or log level changes. The ten existing suites retain only explicitly asserted negative warnings for invalid reaction tags, Stance Break fallback, equipment transaction rejection/rollback, and static trace fail-closed paths.

`TODO-01I` makes `APlayerCharacter`, rather than the shared AttributeSet, the one owner of the local Exhaustion lifecycle. Its authority-only Stamina delegate creates one loose `State.Status.Exhausted` contribution, one exact Infinite `MoveSpeed x0.7` GameplayEffect handle, and one three-second timer. Death-tag receipt, EndPlay, ASC rebinding, and eligible recovery converge through the same idempotent cleanup, so a prior ASC or another system's loose-tag contribution is not removed. `UStaminaActionAbility` still delays recovery after its committed actions by default; `UJumpAbility` deliberately retains valid authored Cost/Delay references while bypassing only that shared positive-Stamina gate and the delay application, making zero-cost Jump a movement exception rather than another Exhaustion owner.

`TODO-07B1` / `TODO-07B1A` add presentation-only feedback to the existing authoritative Health delegates. `ABaseCharacter` owns the global Mesh Overlay timer/cache lifecycle; Player and Enemy request it only for a real nonlethal Health GameplayEffect decrease before their respective reaction-suppression branches. `APlayerCharacter` alone selects the authored local Small/Big/Launch Camera Shake from the existing fail-closed Reaction Tier; no-tag and invalid multi-tier damage remain Overlay-only. It tracks only the exact started instance and originating `PlayerCameraManager` through weak references, so same-tier hits restart the authored single instance and a tier change stops only the prior B1A instance. No camera transform, Player movement, Gameplay Tag, damage route, or Enemy camera behavior changes.

`TODO-02C3E` establishes fail-closed Damage-GE-authored Hit Reaction Tier classification (`Data.Reaction.Small`, `Data.Reaction.Big`, `Data.Reaction.Launch`) and ships the first non-interrupting Player and Enemy Small reaction. `FHitReactionClassifier` provides pure, context-free tag classification; `APlayerCharacter` and `AEnemyCharacter` bind authoritative Health attribute delegates to dispatch target-side reaction events; `UPlayerSmallHitReactionAbility` and `UEnemySmallHitReactionAbility` provide native montage playback on `ReactionOverlayGroup.ReactionOverlay` without stopping movement or cancelling active actions; and `UEnemyHitReactionAbility` re-maps to Enemy Big.

`TODO-02C3I` establishes one-time Launch-facing ownership without changing the existing launch route: before Takeoff Montage activation, both Player and Enemy freeze the target-local `Target -> Attacker` direction plus pre-turn Actor Yaw. At the identity-validated Commit Notify, `FHitReactionImpactResolver` derives both attacker-facing Actor Yaw and attacker-away CharacterMovement velocity from that same pair; each Ability preserves Pitch/Roll, writes Yaw once, then launches. Invalid input fails closed without a transform write, and all exit paths clear the snapshots. Player HitReacting remains the locomotion/lock yaw gate. For Enemy, Root Motion retains first yaw ownership; otherwise only an active `Ability.Reaction.Enemy.Launch` Spec makes `AEnemyAIController` preserve Focus and skip Controller yaw writes, deliberately leaving ordinary Big reaction behavior unchanged. The user confirmed twenty Editor Automation suites and focused Scene01 PIE; no separate Development Editor build log is claimed.

`TODO-02C3J` replaces that one-time snap with Ability-owned pre-launch smoothing. Both Launch Abilities freeze the C3I yaw/velocity pair in `FLaunchFacingSmoothingState`, then let `UAbilityTask_TurnToFacing` move only Yaw along the shortest arc at the authorable native default rate of `1440 degrees/second`, preserving current Pitch/Roll. The Commit Notify and turn completion may arrive in either order; neither launches alone, while `CommitFrozenLaunch()` consumes the frozen velocity exactly once after both are ready. The Takeoff Montage pauses for a late residual turn, and Root Motion, invalid task state, premature Falling, cancellation, death, and unexpected Montage termination fail closed through existing Ability cleanup. The state is non-reflected and physically Public only because exported Public Ability headers hold it by value; it is not a reusable turning framework or Blueprint API. The user confirmed the dedicated and full combat/reaction Automation matrix plus focused Scene01 PIE; no separate Development Editor build log is claimed.

`TODO-02C3L` makes Small and Big presentation selection directional without creating another reaction tier or changing locomotion authority. `FHitReactionImpactResolver` remains the sole Context-to-target-local `Target -> Attacker` resolver; the private `FHitReactionFourWayMontageSelector` accepts only that planar direction plus a complete non-owning Front/Back/Left/Right Montage set. It maps `Abs(X) >= Abs(Y)` to Front/Back and the remaining sectors to Right/Left, using the X axis on exact diagonal ties and ignoring Z. Incomplete configuration and zero, near-zero, NaN, or Inf planar input return `nullptr`. Each Player/Enemy Small/Big Ability validates the same complete set and ends before creating a Montage task when selection fails; the old generic Montage properties no longer exist. Small remains an overlay with no action/movement cancellation, while Big retains its established Root Motion, action cancellation, ledge/falling cleanup, and no-pre-turn policy. Launch, AI yaw ownership, Tags, Context, damage, Poise, and C3K feedback remain unchanged.

`TODO-02C3M` adds the Player Parry-success presentation at the existing native defense boundary. Melee resolution passes the contact `FHitResult` with `bAllowParry = true`; projectile resolution passes `false` and therefore cannot consume a Parry window. A valid Parry remains terminal for that contact and optionally requests the Controller-owned hit-stop (`0.05s / 0.03` approved default), reuses the Player Big camera-shake path, and plays a Parry-owned sound at a validated impact point or Player-location fallback. The channels are independent and fail closed; no Health reaction, blood, overlay, second damage path, GameplayCue, new Tag, or replication route is introduced. Trace Window `DeliveredTargets` and projectile `bHitDelivered` remain the one-contact guards.

`TODO-02C3N` extends the same native boundaries with two optional direct audio dispatches. `UPlayerGuardAbility::TryGuardMeleeHit` receives the resolver's `FHitResult` and plays `GuardSuccessSound` once only after a Guard-Stamina GameplayEffect succeeds; both projectile Guard and a Guard-break absorption are one consumed contact, and invalid/missing presentation is a no-op. `APlayerCharacter::OnHealthAttributeChanged` calls `TriggerReceivedHitSound` after the existing Camera Shake and before Stunned/invalid-reaction suppression, but only for the first Health modifier of each authoritative nonlethal GE Spec whose Instigator matches `Team.Enemy` exactly. Both helpers use a valid target HitResult point or finite actor-location fallback, and neither adds a GameplayCue, audio framework, replication route, or second damage path; authored sound assignments remain local Content WIP.

Focused native source/config commits intentionally keep mutable authoring assets out of version control: the GameplayAbility Blueprint, GameplayEffects, Montage, AnimBP, `BP_Player`, and input assets remain local development WIP. Selected meshes, Skeleton/material dependencies, and animation sequences are a stable source-asset baseline, but the source/config subset alone is not a clone-ready reproduction of the local PIE fixture.

## Pre-Maintenance plan.md Snapshot (TODO-02B3)

# TODO-02B3: Lock-On Retention Hysteresis v1（阶段收口记录）

> 本文件保留 TODO-02B3 的完整计划与收口证据。用户已确认 15% retention 和 retention-only Cycle no-op 两个冻结点；Gemini 已按批准范围完成实现。本阶段只处理既有 Player Lock-On 的“保持”屏幕边界，不改变获取、循环、死亡交接或 Bow/Projectile 的既有所有权。

## Plan State

- **状态**：实现、用户确认的 Automation/PIE 门禁、Main 单轮 defect-first fresh review 均已完成；本记录进入文档收口与提交阶段。
- **用户冻结确认**：retention 固定为每轴 15% 的单一原生常量、不增加配置字段；retention-only 状态下 Cycle 严格 no-op，保留当前锁定，不隐式清锁或自动换目标。
- **仓库**：E:\GameDevelop\PolyQuest（UE 5.8，运行时模块 PolyQuest）。
- **阶段基线**：fdf8fa2c76b119b51cc9b7027756ac272425fb08（main，2026-08-29）。
- **现有工作区纪律**：保留全部既有 WIP：Config/Automation/Presets/1.json、大量 Content/** 修改/删除或未跟踪项，以及本阶段六个 Source/test 文件和四份文档改动。Config/Content/其他 WIP 不属于本阶段，不能回滚、清理、格式化或纳入提交。
- **最近完成阶段**：TODO-02B3 是当前收口阶段；其前序最近提交为 fdf8fa2（Automation Unity 辅助符号修复）。该历史提交不是 TODO-02B3 的运行时证明。
- **前一阶段追溯**：C3N closeout、用户既有验证和提交边界已保留在 ROADMAP.md、ARCHITECTURE.md、README.md 的已完成记录中；本文件不再把 C3N 的历史 handoff 当作当前执行指令。

## Objective And Player Problem

固定高位斜视镜头下，已锁定目标在屏幕边缘因相机轻微移动或敌人位移而越出一像素时会立即清锁，导致锁定、朝向和 Bow 的锁定优先表现生硬。TODO-02B3 只增加一个有限的屏幕空间保持缓冲：

- 主动获取和滚轮循环仍只接受严格视口内的候选；
- 已经拥有的 LockedTarget 在小范围越出视口时继续保持；
- 目标真正离开保持边界、死亡、被销毁或不再满足现有敌我/ASC/状态规则时，仍按当前生命周期清锁或死亡交接；
- 不引入新的目标源、计时宽限、预测或第二条伤害/投射物路径。

## Evidence Baseline And Current Call Chain

以下实现结论来自当前 on-disk 源码、Config、Automation 测试、基线 CodeGraph 调用链和代码审查图的只读结果；用户另确认了本阶段 Automation 与 focused Scene01 PIE 通过。静态、用户 Automation、用户 PIE 和独立编译/Editor readback 证据仍分开记录。

1. APlayerCharacter::Tick() 每帧调用 ValidateCurrentLockedTarget()；有效后才更新锁定朝向。
2. TryAcquireLockOnTarget()、HandleTargetCycleTriggered() 和 TryRetargetAfterLockedTargetDeath() 都通过 BuildLockOnCandidates()；该函数会调用 TryProjectLockOnWorldPoint()，再使用 FPlayerLockOnTargeting::IsStrictlyWithinViewport()。
3. ValidateCurrentLockedTarget() 先检查 Player ASC/Controller、Player Dead、目标 Dead，再调用 FCombatProjectileTargeting::IsValidTargetCandidate() 和 CacheCurrentLockedTargetCandidate()。除确认死亡外，当前无效原因都会清锁。
4. CacheCurrentLockedTargetCandidate() 同时投影 Player 和当前目标，计算顺时针角度/距离并更新 LastValidLockedTargetCandidate；ResolveValidLockedTarget() 直接复用这条验证路径。
5. UBowDrawFireAbility 在启用 Target Assist 的 Release 时调用 ResolveValidLockedTarget()；Bow 自己的自动候选仍由 FCombatProjectileTargeting 以当前 6% 屏幕边界处理。两者必须继续是两个窄边界，不能把 Bow helper 变成全局 Lock-On 设置。
6. PlayerLockOnAutomationTests.cpp 与 ProjectileLifecycleAutomationTests.cpp 已补齐保持外扩、真实循环输入 seam、超过 15% 清锁和 Bow 锁定快照回归；B3 使用了 retention 内的严格视口外坐标与明确越界坐标。

## Frozen Product And Runtime Contract

### 1. Acquisition、Cycle、Death Handoff

- BuildLockOnCandidates() 的投影 margin 固定为 0.0f；IsStrictlyWithinViewport() 的旧语义和边缘拒绝行为保持不变。中键获取、滚轮循环和死亡后的候选扫描都不得使用 retention margin。
- 已保持但暂时落在严格视口外的当前目标不进入循环候选。此时滚轮输入必须 no-op 并保留当前锁定，不得因为 FindCycledTargetIndex() 找不到当前目标而隐式清锁，也不得自动换成别的目标；目标回到严格视口后才恢复正常循环。严格视口内的循环顺序和方向完全不变。
- 只有当前目标确认死亡时才进行一次现有的顺时针严格候选扫描；死亡目标即使最后一帧位于 retention 区域，也不得让交接扫描采用 overscan。其他失效原因仍不自动寻找替代目标。

### 2. Retention Hysteresis

- v1 冻结为每轴 15% 的有限 viewport 扩展：-0.15 * Width < X < 1.15 * Width，-0.15 * Height < Y < 1.15 * Height。扩展边界使用严格不等式，正好落在外边界仍清锁；margin 为零时必须与旧严格判定完全一致。
- 只在已有 LockedTarget 的保持/验证路径对目标投影使用 15%；Player 的投影锚点仍使用严格 margin 0.0f。这样不会把 Player 自身或候选获取范围扩大。
- 保持判定必须继续要求：WorldPoint、投影坐标、Viewport 和 margin 为有限值；Viewport 为正；本地 Controller、CameraManager、Camera 前方点积和 ProjectWorldLocationToScreen() 有效。任何失败都 Fail-Closed。
- 目标仍必须通过现有 FCombatProjectileTargeting::IsValidTargetCandidate()：错误阵营/同队、Dead、Invulnerable、Destroyed、缺失 ASC 或其他现有资格失败都清锁。此阶段不改 Team、ASC、GameplayTag 或死亡生命周期。
- 15% 是 v1 生产调用的唯一上限和唯一原生常量；纯判定 helper 只校验显式 margin 为有限且非负，不再额外硬编码第二个 max。运行时调用点只允许使用 0.0 或 0.15；不新增 UPROPERTY、Config、DataAsset、编辑器调参字段，也不再留一个未定义的“small upper cap”。Focused PIE 只验证边界手感和没有行为回归；若以后要改数值，另开已批准阶段。
- 不增加 Timer grace period、连续丢失帧计数、目标预测、LOS 改写、相机重置、自动重选、目标排序重写或任何 projectile targeting 共用框架。

### 3. Shared Consumers And Ownership

- Tick()、TryGetLockedTargetDirection()、Lock-facing、Action-facing、Dodge-facing 与 ResolveValidLockedTarget() 必须观察同一条 retention 验证结果；不复制一套“宽松锁定”布尔值。
- Lock highlight、LastValidLockedTargetCandidate 的顺时针锚点、Bow Release 的一次性目标快照和已发射箭的生命周期所有权保持原样。Bow 发射后滚轮、清锁、死亡或后续投影变化不得改写已发射 Projectile。
- 不修改 FCombatProjectileTargeting 的 6% 规则，不修改 Bow Ability、Projectile Actor、Damage GameplayEffect、GAS/ASC、GameplayTags、Input Mapping 或 Content 资产。

## Route And Delegation

    Outer: ue-stage-workflow
    Primary: ue5-cpp-gameplay
    Support: ue5-debug-validation
    Route reason: 窄范围原生 Lock-On 屏幕空间保持校验和现有 Automation 扩展；不涉及资产、Blueprint、Input、GameplayTags、Editor 写入或新的 GAS 路径。

    Plan explorers: 0
    Implementation executors: 1 (Gemini)
    Execution route: manual/out-of-band Gemini
    Complex Executor: one scoped lifecycle-sensitive implementation
    Main parallel work: none
    Reason: Lock-On 投影、死亡交接、朝向和 Bow 读取共享同一生命周期合同；单一执行者按冻结 API 顺序修改，Main 保留合同、验证解释、fresh review、文档、暂存和提交所有权。

### Ownership

- **Contract owner: Main**：冻结 margin、边界不等式、Cycle no-op、死亡交接、消费者复用、测试接受条件和非目标。任何需要新文件、公开 API、Tag、Input、Config、Asset 或生命周期规则的情况必须停工返回 Main。
- **Implementation writer: Gemini**：只修改下列批准源码/测试路径，并完成严格实现自审；不得编辑项目文档、Config、Content、Blueprint、Editor 状态或提交。
- **User-owned gates**：用户在 VS 2022/Unreal Editor 中手动编译 PolyQuestEditor (Development Editor)、运行 Automation、做 Editor readback 和 Scene01 PIE/视觉验证。
- **Main after validation**：Main 独立执行一轮 defect-first fresh review；本阶段不执行第二轮 adversarial review，也不把 Gemini 的自审计为独立 review。通过后由 Main 收口 ROADMAP.md、ARCHITECTURE.md、README.md、本计划，按明确路径暂存并提交。

## Approved Native And Test Slice

除以下路径外不得修改任何文件。共享契约文件均标记为 Contract owner: Main；implementation writer: Gemini。

1. E:\GameDevelop\PolyQuest\Source\PolyQuest\Public\Character\Player\PlayerLockOnTargeting.h
   - 增加一个纯屏幕空间、带显式 margin ratio 的判定原语（建议名 IsWithinViewportWithMargin），保留 IsStrictlyWithinViewport() 原声明和语义。
   - 文档说明 finite/positive viewport、finite/non-negative margin、严格扩展边界和 margin=0 等价性；helper 不承担产品上限，运行时调用点只传 0.0 或 0.15；不放置 Editor/Config 可调字段。

2. E:\GameDevelop\PolyQuest\Source\PolyQuest\Private\Character\Player\PlayerLockOnTargeting.cpp
   - 实现上述纯判定，检查 margin 与计算出的扩展量有限；不要改变排序、鼠标最近、循环或死亡 successor 算法。
   - 不复用或修改 CombatProjectileTargeting.cpp 的 Bow 6% helper。

3. E:\GameDevelop\PolyQuest\Source\PolyQuest\Public\Character\Player\PlayerCharacter.h
   - 为私有 TryProjectLockOnWorldPoint() 增加明确的 margin 参数（默认或调用点显式为 0 均可，但获取/循环调用必须肉眼可见地传 0）。
   - 在 WITH_DEV_AUTOMATION_TESTS 下增加真实循环触发所需的最小 seam（如 TriggerTestTargetCycle(float)）；Shipping/反射 API 不得新增。
   - 不改变 ResolveValidLockedTarget()、Lock-on、Bow、ASC、Input 或 GameplayTag 的生产公开契约。

4. E:\GameDevelop\PolyQuest\Source\PolyQuest\Private\Character\Player\PlayerCharacter.cpp
   - 在本文件匿名 namespace 放置唯一的 constexpr float retention ratio 0.15f，或等价的单一原生来源；不得引入第二个 cap。
   - BuildLockOnCandidates() 的 Player/候选投影显式使用 0.0f。
   - CacheCurrentLockedTargetCandidate() 对 Player 使用 0.0f，仅对当前 Target 使用 0.15f；保留 finite、camera、controller、ASC、team、Dead/Invulnerable 检查和缓存更新顺序。
   - 保持 ValidateCurrentLockedTarget() 的死亡分支和 TryRetargetAfterLockedTargetDeath() 的一次严格扫描不变。
   - 调整 HandleTargetCycleTriggered()：严格候选仍只来自 BuildLockOnCandidates()；仅当该函数成功返回、当前 LockedTarget 通过本帧 retention 验证但不在严格候选列表时，输入才 no-op 并保留锁定，不得清锁或自动重选。`NextIndex == INDEX_NONE` 时不得只依据旧的 LastValidLockedTargetCandidate；需要使用本次验证结果或一次窄的当前资格+投影重检确认“仍在 retention、仅缺席严格候选”。BuildLockOnCandidates() 失败（Player 投影、Controller/Camera、世界等基础失败）以及其他原有失败语义继续清锁/返回，不得用“LockedTarget 仍非空”做宽泛兜底。
   - ResolveValidLockedTarget()、TryGetLockedTargetDirection()、Lock-facing/Action-facing/Dodge-facing 只通过现有验证路径获得新保持语义，不添加旁路状态。

5. E:\GameDevelop\PolyQuest\Source\PolyQuest\Private\Tests\PlayerLockOnAutomationTests.cpp
   - 扩展纯判定：margin=0 与 strict 等价；X/Y 四边的刚内/刚外；精确扩展边界拒绝；NaN/Inf、无效 viewport、负/非有限 margin Fail-Closed。
   - 扩展真实 Player fixture：严格视口外目标不能 Acquire；严格候选外目标不能被 Cycle 选入；已锁定目标在边缘/刚越界且仍在 15% 内时保持高亮和锁定；超过 15%（X、Y）后清锁且不自动重选；相机/目标移动跨边界时结果稳定。
   - 覆盖 retention 状态下的 Cycle no-op、死亡目标一次严格顺时针交接、Dead/Destroyed/Invulnerable/错误阵营/Player Dead、缺失 Controller/Camera、投影失败和 teardown。
   - 保留现有 Lock-facing、Action-facing、Dodge、Sprint、Guard、Parry、Root Motion、Bow requester 回归；测试 hook 必须最终走与生产相同的 margin 判定，不复制第二套数学。

6. E:\GameDevelop\PolyQuest\Source\PolyQuest\Private\Tests\ProjectileLifecycleAutomationTests.cpp
   - 修正现有 B3 “X = 0 即无效锁”断言：边缘点在新 retention 合同下应保持；无效/回退样例改为明确超过 15% 的坐标（例如 X < -0.15 * Width）。
   - 增加一个仅在 15% retention 内、但位于严格视口外且超出 Bow 6% 自动候选边界的锁定目标（例如 1920 宽视口的 X=-150；15% 外扩下界为 -288，Bow 6% 下界为约 -115.2），确认 ResolveValidLockedTarget() 和 Target Assist Release 仍优先使用该锁定快照；再用明确超过 15% 的坐标（例如 X=-300）确认清锁并回到 B2 自动候选。
   - 保留 Target Assist disabled、箭发射后不重定向、死亡交接和 Projectile 生命周期断言；不修改 Bow/Projectile 生产代码。

### Read-only reference paths (do not edit)

- E:\GameDevelop\PolyQuest\Source\PolyQuest\Private\Combat\Projectile\CombatProjectileTargeting.cpp：只读取 Bow 的 6% 边界、Camera 前方和候选资格，不能改成共享 Lock-On helper。
- E:\GameDevelop\PolyQuest\Source\PolyQuest\Private\AbilitySystem\Abilities\BowDrawFireAbility.cpp：只确认 ResolveValidLockedTarget() 的一次性 Release 快照调用。
- E:\GameDevelop\PolyQuest\PolyQuest.uproject、Source/PolyQuest/PolyQuest.Build.cs、Config/Tags/PolyQuestGameplayTags.ini：只做依赖/Tag 只读核对；本阶段不应需要改动。

## Execution Order And Stop Conditions

1. Gemini 先读取本计划、AGENTS.md、当前基线和六个批准路径，逐行确认 BuildLockOnCandidates 的三类调用、CacheCurrentLockedTargetCandidate 的双投影和 B3 的旧边界断言。
2. 先完成 FPlayerLockOnTargeting 纯判定，再扩展 TryProjectLockOnWorldPoint 的显式参数；不先改 IsStrictlyWithinViewport 的行为。
3. 按“获取/循环显式 0 → 保持目标 0.15 → Cycle no-op”顺序修改 Player 调用链；每一步检查死亡交接、highlight 和 ResolveValidLockedTarget 没有旁路。
4. 扩展两个既有 Automation 套件，优先写失败/边界/生命周期用例，再写 Bow retention 快照回归；不新建通用测试框架。
5. Gemini 只做静态自检，并完成两遍执行者自审：第一遍 defect-first 检查生命周期/边界/回归，第二遍按冻结合同做 adversarial 检查；随后读取最终 diff/调用链、运行 Rider lint 或 problem 检查（端点可用时）、git diff --check、批准路径审计。两遍都不能称为 Main 的独立 fresh review；不得调用 UBT、Build.bat、Visual Studio build、Editor、Automation、PIE、打包、Git stage/commit。
6. 任何需要第七个源码/测试文件、修改 Bow/Projectile/Build.cs/Config/Tag/Input/Content、改变死亡交接或增加第二个 grace/cap 的需求，立即停止并把证据交回 Main。

## Validation Matrix And Evidence Gates

### Gemini/Main static gate (not runtime proof)

- IsStrictlyWithinViewport() 的旧测试与调用行为保持；BuildLockOnCandidates() 所有获取/循环/死亡调用仍传 0。
- CacheCurrentLockedTargetCandidate() 只有当前 Target 使用固定 0.15，Player 锚点不扩大；所有失败路径仍清锁或走既有死亡交接。
- ResolveValidLockedTarget()、Lock-facing/Action-facing/Dodge-facing 和 B3 Release 读取同一验证结果；Projectile 发射后不读取锁。
- 批准路径之外无差异；git diff --check 为零；Rider/CodeGraph/CRG 输出只作为静态/影响证据。codegraph 当前索引与 fdf8fa2 一致；code-review-graph 只补充调用影响，不证明运行时。

### User-owned compile and Editor readback evidence

- 本记录没有独立的 Visual Studio `PolyQuestEditor (Development Editor)` 编译日志，也没有新增 Editor 资产读回；不从用户 Automation/PIE 结果反推独立编译或 readback 证据。
- 本阶段没有新的资产读写要求；现有 LockOn/TargetCycle 输入引用和 Bow Target Assist 资产不在批准修改路径内。

### User-owned Automation gate (confirmed)

- 用户确认 TODO-02B3 相关 Automation 通过，包含 `PolyQuest.Player.LockOn` 以及 `PolyQuest.Projectile.Lifecycle` / `PolyQuest.Projectile.TargetAssist` 的 Bow 回归。
- 该结果是用户运行证据；本轮未重新运行 Automation。

### User-owned focused Scene01 PIE gate (confirmed)

- 中键获取与滚轮循环仍只接受严格视口内目标；严格视口外候选不能主动锁入。
- 已锁目标在视口边缘、刚越出边界但在 15% 内时保持锁定、高亮、合法锁定朝向和 Bow Release 锁定优先；相机轻移和目标轻移均不瞬间脱锁。
- 目标超过任一轴的 15% 扩展边界、死亡/销毁、Invulnerable、错误阵营、Player Dead、Controller/Camera/投影失效时按合同清锁或只做一次死亡交接；不自动重选。
- 在 retention 区域滚轮输入不清锁、不偷换目标；目标回到严格区域后循环恢复。
- 用户确认 focused `Scene01` PIE 通过，覆盖上述锁定保持/清锁、Cycle no-op、Bow 锁定优先与既有朝向/生命周期回归；本轮未重新启动 Editor 或 PIE。

### Evidence labels

- 当前已确认：源码/Config 静态读取、基线 CodeGraph 调用链、code-review-graph 的影响补充、用户确认的 TODO-02B3 Automation 与 focused Scene01 PIE。
- 当前未单独提供：本阶段 Development Editor 编译日志和 Editor readback 记录；本轮未重新运行任何用户门禁。Gemini 的两遍自审不等于 Main 的独立 fresh review。

## Known Debt And Blocker Decision

- Main 的单轮 defect-first fresh review 未发现 P0/P1/P2 或需要返工的源代码阻塞；既有 Lock-On/Bow 源码边界完整，保持用例和循环触发 seam 已在本阶段补齐。
- helper 契约注释完整度、个别测试注释措辞和 Y 轴更深的集成矩阵属于非阻塞观察，本阶段不把它们升级为债务或追加实现范围。
- Content/** 与 Config/** WIP、作者化输入/Widget/Bow/Animation 资产未形成干净检出 fixture，是版本边界和验证说明，不是本阶段源码实现 blocker；继续原样保留。
- Bow 的固定 6% overscan 是已有、已验证的武器族规则；本阶段不把它改为 15%，也不把 15% 写回 Projectile 配置。
- 若 focused PIE 证明 15% 手感仍需改值，不能在本阶段由执行者临时调参；应先结束本阶段并单独确认新阶段/新 plan。

## Documentation And Commit Boundary

- 本阶段计划阶段只允许 Main 修改 plan.md 和必要的 ROADMAP.md 漂移表述；ARCHITECTURE.md 与 README.md 仅记录已通过门禁的稳定事实，本次已写入 hysteresis 的收口摘要。
- 实现与用户门禁通过、Main 单轮 fresh review 完成后，Main 已将 TODO-02B3 移到 Done Milestones，并在 Architecture/README 写入实际 margin、Cycle no-op、死亡交接和 Bow 分界的已验证摘要。
- 提交候选为六个批准源码/测试路径及 Main 明确批准的文档收尾；排除 Config/Automation/Presets/1.json、所有 Content/**、Build.cs、uproject 和其他 WIP。本次用户已明确授权提交，仍不得扩大暂存范围。

## Gemini Handoff Prompt

以下提示可在用户已确认本计划后原样交给 Gemini；它是冻结执行边界，不是新的架构授权：

> 你是 TODO-02B3 的实现执行者。工作目录必须是 E:\GameDevelop\PolyQuest，基线为 fdf8fa2c76b119b51cc9b7027756ac272425fb08；先读取 AGENTS.md 和当前 plan.md。执行路线是 manual/out-of-band Gemini，Primary Skill 为 ue5-cpp-gameplay，Support Skill 为 ue5-debug-validation。Contract owner: Main；implementation writer: Gemini。
>
> 只允许修改本计划列出的六个绝对路径。保持 IsStrictlyWithinViewport、获取、循环、死亡一次性交接、Team/ASC/Dead/Invulnerable、Lock-facing、Bow 6% 自动候选和已发射 Projectile 生命周期不变。新增的 retention 只对已有 LockedTarget 的目标投影使用固定每轴 15% 严格扩展；纯 helper 只校验有限/非负 margin，运行时调用点只能传 0.0 或 0.15；Player 投影和所有 BuildLockOnCandidates 调用使用 0。只有 BuildLockOnCandidates 成功、且当前目标本帧通过 retention 但不在严格候选列表时，Cycle 才能 no-op 保留锁定；`NextIndex == INDEX_NONE` 时不得只依据旧缓存，必须以本次目标资格+投影重检确认该条件。基础投影/Controller/Camera/世界失败仍按原语义清锁或返回，不得以 LockedTarget 非空作为宽泛兜底。不得新增第二个 cap、Timer grace、预测、LOS/相机行为、Tag/Input/Config/Asset/Build.cs 或通用 targeting 框架。
>
> 测试必须走相同生产 margin 判定，覆盖 0 等价 strict、X/Y 边界、NaN/Inf、Acquire/Cycle strict、retention 内保持、超过 15% 清锁、Cycle no-op、状态/teardown、死亡严格交接，以及 B3 Bow lock snapshot 回归。B3 至少使用一个严格视口外但仍在 retention 内、且超出 Bow 6% 自动边界的坐标（1920 宽时可用 X=-150），并用 X=-300 一类坐标覆盖超过 15% 的清锁；不得修改 Bow/Projectile 生产源码。
>
> 只做静态自审：先做一遍 defect-first，再做一遍针对冻结合同的 adversarial 自审；若 `.codegraph/` 可用，先用 CodeGraph 核对符号和调用链；`.code-review-graph/` 只能作为变更影响补充，不能当编译或运行时证明；随后读取最终 diff，运行 Rider lint/problem 检查（若可用）与 git diff --check。两遍自审都不能冒充 Main 的独立 fresh review。不得编译、运行 Editor/Automation/PIE、打包、stage、commit 或扩大范围。完成 handoff 时列出 changed paths、静态证据、两遍自审 findings、未运行的用户门禁和剩余风险；遇到未列出的文件或契约需求立即停工返回 Main。

## Dependency Order After This Stage

当前路线已按“先闭合玩家战斗体验、再进入敌人远程内容”的产品优先级更新；本阶段已完成，后续只制定不执行：

TODO-03A3E World Pickup Interaction Prompt v1
→ TODO-07B4 Attack Impact Camera Shake And Reaction-Tier Mapping v1
→ TODO-03A7 Selected Melee Motion-Warp Contact Assist v1
→ TODO-05A Front Critical v1
→ TODO-05B Backstab v1
→ TODO-05C Stagger Execution And Stagger Backstab v1
→ TODO-03C Ranged Enemy v1

`TODO-05A -> TODO-05B -> TODO-05C` remains the technical dependency order inside the punish family; `TODO-07B4` is a player-combat-experience gate placed before the ranged-enemy stage rather than a prerequisite of Motion Warping or punish implementation.

每一项都必须先通过自己的 adoption、compile、Automation、Editor/PIE 和 review 门禁；路线图未来项不是本阶段已实现事实。

## Closeout Record

- **Implementation evidence**：Gemini 修改了六个批准 Source/test 路径：15% 每轴 retention helper、严格 Acquire/Cycle/Death 投影、retention-only Cycle no-op、Player 验证路径复用，以及 Lock-On/Bow 生命周期回归用例；未修改 Bow/Projectile 生产代码、Config 或 Content。
- **User compile/Editor/Automation/PIE evidence**：用户确认本阶段 Automation 与 focused Scene01 PIE 通过。本记录没有独立 Development Editor 编译日志或新增 Editor readback，不作推断；本轮未重新运行这些门禁。
- **Main fresh review**：Main 已完成一轮独立 defect-first fresh review，未发现 P0/P1/P2 或需要返工的源代码问题。Gemini 的两遍执行者自审仅作为交接证据，不替代该 review；本阶段不执行第二轮 adversarial review。
- **Documentation/commit**：本次收口同步 ROADMAP.md、ARCHITECTURE.md、README.md 与本计划，并登记未来 `TODO-07B4`。提交只包含六个批准 Source/test 路径与四份文档；Config/Automation/Presets/1.json、所有 Content/**、uproject、Build.cs 和其他 WIP 排除在外。

## Archive Rules

- 历史记录只用于追溯提交、验证和设计演进，不覆盖源码、Config、实际资产或当前文档。
- 新完成阶段只在当前 ROADMAP.md 保留紧凑结果；需要长期保留的详细收口证据追加到本文件，并注明提交和验证类型。
- 不把归档文件中的未来 TODO、旧依赖或旧资产描述当作当前实现事实。

## DOC-MAINT-01 Review Adjustment (superseded, 2026-08-29)

- Gemini review 建议删除 `Done Milestones`、移除路线图原则段和底部文档职责段，并进一步压缩 `Current State`。
- Main 保留紧凑的 `Done Milestones`，因为 `AGENTS.md` 将其定义为完成阶段的交接要求；同时采纳其余精简建议，将稳定架构原则归回 `ARCHITECTURE.md`，在 `ROADMAP.md` 仅保留必要的 `Route Constraints` 与顶部归档指针。
- 这是文档结构决策记录，不是新的运行时或验证证据；当前路线和契约仍以 `ROADMAP.md`、`ARCHITECTURE.md` 及源码/Config/实际资产为准。

## DOC-MAINT-01 Policy Revision (2026-08-29)

- 用户授权在不受旧规则约束的前提下重新判断 `Done Milestones` 是否必要。Main 采纳删除：`ROADMAP.md` 的 `Current State` 保留唯一最新基线/阶段指针，`plan.md` 保存最近阶段收口，详细历史进入本归档。
- `AGENTS.md` 已改为不要求独立完成章节；只有不重复且对依赖有实际帮助的完成事实才可留在路线图中。
- 本修订取代上方早期“新完成阶段只在当前 `ROADMAP.md` 保留紧凑结果”的措辞：活动路线只列开放阶段；已完成阶段不得保留完整完成块，必要时只留一行非重复依赖指针。
- 本修订是信息架构与维护规则变更，不是新的运行时实现或验证证据。

## TODO-03A3E Final Closeout (2026-08-29; HEAD `6a96fc1`, working tree not clean)

> 本节是在文档维护门中从活动 `ROADMAP.md` 移出的阶段收口摘要。记录时 `HEAD` 为 `6a96fc1`，工作区仍含既有 Config/Content/Source WIP，因此不是干净 checkout 快照；本节是历史追溯资料，不是运行时权威。

- **范围与所有权**：世界武器拾取提示由 Player-owned 的事件驱动候选快照驱动；Controller 只拥有本地被动 `UWorldInteractionPromptWidget`，`AWorldWeaponPickup::CanInteract` 仍是资格门，`UWeaponEquipmentComponent::TryEquipWorldPickup` 仍是唯一装备变更路径。Overlap、拾取状态、移动、FormerOwner 冷却、Dead、Possess、交易、销毁和 teardown 都汇入同一解析器；未增加 Tick 轮询、通用交互框架、第二套交易逻辑、Gameplay Tag 或 Input/Config 路由。
- **用户运行证据**：用户手动选择当前全部 PolyQuest Automation 套件，记录为 25/25 `Success`；用户确认 focused `Scene01` PIE 覆盖提示显隐、最近候选切换、E 装备/失败交互、FormerOwner 冷却恢复、重新 Possess 和 teardown。
- **静态与复核证据**：Gemini 报告 CodeGraph 调用链核对、Rider error-level 检查无诊断以及 `git diff --check -- Source/PolyQuest` 通过；Main 完成一轮受控 defect-first fresh review，未发现 P0/P1/P2 blocker。没有执行第二轮 adversarial review，也没有派遣独立 Reviewer。
- **未声称证据**：没有独立 Development Editor 编译日志或新的 Editor readback；Automation/PIE 结果不替代这些证据。`Config/Automation/Presets/1.json` 不是完整测试清单，25/25 来自用户手动选择，不从 preset 推断。
- **排除项与债务**：`WeaponEquipmentComponentAutomationTests.cpp` 的未批准 TransactionMatrix 修复、本地 Guard WIP 资产、全部 Config/Content WIP 和其他 Source WIP 均未纳入；真实 overlap/delegate 深度与失效 E 快照不改装备的直接断言仍是非阻塞验证债务，TransactionMatrix 资产依赖留待单独批准的装备/资产基线阶段。
- **提交边界**：阶段实现提交为 `6a96fc1`；本节文档维护不改变该提交的代码/资产范围。

## TODO-07B4 Implementation Closeout (2026-08-29; working-tree snapshot at HEAD `9c2ca48`, not clean)

> 本节是 TODO-07B4 的历史收口记录。归档时工作区包含既有 `AGENTS.md`、Config、Content/** 与其他 Source WIP，因此不是干净 checkout 快照；本节不替代源码、Config 或实际资产作为运行时权威。

- **范围与所有权**：`AEnemyCharacter::OnHealthAttributeChanged()` 继续负责同一 GameplayEffect Spec 的 Health 去重、ReactionTier 分类和 lethal 顺序；`HandleCombatImpactFeedback()` 在精确 `Team.Player` 过滤后只向真正的 `APlayerCharacter` 转发一次 attacker-impact Shake。Player 独立读取 `Small/Big/LaunchAttackerImpactCameraShakeClass`，受击路径继续读取原三字段；两组字段可以引用同一 Camera Shake 资产，但不存在隐式回退。PlayerCameraManager、活动 Shake 弱引用、同档重启/换档和 UnPossessed/EndPlay 清理仍由 Player 统一拥有。
- **用户运行证据**：用户确认 focused `PolyQuest.Combat.AttackerImpactCameraShake` Automation 再次 `Success`，并确认 `/Game/Maps/Scene01` PIE 通过，覆盖攻击者命中镜头冲击与既有顿帧共存的体验路径。
- **静态与复核证据**：Gemini 报告 Rider error-level 检查无诊断和 `git diff --check` 通过；Main 用 CodeGraph 与 code-review-graph 做一跳结构/影响核对，并完成一轮独立 defect-first fresh review。修复前发现的测试字段隔离 P2 已由 attacker-only fixture 清空受击字段后再配置的断言闭环；修复后未发现 P0/P1/P2 blocker，不执行第二轮 adversarial review。
- **未声称证据**：本次记录没有独立手动 `PolyQuestEditor` 编译日志，也没有六字段 `BP_Player` Editor readback；不以 Automation 或 PIE 替代这些证据。缺失 attacker 字段时测试产生的 fail-closed warning 是预期负向断言，不是运行时错误。
- **排除项与债务**：未修改或纳入 `AGENTS.md`、`Config/Automation/Presets/1.json`、全部 `Content/**`、`WeaponEquipmentComponentAutomationTests.cpp`、Build.cs、uproject、Gameplay Tags、Resolver/Trace/Projectile 和其他 WIP。六字段作者化 readback/手动编译证据作为 Roadmap validation debt，closure trigger 是在可提交的作者化基线或打包前完成对应读回；不阻塞下一阶段的源代码计划。
- **提交边界**：TODO-07B4 的四个批准 Source/test 路径与本阶段文档一并提交；最终提交 hash 以 Git 历史为准。

## TODO-03A7 Selected Melee Motion-Warp Contact Assist Closeout (2026-08-30; working-tree snapshot at HEAD `c91fbfd`, not clean)

> 本节保留被 TODO-03A7B 替换前的 `plan.md` 收口事实。归档快照位于当前 HEAD `c91fbfd`，工作区仍含用户的 Config/Content WIP，因此不是干净 checkout；实现本身的历史父基线是 `90ad381`，不能将其改写为当前仓库基线。本文件仅供追溯，不是运行时权威。

- **范围与所有权**：TODO-03A7 只为 Player 的 Light Combo entry 0 引入显式 opt-in 的 Motion Warping 接触辅助；Player 独占一个 `UMotionWarpingComponent`，Ability 负责 Lock-On 一次性快照、有限距离/角度/接地判定和清理，Player C++ bridge 只负责组件写入/清理。Charged、Sprint、Skill、Bow、Enemy、Locomotion、Hit Reaction、Projectile 和全局 targeting 均未接入。
- **运行时合同**：entry 默认关闭；生产参数为 `WarpStopDistance=190cm`、`MaxWarpDistance=110cm`、`MaxWarpAngleDegrees=60°`。无锁、死亡/销毁目标、死亡交接、非法 ASC/Controller、空中、非有限或超界几何均 fail-closed，Warp 失败不阻止普通轻攻击。目标以 Actor center 的静态 Transform 写入，不 Tick 跟随、不重选。
- **清理与伤害边界**：entry 替换、Montage 启动/同步失败、取消、自然结束、离地、Player 死亡、UnPossess 和 EndPlay 均清除 stale Warp Target；唯一近战伤害路径仍是 `UAbilityTask_MeleeTraceWindow -> FMeleeHitResolver -> Damage GameplayEffect`。
- **历史验证证据**：用户确认 `PolyQuest.Combat.PlayerMeleeMotionWarping` Automation 与 `/Game/Maps/Scene01` PIE 通过；Gemini 报告 Rider errors-only 检查和 `git diff --check` 通过。没有独立手动 `PolyQuestEditor` Development Editor 编译记录、逐项 Motion-Warp Editor readback，Automation 也未完整驱动生产 `StartComboEntry(0)` 入口；这些是验证债务，不被改写成 no-adoption 或运行时失败。
- **历史复核与提交**：Main 完成一轮受控 defect-first fresh review，未发现 P0/P1/P2 blocker；实现提交为 `c8b72c63522baea4a4c1fed983d48f226b20357d`。随后文档/测试路径收口提交为当前 HEAD `c91fbfdaab49e933922e902b92565859506f9351`；`WeaponEquipmentComponentAutomationTests.cpp` 已在该提交中，不是本阶段未提交 Source WIP。
- **排除项与后续指针**：全部 Config/Content、其他 Source WIP、测试 preset、Blueprint/Montage/AnimBP/地图、Engine 源码和旧 Test 项目均未纳入。后续玩家 Motion-Warp 采用拆片从 `TODO-03A7B` 开始；本节中的 `90ad381` 仅表示当时任务视角的父提交。

## TODO-03A7B Light Combo Motion-Warp Adoption Closeout (2026-08-31; working-tree snapshot at parent HEAD `c91fbfd`, not clean)

> 本节记录 TODO-03A7B 的实现与验证收口。快照基于父 HEAD `c91fbfdaab49e933922e902b92565859506f9351`，工作区仍含用户的 Config/Content 及其他 WIP，因此不是干净 checkout；本节是历史追溯，不是源码、Config 或实际资产的运行时权威。`90ad381` 继续只表示上一阶段任务视角的历史父基线。

- **范围与所有权**：六个批准 Source/test 文件完成 Light Combo entry `0..2` 显式门控、每段 opt-in、一次性静态 Lock-On 快照、任务/蒙太奇激活两级门禁、rollback 与生命周期清理。Player/Enemy 仅通过窄桥处理 `ClearLockedTarget()`、目标死亡/销毁和 UnPossess 的 Light-only 取消；左上角距离 HUD 保持 debug-only。唯一近战伤害路径仍为 `UAbilityTask_MeleeTraceWindow -> FMeleeHitResolver -> Damage GameplayEffect`，未引入通用 dispatcher、自动重选或其他攻击家族 Motion Warping。
- **用户证据**：用户确认修复后的 `PolyQuest.Combat.PlayerMeleeMotionWarping` focused Automation 成功，并确认 `/Game/Maps/Scene01` PIE 通过。该证据只覆盖用户实际运行的 focused 场景，不扩展为全量回归、编译或资产读回结论。
- **静态与复核证据**：Gemini 报告 Rider errors-only 无 error、`git diff --check` 通过；Main 以 diff-first、一跳 CodeGraph 和补充 code-review-graph 完成 defect-first fresh review，未发现 P0/P1/P2 blocker。没有执行第二轮 adversarial review。
- **未声称证据与验证债务**：没有独立手动 `PolyQuestEditor` Development Editor 编译，也没有逐项 entry 1/2 Motion-Warp Notify/Modifier/Root Motion/窗口 Editor readback；作者化 baseline 仍为本地 WIP。现有 3.14 seam 未独立构造 `ReadyForActivation()` 返回后同步 `EndTask()`，生产顺序已静态复核；该覆盖债务非本次提交 blocker，关闭触发器是未来可安全建立确定性 seam 时补测。
- **文档与规则**：`plan.md` 保留详细基线、执行记录、证据和债务；`ROADMAP.md` 仅更新当前/下一阶段指针与 canonical debt；`ARCHITECTURE.md` 更新稳定的 entry 0..2 Motion-Warp 合同；`README.md` 更新公开状态；`AGENTS.md` 增加 `ReadyForActivation()` 同步重入门禁规则。
- **排除项与提交边界**：全部 `Content/**`、`Config/Automation/Presets/1.json`、其他 Source/文档 WIP、Blueprint/Montage/AnimBP/DataAsset/地图、`PolyQuest.uproject`、`Build.cs`、Gameplay Tags、Engine/旧 Test 项目均未修改或暂存；无资产清理、回滚或导入。最终提交 hash 以 Git 历史为准。

## TODO-03A7B Final Commit Closeout (2026-08-31; HEAD `ae0139b`, working-tree not clean)

> 本节补充 TODO-03A7B 的最终提交视角。`HEAD` 为 `ae0139be10c3273cc8311c19d0210bb1d2e0cbc4`，工作区仍含用户的 Config/Content WIP，因此不是干净 checkout 快照；本节仅作历史追溯，不替代源码、Config 或实际资产作为运行时权威。

- **提交与范围**：`ae0139b` 完成 Light Combo entry `0..2` 的显式 opt-in、一次性静态 Lock-On 快照、Task/Montage 两级门禁、目标死亡/销毁与 UnPossess 清理桥接，并同步阶段文档/规则。`Content/**`、`Config/Automation/Presets/1.json`、其他 Source WIP、Blueprint/Montage/AnimBP/DataAsset/地图、`PolyQuest.uproject`、`Build.cs` 和 Gameplay Tags 未纳入。
- **用户证据**：用户确认修复后的 `PolyQuest.Combat.PlayerMeleeMotionWarping` focused Automation 与 `/Game/Maps/Scene01` PIE 通过；该证据只覆盖实际运行场景，不扩展为全量回归、手动编译或作者化资产 readback。
- **静态/复核证据**：Gemini 报告 Rider errors-only 无 error、`git diff --check` 通过；Main 完成批准范围内的一轮 diff-first、定点 CodeGraph/补充 code-review-graph 与 defect-first fresh review，未发现 P0/P1/P2 blocker；未执行第二轮 adversarial review。
- **未解决债务**：没有独立记录的手动 `PolyQuestEditor` Development Editor 编译，也没有逐项 entry 1/2 Motion-Warp Notify、Modifier、Root Motion、目标名和时间窗的 Editor readback；`ReadyForActivation()` 同步 `EndTask()` 的无头重入夹具也未独立构造。这些是验证债务，不改写为运行时失败或 no-adoption。
- **后续指针**：下一阶段为 `TODO-03A7C`。它应以当前 `ae0139b` 为仓库基线，分别审查 Charged/Sprint 的实际 Montage 与 Root Motion 适配；本归档中的 `90ad381`、`c91fbfd` 只保留各自历史任务视角，不能当作当前 HEAD。

## TODO-03A7C Charged/Sprint Melee Motion-Warp Adoption Closeout (2026-08-31; HEAD `ae0139b`, working-tree not clean)

> 本节记录 TODO-03A7C 的源码实现与用户验证收口。归档快照的 `HEAD` 为 `ae0139be10c3273cc8311c19d0210bb1d2e0cbc4`；工作区仍含用户的 Config/Content WIP、其他 Source WIP 与本阶段未提交变更，因此不是干净 checkout 快照。本节只作历史追溯，不替代源码、Config 或实际资产作为运行时权威。

- **范围与所有权**：9 个批准 Source/test 路径完成共享的无状态 `MeleeMotionWarping` config/snapshot/evaluator、Light 兼容 wrapper、Charged release 与 Sprint Attack 的独立 Ability-owned 静态快照接入。Charged 在成功 Cost/Commit、主动 Montage 活跃、Damage/Poise 设置和 `bReleaseStarted` 之后、`Montage_Resume()` 之前尝试一次；Sprint 在 Montage Task active/unfinished 与 Montage active 两级门禁之后、Guard/Sprint 取消之前尝试一次。两者只使用现有 Player bridge，不改变 ASC/AttributeSet、Gameplay Tags/Input、Cost、Trace/Resolver/Damage、Guard/Dodge 或 Sprint ownership。
- **Main 窄修复**：Main 在已批准的 Charged/Sprint cpp 内补充 Montage/AnimInstance `IsValid()` 门禁，未改变公开 API、时序、架构或资产范围。该修复符合项目后来记录的范围/可逆性例外；严重级别本身不授予实现权限。
- **用户证据**：用户确认最终修复后的 `PolyQuest.Combat.PlayerMeleeMotionWarping` focused Automation 成功，并确认 Scene01 PIE 通过；该证据只覆盖实际运行场景，不扩展为全量回归、手动编译或 Editor readback。
- **静态与复核证据**：Gemini 报告批准文件 Rider error-level 检查无 error、`git diff --check` 通过；Main 完成定点 CodeGraph/补充 code-review-graph 影响核对与一轮 diff-first defect-first fresh review，未发现需要返工的阻塞缺陷。图结果与 test gap 只作静态影响提示，不作运行时证明；未执行第二轮 adversarial review。
- **未声称证据与验证债务**：没有独立的 VS2022 `PolyQuestEditor` Development Editor 编译记录，也没有逐项 Charged/Sprint GA/Montage/Notify/Modifier/Root Motion/目标名/时间窗的 Editor readback；现有测试 seam 不能替代真实生产 Task/Montage 异步时序证明。关闭触发器是用户完成相应编译/readback，或对不适用 Ability 提供真实 evidence-backed no-adoption；这些债务不阻塞下一阶段源码计划。
- **排除项与提交边界**：全部 `Content/**`、`Config/Automation/Presets/1.json`、其他 Source/文档 WIP、`PolyQuest.uproject`、`Build.cs`、Gameplay Tags、Input、旧 Test 项目和用户资产均未纳入本阶段；无资产清理、回滚、导入或 Editor 写入。阶段候选提交仅限 9 个批准 Source/test 路径与明确文档收口，须待用户另行批准后 staging/commit。
- **后续指针**：下一阶段为 `TODO-03A7D：Melee Skill Motion-Warp Adoption v1`；本阶段不提前实现或改写其计划。

## TODO-03A7D Melee Skill Motion-Warp Adoption Source Closeout (2026-08-31; working-tree snapshot at parent HEAD `2472045`, not clean)

> 本节记录 TODO-03A7D 的源码实施收口。归档快照基于提交 `2472045`，工作区仍含用户的 `Content/**`、`Config/**` 与本阶段 Source/文档 WIP，因此不是干净 checkout；本节只作历史追溯，不替代源码、Config 或实际资产作为运行时权威。

- **范围与所有权**：`UPlayerMeleeSkillAbility` 完成默认关闭的五项 Motion-Warp 配置、Ability-owned 一次性静态 Lock-On 快照、共享几何 evaluator 接入、Montage/AbilityTask 激活门禁和 EndAbility 清理；`APlayerCharacter::UnPossessed()` 以独立 `Ability.Skill.Melee` Tag 容器取消技能，不改变 `Ability.Attack.Light` 或其他类别。唯一 `Trace -> Resolver -> Damage GE` 路径、ASC、Input、Gameplay Tags、Guard/Parry 与装备所有权保持不变。
- **Main 窄修复**：Main 在批准的 `PlayerMeleeSkillAbility.cpp::ActivateAbility()` 内补充六个 Notify Task 的 `ReadyForActivation()` 前后重入/有效性守卫及最终 Player/World 门禁；未改变公开 API、架构、资产或玩家伤害合同。
- **用户证据**：用户确认修复后的 `PolyQuest.Combat.PlayerMeleeMotionWarping` focused Automation 成功；用户在初始实现交接时确认过 Scene01 PIE。修复后没有单独的新 PIE 收据，故不将其扩展为最新视觉回归或全量套件证明。
- **静态与复核证据**：Gemini 报告批准文件 Rider error-level 检查无 error、`git diff --check` 通过；Main 完成一轮批准范围内的 diff-first defect-first fresh review，未发现 P0/P1/P2 blocker。未执行第二轮 adversarial review。
- **未声称证据与验证债务**：没有独立手动 `PolyQuestEditor` Development Editor 编译记录，也没有 `GA_Skill_Whirlwind` 父类、五项配置、Montage Notify/Modifier/Root Motion/目标名/时间窗的逐项 Editor readback；作者化资产继续是本地 WIP。现有 test seam 不替代真实生产 Task/Montage 异步时序证明。上述是非阻塞 authored-validation debt，关闭触发器是用户完成指定 readback/编译，或提供真实 evidence-backed no-adoption。
- **文档与路线**：`plan.md` 保存本阶段详细基线、实现、证据和债务；`ROADMAP.md` 将 `TODO-03A7D` 记为源码收口并插入后续 `TODO-03H5：Post-Motion-Warp Combat Health Review v1`；由于缺少资产 readback，`ARCHITECTURE.md` 与 `README.md` 不写入 Whirlwind 已采用事实。
- **排除项与提交边界**：全部 `Content/**`、`Config/Automation/Presets/1.json`、其他 Source/文档 WIP、Blueprint/Montage/AnimBP/DataAsset/地图、`PolyQuest.uproject`、`Build.cs`、Gameplay Tags、旧 Test 项目和用户资产均未纳入；无资产清理、回滚、导入或 Editor 写入。最终提交 hash 以 Git 历史为准。
- **后续指针**：下一项为 `TODO-03H5：Post-Motion-Warp Combat Health Review v1`；该审查先于 `TODO-05A`，只修真实的生命周期/所有权/数据完整性或玩家回归阻塞，不扩展 Motion-Warp 功能。

## TODO-03A7F Melee Motion-Warp Trigger Range Contract Closeout (2026-08-31; working-tree snapshot at parent HEAD `7c3ddf0`, not clean)

> 本节记录 TODO-03A7F 的源码合同与验证收口。快照基于提交 `7c3ddf0`，工作区同时包含用户的 `Config/Automation/Presets/1.json`、大量 `Content/**` WIP 以及本阶段未提交 Source/文档改动，因此不是干净 HEAD 快照；本节仅作历史追溯，不替代源码、Config 或实际资产作为运行时权威。

- **范围与所有权**：共享 `FMeleeMotionWarpConfig`、`FComboChainEntry` 与 Light、Charged、Sprint、Melee Skill 四个 Player 消费者统一迁移到显式目标距离区间 `MinTriggerDistance <= WarpStopDistance <= MaxTriggerDistance`。`MaxTriggerDistance` 表示目标触发距离上限，不是位移预算；`Min < Stop` 隐式允许有界反向修正，`Min == Stop` 保持前向-only，精确停距为 no-op。既有一次性静态 Lock-On 快照、Player 窄桥、Task/Montage/EndAbility/UnPossess 清理和 `Trace -> Resolver -> Damage GE` 唯一路径保持不变。
- **实际实现路径**：本阶段修改了 11 个批准 Source/test 文件，包含共享 evaluator、Combo/Ability 字段映射与 debug 范围显示，以及 Section 1-6 的边界、反向、精确停距和四消费者回归覆盖；没有新增 Motion-Warp 消费者、目标跟随、自动重选、伤害路径、Gameplay Tag/Input 或全局服务。
- **用户证据**：用户确认 `PolyQuest.Combat.PlayerMeleeMotionWarping` focused Automation 与 `/Game/Maps/Scene01` PIE 通过。该证据只覆盖用户实际运行的场景，不扩展为全量回归、独立手动 `PolyQuestEditor` 编译或 authored 资产 readback。
- **静态与复核证据**：Gemini 报告批准文件 Rider error-level 检查、VS2022 编译和 `git diff --check` 通过；Main 本阶段执行一轮 diff-first、直接调用边界核对和 defect-first fresh review，未发现 P0/P1/P2 blocker。3.17 的旧 target 清理先发生在 `StartComboEntry()`，因此其 evaluator 自身清理断言存在非阻塞 P3 覆盖限制；不把该限制写成运行时失败。
- **未解决债务**：旧 `MaxWarpDistance` 到新字段的 authored-field migration、相关 GA/Combo/Montage/Notify/Modifier/Root Motion/目标名的逐项 Editor readback，以及独立用户手动 `PolyQuestEditor` 编译收据仍未形成。关闭条件是用户完成指定 readback/编译，或对不适用资产给出真实 evidence-backed no-adoption；在此之前不宣称 authored adoption 或干净可复现基线。
- **排除项与提交边界**：全部 `Content/**`、`Config/Automation/Presets/1.json`、其他 Source/Config/文档 WIP、Blueprint/Montage/AnimBP/DataAsset/地图、`PolyQuest.uproject`、`Build.cs`、Gameplay Tags、Input、旧 Test 项目和用户资产均未纳入；无资产清理、回滚、导入或 Editor 写入。下一阶段指针为 `TODO-03H5：Post-Motion-Warp Combat Health Review v1`。

## TODO-03H5 Post-Motion-Warp Combat Health Review Closeout (2026-08-31; HEAD `a3e0f78`, working-tree not clean)

> 本节记录 TODO-03H5 的 Review-Only 收口。快照基于当前父提交 `a3e0f78a1cfd9281d5ed9a29e4e9e7e4994d4535`；工作区仍含用户的 `Config/Automation/Presets/1.json` 与大量 `Content/**` WIP，因此不是干净 checkout 快照。本节仅作历史追溯，不替代源码、Config 或实际资产作为运行时权威。

- **审查范围与结论**：对 `LightAttackAbility`、`ChargedAttackAbility`、`SprintAttackAbility`、`PlayerMeleeSkillAbility`、共享 `MeleeMotionWarping` evaluator、Player/Enemy 生命周期桥、`ReadyForActivation()` 重入边界、ASC/Gameplay Tag/Input 直接契约，以及唯一 `UAbilityTask_MeleeTraceWindow -> FMeleeHitResolver -> Damage GameplayEffect` 路径完成一跳、diff-first 的健康审查。未发现 P0/P1/P2 blocker，也没有产生 Source、Config、Content 或资产修改。
- **设计定性**：`UnPossessed()` 当前只显式取消 `Ability.Attack.Light` 与 `Ability.Skill.Melee`；静态证据未显示 Charged/Sprint 因非对称取消列表产生 Warp、快照、野指针或 active-state 泄漏，因此保留为设计差异，交由 `TODO-03I1/03I2` 统一输入与 Tag 路线审查，不在 H5 扩大范围。A7F 3.17 exact-stop 集成断言限制仍是非阻塞 P3。
- **证据边界**：本阶段新增证据是 Gemini 的源码静态审查；A7B/C/D/F 的用户 focused Automation、Scene01 PIE 以及既有编译/静态收据仍分别归属于原阶段，不在 H5 重复运行，也不扩展为 H5 新的运行时或 authored-asset 证明。真实跨帧 GameplayTask 时序、GA/GE/Montage/Notify/Modifier/Root Motion readback 与独立手动 `PolyQuestEditor` 编译仍是验证债务。
- **排除项与后续指针**：未修改或纳入任何用户 `Config/**`、`Content/**`、资产、Gameplay Tags、Input、`ARCHITECTURE.md` 或其他 WIP；无编译、Editor 写入、Automation/PIE、stage 或清理操作属于本阶段审查本身。下一规划阶段为 `TODO-03I1：Unified Combat Input Contract And Loadout Simplification v1`。
